{
  "meta": {
    "rpact_version": "4.4.0",
    "r_version": "4.6.1",
    "generated": "2026-08-29 10:57:34 IST",
    "n_functions": 78
  },
  "functions": [
    {
      "name": "getAccrualTime",
      "family": "samplesize_power",
      "title": "Get Accrual Time",
      "description": "Returns an AccrualTime object that contains the accrual time and the accrual intensity.",
      "args": [
        {
          "name": "accrualTime",
          "required": false,
          "default": "NA_real_",
          "description": "The assumed accrual time intervals for the study, default is c(0, 12) (for details see getAccrualTime())."
        },
        {
          "name": "...",
          "required": true,
          "default": null,
          "description": "Ensures that all arguments (starting from the \"...\") are to be named and that a warning will be displayed if unknown arguments are passed."
        },
        {
          "name": "accrualIntensity",
          "required": false,
          "default": "NA_real_",
          "description": "A numeric vector of accrual intensities, default is the relative intensity 0.1 (for details see getAccrualTime())."
        },
        {
          "name": "accrualIntensityType",
          "required": false,
          "default": "c(\"auto\", \"absolute\", \"relative\")",
          "description": "A character value specifying the accrual intensity input type. Must be one of \"auto\", \"absolute\", or \"relative\"; default is \"auto\", i.e., if all values are < 1 the type is \"relative\", otherwise it is \"absolute\"."
        },
        {
          "name": "maxNumberOfSubjects",
          "required": false,
          "default": "NA_real_",
          "description": "The maximum number of subjects."
        }
      ]
    },
    {
      "name": "getAnalysisResults",
      "family": "analysis",
      "title": "Get Analysis Results",
      "description": "Calculates and returns the analysis results for the specified design and data.",
      "args": [
        {
          "name": "design",
          "required": true,
          "default": null,
          "description": "The trial design."
        },
        {
          "name": "dataInput",
          "required": true,
          "default": null,
          "description": "The summary data used for calculating the test results. This is either an element of DatasetMeans, of DatasetRates, or of DatasetSurvival and should be created with the function getDataset(). For more information see getDataset()."
        },
        {
          "name": "...",
          "required": true,
          "default": null,
          "description": "Further arguments to be passed to methods (cf., separate functions in \"See Also\" below), e.g., thetaH1 and stDevH1 (or assumedStDev / assumedStDevs), pi1, pi2, or piTreatments, piControl(s) The assumed effect size, standard deviation or rates to calculate the conditional power if nPlanned is specified. For survival designs, thetaH1 refers to the hazard ratio. For one-armed trials with binary outcome, only pi1 can be specified, for two-armed trials with binary outcome, pi1 and pi2 can be specified referring to the assumed treatment and control rate, respectively. In multi-armed or enrichment designs, you can specify a value or a vector with elements referring to the treatment arms or the sub-populations, respectively. For testing rates, the parameters to be specified are piTreatments and piControl (multi-arm designs) and piTreatments and piControls (enrichment designs). If not specified, the conditional power is calculated under the assumption of observed effect sizes, standard deviations, rates, or hazard ratios. iterationsIterations for simulating the power for Fisher's combination test. If the power for more than one remaining stages is to be determined for Fisher's combination test, it is estimated via simulation with specified iterations, the default is 1000. seedSeed for simulating the conditional power for Fisher's combination test. See above, default is a random seed. normalApproximationThe type of computation of the p-values. Default is FALSE for testing means (i.e., the t test is used) and TRUE for testing rates and the hazard ratio. For testing rates, if normalApproximation = FALSE is specified, the binomial test (one sample) or the exact test of Fisher (two samples) is used for calculating the p-values. In the survival setting, normalApproximation = FALSE has no effect. equalVariancesThe type of t test. For testing means in two treatment groups, either the t test assuming that the variances are equal or the t test without assuming this, i.e., the test of Welch-Satterthwaite is calculated, default is TRUE. stdErrorEstimateEstimate of standard error for calculation of final confidence intervals for comparing rates in two treatment groups, default is \"pooled\". intersectionTestDefines the multiple test for the intersection hypotheses in the closed system of hypotheses when testing multiple hypotheses. Five options are available in multi-arm designs: \"Dunnett\", \"Bonferroni\", \"Simes\", \"Sidak\", and \"Hierarchical\", default is \"Dunnett\". Four options are available in population enrichment designs: \"SpiessensDebois\" (one subset only), \"Bonferroni\", \"Simes\", and \"Sidak\", default is \"Simes\". varianceOptionDefines the way to calculate the variance in multiple treatment arms (> 2) or population enrichment designs for testing means. For multiple arms, three options are available: \"overallPooled\", \"pairwisePooled\", and \"notPooled\", default is \"overallPooled\". For enrichment designs, the options are: \"pooled\", \"pooledFromFull\" (one subset only), and \"notPooled\", default is \"pooled\". stratifiedAnalysisFor enrichment designs, typically a stratified analysis should be chosen. For testing means and rates, also a non-stratified analysis based on overall data can be performed. For survival data, only a stratified analysis is possible (see Brannath et al., 2009), default is TRUE."
        },
        {
          "name": "directionUpper",
          "required": false,
          "default": "NA",
          "description": "Logical. Specifies the direction of the alternative, only applicable for one-sided testing; default is TRUE which means that larger values of the test statistics yield smaller p-values."
        },
        {
          "name": "thetaH0",
          "required": false,
          "default": "NA_real_",
          "description": "The null hypothesis value, default is 0 for the normal and the binary case (testing means and rates, respectively), it is 1 for the survival case (testing the hazard ratio). For non-inferiority designs, thetaH0 is the non-inferiority bound. That is, in case of (one-sided) testing of means: a value != 0 (or a value != 1 for testing the mean ratio) can be specified. rates: a value != 0 (or a value != 1 for testing the risk ratio pi1 / pi2) can be specified. survival data: a bound for testing H0: hazard ratio = thetaH0 != 1 can be specified. count data: a bound for testing H0: lambda1 / lambda2 = thetaH0 != 1 can be specified. For testing a rate in one sample, a value thetaH0 in (0, 1) has to be specified for defining the null hypothesis H0: pi = thetaH0."
        },
        {
          "name": "nPlanned",
          "required": false,
          "default": "NA_real_",
          "description": "The additional (i.e., \"new\" and not cumulative) sample size planned for each of the subsequent stages. The argument must be a vector with length equal to the number of remaining stages and contain the combined sample size from both treatment groups if two groups are considered. For survival outcomes, it should contain the planned number of additional events. For multi-arm designs, it is the per-comparison (combined) sample size. For enrichment designs, it is the (combined) sample size for the considered sub-population."
        },
        {
          "name": "allocationRatioPlanned",
          "required": false,
          "default": "1",
          "description": "The planned allocation ratio n1 / n2 for a two treatment groups design, default is 1. For multi-arm designs, it is the allocation ratio relating the active arm(s) to the control. For simulating means and rates for a two treatment groups design, it can be a vector of length kMax, the number of stages. It can be a vector of length kMax, too, for multi-arm and enrichment designs. In these cases, a change of allocating subjects to treatment groups over the stages can be assessed. Note that internally allocationRatioPlanned is treated as a vector of length kMax, not a scalar."
        },
        {
          "name": "stage",
          "required": false,
          "default": "NA_integer_",
          "description": "The stage number (optional). Default: total number of existing stages in the data input."
        },
        {
          "name": "maxInformation",
          "required": false,
          "default": "NULL",
          "description": "Positive value specifying the maximum information."
        },
        {
          "name": "informationEpsilon",
          "required": false,
          "default": "NULL",
          "description": "Positive integer value specifying the absolute information epsilon, which defines the maximum distance from the observed information to the maximum information that causes the final analysis. Updates at the final analysis in case the observed information at the final analysis is smaller (\"under-running\") than the planned maximum information maxInformation, default is 0. Alternatively, a floating-point number > 0 and < 1 can be specified to define a relative information epsilon."
        }
      ]
    },
    {
      "name": "getAvailablePlotTypes",
      "family": "utility",
      "title": "Get Available Plot Types",
      "description": "Function to identify the available plot types of an object.",
      "args": [
        {
          "name": "obj",
          "required": true,
          "default": null,
          "description": "The object for which the plot types shall be identified, e.g. produced by getDesignGroupSequential() or getSampleSizeMeans()."
        },
        {
          "name": "output",
          "required": false,
          "default": "c(\"numeric\", \"caption\", \"numcap\", \"capnum\")",
          "description": "The output type. Can be one of c(\"numeric\", \"caption\", \"numcap\", \"capnum\")."
        },
        {
          "name": "numberInCaptionEnabled",
          "required": false,
          "default": "FALSE",
          "description": "If TRUE, the number will be added to the caption, default is FALSE."
        }
      ]
    },
    {
      "name": "getClosedCombinationTestResults",
      "family": "analysis",
      "title": "Get Closed Combination Test Results",
      "description": "Calculates and returns the results from the closed combination test in multi-arm and population enrichment designs.",
      "args": [
        {
          "name": "stageResults",
          "required": true,
          "default": null,
          "description": "The results at given stage, obtained from getStageResults()."
        }
      ]
    },
    {
      "name": "getClosedConditionalDunnettTestResults",
      "family": "analysis",
      "title": "Get Closed Conditional Dunnett Test Results",
      "description": "Calculates and returns the results from the closed conditional Dunnett test.",
      "args": [
        {
          "name": "stageResults",
          "required": true,
          "default": null,
          "description": "The results at given stage, obtained from getStageResults()."
        },
        {
          "name": "...",
          "required": true,
          "default": null,
          "description": "Ensures that all arguments (starting from the \"...\") are to be named and that a warning will be displayed if unknown arguments are passed."
        },
        {
          "name": "stage",
          "required": false,
          "default": "stageResults$stage",
          "description": "The stage number (optional). Default: total number of existing stages in the data input."
        }
      ]
    },
    {
      "name": "getConditionalPower",
      "family": "analysis",
      "title": "Get Conditional Power",
      "description": "Calculates and returns the conditional power.",
      "args": [
        {
          "name": "stageResults",
          "required": true,
          "default": null,
          "description": "The results at given stage, obtained from getStageResults()."
        },
        {
          "name": "...",
          "required": true,
          "default": null,
          "description": "Further (optional) arguments to be passed: thetaH1 and stDevH1 (or assumedStDev / assumedStDevs), pi1, pi2, or piTreatments, piControl(s) The assumed effect size, standard deviation or rates to calculate the conditional power if nPlanned is specified. For survival designs, thetaH1 refers to the hazard ratio. For one-armed trials with binary outcome, only pi1 can be specified, for two-armed trials with binary outcome, pi1 and pi2 can be specified referring to the assumed treatment and control rate, respectively. In multi-armed or enrichment designs, you can specify a value or a vector with elements referring to the treatment arms or the sub-populations, respectively. For testing rates, the parameters to be specified are piTreatments and piControl (multi-arm designs) and piTreatments and piControls (enrichment designs). If not specified, the conditional power is calculated under the assumption of observed effect sizes, standard deviations, rates, or hazard ratios. iterationsIterations for simulating the power for Fisher's combination test. If the power for more than one remaining stages is to be determined for Fisher's combination test, it is estimated via simulation with specified iterations, the default is 1000. seedSeed for simulating the conditional power for Fisher's combination test. See above, default is a random seed."
        },
        {
          "name": "nPlanned",
          "required": true,
          "default": null,
          "description": "The additional (i.e., \"new\" and not cumulative) sample size planned for each of the subsequent stages. The argument must be a vector with length equal to the number of remaining stages and contain the combined sample size from both treatment groups if two groups are considered. For survival outcomes, it should contain the planned number of additional events. For multi-arm designs, it is the per-comparison (combined) sample size. For enrichment designs, it is the (combined) sample size for the considered sub-population."
        },
        {
          "name": "allocationRatioPlanned",
          "required": false,
          "default": "1",
          "description": "The planned allocation ratio n1 / n2 for a two treatment groups design, default is 1. For multi-arm designs, it is the allocation ratio relating the active arm(s) to the control. For simulating means and rates for a two treatment groups design, it can be a vector of length kMax, the number of stages. It can be a vector of length kMax, too, for multi-arm and enrichment designs. In these cases, a change of allocating subjects to treatment groups over the stages can be assessed. Note that internally allocationRatioPlanned is treated as a vector of length kMax, not a scalar."
        }
      ]
    },
    {
      "name": "getConditionalRejectionProbabilities",
      "family": "analysis",
      "title": "Get Conditional Rejection Probabilities",
      "description": "Calculates the conditional rejection probabilities (CRP) for given test results.",
      "args": [
        {
          "name": "stageResults",
          "required": true,
          "default": null,
          "description": "The results at given stage, obtained from getStageResults()."
        },
        {
          "name": "...",
          "required": true,
          "default": null,
          "description": "Further (optional) arguments to be passed: iterationsIterations for simulating the conditional rejection probabilities for Fisher's combination test. For checking purposes, it can be estimated via simulation with specified iterations. seedSeed for simulating the conditional rejection probabilities for Fisher's combination test. See above, default is a random seed."
        }
      ]
    },
    {
      "name": "getData",
      "family": "simulation",
      "title": "Get Simulation Data",
      "description": "Returns the aggregated simulation data.",
      "args": [
        {
          "name": "x",
          "required": true,
          "default": null,
          "description": "A SimulationResults object created by getSimulationMeans(), getSimulationRates(), getSimulationSurvival(), getSimulationMultiArmMeans(), getSimulationMultiArmRates(), or getSimulationMultiArmSurvival()."
        }
      ]
    },
    {
      "name": "getData.SimulationResults",
      "family": "simulation",
      "title": "Get Simulation Data",
      "description": "Returns the aggregated simulation data.",
      "args": [
        {
          "name": "x",
          "required": true,
          "default": null,
          "description": "A SimulationResults object created by getSimulationMeans(), getSimulationRates(), getSimulationSurvival(), getSimulationMultiArmMeans(), getSimulationMultiArmRates(), or getSimulationMultiArmSurvival()."
        }
      ]
    },
    {
      "name": "getDataset",
      "family": "analysis",
      "title": "Get Dataset",
      "description": "Creates a dataset object and returns it.",
      "args": [
        {
          "name": "...",
          "required": true,
          "default": null,
          "description": "A data.frame or some data vectors defining the dataset."
        },
        {
          "name": "floatingPointNumbersEnabled",
          "required": false,
          "default": "FALSE",
          "description": "If TRUE, sample sizes and event numbers can be specified as floating-point numbers (this make sense, e.g., for theoretical comparisons); by default floatingPointNumbersEnabled = FALSE, i.e., samples sizes and event numbers defined as floating-point numbers will be truncated."
        }
      ]
    },
    {
      "name": "getDataSet",
      "family": "analysis",
      "title": "Get Dataset",
      "description": "Creates a dataset object and returns it.",
      "args": [
        {
          "name": "...",
          "required": true,
          "default": null,
          "description": "A data.frame or some data vectors defining the dataset."
        },
        {
          "name": "floatingPointNumbersEnabled",
          "required": false,
          "default": "FALSE",
          "description": "If TRUE, sample sizes and event numbers can be specified as floating-point numbers (this make sense, e.g., for theoretical comparisons); by default floatingPointNumbersEnabled = FALSE, i.e., samples sizes and event numbers defined as floating-point numbers will be truncated."
        }
      ]
    },
    {
      "name": "getDesignCharacteristics",
      "family": "design",
      "title": "Get Design Characteristics",
      "description": "Calculates the characteristics of a design and returns it.",
      "args": [
        {
          "name": "design",
          "required": false,
          "default": "NULL",
          "description": "The trial design."
        },
        {
          "name": "...",
          "required": true,
          "default": null,
          "description": "Ensures that all arguments (starting from the \"...\") are to be named and that a warning will be displayed if unknown arguments are passed."
        }
      ]
    },
    {
      "name": "getDesignConditionalDunnett",
      "family": "design",
      "title": "Get Design Conditional Dunnett Test",
      "description": "Defines the design to perform an analysis with the conditional Dunnett test.",
      "args": [
        {
          "name": "alpha",
          "required": false,
          "default": "0.025",
          "description": "The significance level alpha, default is 0.025. Must be a positive numeric of length 1."
        },
        {
          "name": "informationAtInterim",
          "required": false,
          "default": "0.5",
          "description": "The information to be expected at interim, default is informationAtInterim = 0.5."
        },
        {
          "name": "...",
          "required": true,
          "default": null,
          "description": "Ensures that all arguments (starting from the \"...\") are to be named and that a warning will be displayed if unknown arguments are passed."
        },
        {
          "name": "secondStageConditioning",
          "required": false,
          "default": "TRUE",
          "description": "The way the second stage p-values are calculated within the closed system of hypotheses. If secondStageConditioning = FALSE is specified, the unconditional adjusted p-values are used, otherwise conditional adjusted p-values are calculated, default is secondStageConditioning = TRUE (for details, see Koenig et al., 2008)."
        },
        {
          "name": "directionUpper",
          "required": false,
          "default": "NA",
          "description": "Logical. Specifies the direction of the alternative, only applicable for one-sided testing; default is TRUE which means that larger values of the test statistics yield smaller p-values."
        }
      ]
    },
    {
      "name": "getDesignFisher",
      "family": "design",
      "title": "Get Design Fisher",
      "description": "Performs Fisher's combination test and returns critical values for this design.",
      "args": [
        {
          "name": "...",
          "required": true,
          "default": null,
          "description": "Ensures that all arguments (starting from the \"...\") are to be named and that a warning will be displayed if unknown arguments are passed."
        },
        {
          "name": "kMax",
          "required": false,
          "default": "NA_integer_",
          "description": "The maximum number of stages K. Must be a positive integer of length 1 (default value is 3). The maximum selectable kMax is 20 for group sequential or inverse normal and 6 for Fisher combination test designs."
        },
        {
          "name": "alpha",
          "required": false,
          "default": "NA_real_",
          "description": "The significance level alpha, default is 0.025. Must be a positive numeric of length 1."
        },
        {
          "name": "method",
          "required": false,
          "default": "c(\"equalAlpha\", \"fullAlpha\", \"noInteraction\", \"userDefinedAlpha\")",
          "description": "\"equalAlpha\", \"fullAlpha\", \"noInteraction\", or \"userDefinedAlpha\", default is \"equalAlpha\" (for details, see Wassmer, 1999)."
        },
        {
          "name": "userAlphaSpending",
          "required": false,
          "default": "NA_real_",
          "description": "The user defined alpha spending. Numeric vector of length kMax containing the cumulative alpha-spending (Type I error rate) up to each interim stage: 0 <= alpha_1 <= ... <= alpha_K <= alpha."
        },
        {
          "name": "alpha0Vec",
          "required": false,
          "default": "NA_real_",
          "description": "Stopping for futility bounds for stage-wise p-values."
        },
        {
          "name": "alpha0Scale",
          "required": false,
          "default": "c(\"pValue\", \"zValue\", \"condPowerAtObserved\", \"predictivePower\")",
          "description": "Character. The scale of the futility bounds. Must be one of \"pValue\", \"zValue\", \"condPowerAtObserved\", or \"predictivePower\". Default is \"pValue\"."
        },
        {
          "name": "informationRates",
          "required": false,
          "default": "NA_real_",
          "description": "The information rates t_1, ..., t_kMax (that must be fixed prior to the trial), default is (1:kMax) / kMax. For the weighted inverse normal design, the weights are derived through w_1 = sqrt(t_1), and w_k = sqrt(t_k - t_(k-1)). For the weighted Fisher's combination test, the weights (scales) are w_k = sqrt((t_k - t_(k-1)) / t_1) (see the documentation)."
        },
        {
          "name": "sided",
          "required": false,
          "default": "1",
          "description": "Is the alternative one-sided (1) or two-sided (2), default is 1. Must be a positive integer of length 1."
        },
        {
          "name": "bindingFutility",
          "required": false,
          "default": "NA",
          "description": "If bindingFutility = TRUE is specified the calculation of the critical values is affected by the futility bounds (default is TRUE)."
        },
        {
          "name": "directionUpper",
          "required": false,
          "default": "NA",
          "description": "Logical. Specifies the direction of the alternative, only applicable for one-sided testing; default is TRUE which means that larger values of the test statistics yield smaller p-values."
        },
        {
          "name": "tolerance",
          "required": false,
          "default": "1e-14",
          "description": "The numerical tolerance, default is 1e-14."
        },
        {
          "name": "iterations",
          "required": false,
          "default": "0",
          "description": "The number of simulation iterations, e.g., getDesignFisher(iterations = 100000) checks the validity of the critical values for the design. The default value of iterations is 0, i.e., no simulation will be executed."
        },
        {
          "name": "seed",
          "required": false,
          "default": "NA_real_",
          "description": "Seed for simulating the power for Fisher's combination test. See above, default is a random seed."
        }
      ]
    },
    {
      "name": "getDesignGroupSequential",
      "family": "design",
      "title": "Get Design Group Sequential",
      "description": "Provides adjusted boundaries and defines a group sequential design.",
      "args": [
        {
          "name": "...",
          "required": true,
          "default": null,
          "description": "Ensures that all arguments (starting from the \"...\") are to be named and that a warning will be displayed if unknown arguments are passed."
        },
        {
          "name": "kMax",
          "required": false,
          "default": "NA_integer_",
          "description": "The maximum number of stages K. Must be a positive integer of length 1 (default value is 3). The maximum selectable kMax is 20 for group sequential or inverse normal and 6 for Fisher combination test designs."
        },
        {
          "name": "alpha",
          "required": false,
          "default": "NA_real_",
          "description": "The significance level alpha, default is 0.025. Must be a positive numeric of length 1."
        },
        {
          "name": "beta",
          "required": false,
          "default": "NA_real_",
          "description": "Type II error rate, necessary for providing sample size calculations (e.g., getSampleSizeMeans()), beta spending function designs, or optimum designs, default is 0.20. Must be a positive numeric of length 1."
        },
        {
          "name": "sided",
          "required": false,
          "default": "1L",
          "description": "Is the alternative one-sided (1) or two-sided (2), default is 1. Must be a positive integer of length 1."
        },
        {
          "name": "informationRates",
          "required": false,
          "default": "NA_real_",
          "description": "The information rates t_1, ..., t_kMax (that must be fixed prior to the trial), default is (1:kMax) / kMax. For the weighted inverse normal design, the weights are derived through w_1 = sqrt(t_1), and w_k = sqrt(t_k - t_(k-1)). For the weighted Fisher's combination test, the weights (scales) are w_k = sqrt((t_k - t_(k-1)) / t_1) (see the documentation)."
        },
        {
          "name": "futilityBounds",
          "required": false,
          "default": "NA_real_",
          "description": "The futility bounds, defined on the scale defined by futilityBoundsScale. (numeric vector of length kMax - 1)."
        },
        {
          "name": "typeOfDesign",
          "required": false,
          "default": "c(\"OF\", \"P\", \"WT\", \"PT\", \"HP\", \"WToptimum\", \"asP\", \"asOF\", \"asKD\",      \"asHSD\", \"asUser\", \"noEarlyEfficacy\")",
          "description": "The type of design. Type of design is one of the following: O'Brien & Fleming (\"OF\"), Pocock (\"P\"), Wang & Tsiatis Delta class (\"WT\"), Pampallona & Tsiatis (\"PT\"), Haybittle & Peto (\"HP\"), Optimum design within Wang & Tsiatis class (\"WToptimum\"), O'Brien & Fleming type alpha spending (\"asOF\"), Pocock type alpha spending (\"asP\"), Kim & DeMets alpha spending (\"asKD\"), Hwang, Shi & DeCani alpha spending (\"asHSD\"), user defined alpha spending (\"asUser\"), no early efficacy stop (\"noEarlyEfficacy\"), default is \"OF\"."
        },
        {
          "name": "deltaWT",
          "required": false,
          "default": "NA_real_",
          "description": "Delta for Wang & Tsiatis Delta class."
        },
        {
          "name": "deltaPT1",
          "required": false,
          "default": "NA_real_",
          "description": "Delta1 for Pampallona & Tsiatis class rejecting H0 boundaries."
        },
        {
          "name": "deltaPT0",
          "required": false,
          "default": "NA_real_",
          "description": "Delta0 for Pampallona & Tsiatis class rejecting H1 boundaries."
        },
        {
          "name": "optimizationCriterion",
          "required": false,
          "default": "c(\"ASNH1\", \"ASNIFH1\", \"ASNsum\")",
          "description": "Optimization criterion for optimum design within Wang & Tsiatis class (\"ASNH1\", \"ASNIFH1\", \"ASNsum\"), default is \"ASNH1\", see details."
        },
        {
          "name": "gammaA",
          "required": false,
          "default": "NA_real_",
          "description": "Parameter for alpha spending function."
        },
        {
          "name": "typeBetaSpending",
          "required": false,
          "default": "c(\"none\", \"bsP\", \"bsOF\", \"bsKD\", \"bsHSD\", \"bsUser\")",
          "description": "Type of beta spending. Type of of beta spending is one of the following: O'Brien & Fleming type beta spending, Pocock type beta spending, Kim & DeMets beta spending, Hwang, Shi & DeCani beta spending, user defined beta spending (\"bsOF\", \"bsP\", \"bsKD\", \"bsHSD\", \"bsUser\", default is \"none\")."
        },
        {
          "name": "userAlphaSpending",
          "required": false,
          "default": "NA_real_",
          "description": "The user defined alpha spending. Numeric vector of length kMax containing the cumulative alpha-spending (Type I error rate) up to each interim stage: 0 <= alpha_1 <= ... <= alpha_K <= alpha."
        },
        {
          "name": "userBetaSpending",
          "required": false,
          "default": "NA_real_",
          "description": "The user defined beta spending. Vector of length kMax containing the cumulative beta-spending up to each interim stage."
        },
        {
          "name": "efficacyStops",
          "required": false,
          "default": "NA",
          "description": "Logical vector of length kMax - 1 indicating efficacy stops. Default is NA."
        },
        {
          "name": "futilityStops",
          "required": false,
          "default": "NA",
          "description": "Logical vector of length kMax - 1 indicating futility stops. Default is NA."
        },
        {
          "name": "gammaB",
          "required": false,
          "default": "NA_real_",
          "description": "Parameter for beta spending function."
        },
        {
          "name": "bindingFutility",
          "required": false,
          "default": "NA",
          "description": "Logical. If bindingFutility = TRUE is specified the calculation of the critical values is affected by the futility bounds and the futility threshold is binding in the sense that the study must be stopped if the futility condition was reached (default is FALSE)."
        },
        {
          "name": "futilityBoundsScale",
          "required": false,
          "default": "c(\"zValue\", \"pValue\", \"reverseCondPower\", \"condPowerAtObserved\",      \"predictivePower\")",
          "description": "Character. The scale of the futility bounds. Must be one of \"zValue\", \"pValue\", \"reverseCondPower\", \"condPowerAtObserved\", or \"predictivePower\". Default is \"zValue\"."
        },
        {
          "name": "directionUpper",
          "required": false,
          "default": "NA",
          "description": "Logical. Specifies the direction of the alternative, only applicable for one-sided testing; default is TRUE which means that larger values of the test statistics yield smaller p-values."
        },
        {
          "name": "betaAdjustment",
          "required": false,
          "default": "NA",
          "description": "For two-sided beta spending designs, if betaAdjustement = TRUE a linear adjustment of the beta spending values is performed if an overlapping of decision regions for futility stopping at earlier stages occurs, otherwise no adjustment is performed (default is TRUE)."
        },
        {
          "name": "constantBoundsHP",
          "required": false,
          "default": "3",
          "description": "The constant bounds up to stage kMax - 1 for the Haybittle & Peto design (default is 3)."
        },
        {
          "name": "twoSidedPower",
          "required": false,
          "default": "NA",
          "description": "For two-sided testing, if twoSidedPower = TRUE is specified the sample size calculation is performed by considering both tails of the distribution. Default is FALSE, i.e., it is assumed that one tail probability is equal to 0 or the power should be directed to one part."
        },
        {
          "name": "delayedInformation",
          "required": false,
          "default": "NA_real_",
          "description": "Delay of information for delayed response designs. Can be a numeric value or a numeric vector of length kMax - 1"
        },
        {
          "name": "tolerance",
          "required": false,
          "default": "1e-08",
          "description": "The numerical tolerance, default is 1e-08."
        }
      ]
    },
    {
      "name": "getDesignInverseNormal",
      "family": "design",
      "title": "Get Design Inverse Normal",
      "description": "Provides adjusted boundaries and defines a group sequential design for its use in the inverse normal combination test.",
      "args": [
        {
          "name": "...",
          "required": true,
          "default": null,
          "description": "Ensures that all arguments (starting from the \"...\") are to be named and that a warning will be displayed if unknown arguments are passed."
        },
        {
          "name": "kMax",
          "required": false,
          "default": "NA_integer_",
          "description": "The maximum number of stages K. Must be a positive integer of length 1 (default value is 3). The maximum selectable kMax is 20 for group sequential or inverse normal and 6 for Fisher combination test designs."
        },
        {
          "name": "alpha",
          "required": false,
          "default": "NA_real_",
          "description": "The significance level alpha, default is 0.025. Must be a positive numeric of length 1."
        },
        {
          "name": "beta",
          "required": false,
          "default": "NA_real_",
          "description": "Type II error rate, necessary for providing sample size calculations (e.g., getSampleSizeMeans()), beta spending function designs, or optimum designs, default is 0.20. Must be a positive numeric of length 1."
        },
        {
          "name": "sided",
          "required": false,
          "default": "1L",
          "description": "Is the alternative one-sided (1) or two-sided (2), default is 1. Must be a positive integer of length 1."
        },
        {
          "name": "informationRates",
          "required": false,
          "default": "NA_real_",
          "description": "The information rates t_1, ..., t_kMax (that must be fixed prior to the trial), default is (1:kMax) / kMax. For the weighted inverse normal design, the weights are derived through w_1 = sqrt(t_1), and w_k = sqrt(t_k - t_(k-1)). For the weighted Fisher's combination test, the weights (scales) are w_k = sqrt((t_k - t_(k-1)) / t_1) (see the documentation)."
        },
        {
          "name": "futilityBounds",
          "required": false,
          "default": "NA_real_",
          "description": "The futility bounds, defined on the scale defined by futilityBoundsScale. (numeric vector of length kMax - 1)."
        },
        {
          "name": "typeOfDesign",
          "required": false,
          "default": "c(\"OF\", \"P\", \"WT\", \"PT\", \"HP\", \"WToptimum\", \"asP\", \"asOF\", \"asKD\",      \"asHSD\", \"asUser\", \"noEarlyEfficacy\")",
          "description": "The type of design. Type of design is one of the following: O'Brien & Fleming (\"OF\"), Pocock (\"P\"), Wang & Tsiatis Delta class (\"WT\"), Pampallona & Tsiatis (\"PT\"), Haybittle & Peto (\"HP\"), Optimum design within Wang & Tsiatis class (\"WToptimum\"), O'Brien & Fleming type alpha spending (\"asOF\"), Pocock type alpha spending (\"asP\"), Kim & DeMets alpha spending (\"asKD\"), Hwang, Shi & DeCani alpha spending (\"asHSD\"), user defined alpha spending (\"asUser\"), no early efficacy stop (\"noEarlyEfficacy\"), default is \"OF\"."
        },
        {
          "name": "deltaWT",
          "required": false,
          "default": "NA_real_",
          "description": "Delta for Wang & Tsiatis Delta class."
        },
        {
          "name": "deltaPT1",
          "required": false,
          "default": "NA_real_",
          "description": "Delta1 for Pampallona & Tsiatis class rejecting H0 boundaries."
        },
        {
          "name": "deltaPT0",
          "required": false,
          "default": "NA_real_",
          "description": "Delta0 for Pampallona & Tsiatis class rejecting H1 boundaries."
        },
        {
          "name": "optimizationCriterion",
          "required": false,
          "default": "c(\"ASNH1\", \"ASNIFH1\", \"ASNsum\")",
          "description": "Optimization criterion for optimum design within Wang & Tsiatis class (\"ASNH1\", \"ASNIFH1\", \"ASNsum\"), default is \"ASNH1\", see details."
        },
        {
          "name": "gammaA",
          "required": false,
          "default": "NA_real_",
          "description": "Parameter for alpha spending function."
        },
        {
          "name": "typeBetaSpending",
          "required": false,
          "default": "c(\"none\", \"bsP\", \"bsOF\", \"bsKD\", \"bsHSD\", \"bsUser\")",
          "description": "Type of beta spending. Type of of beta spending is one of the following: O'Brien & Fleming type beta spending, Pocock type beta spending, Kim & DeMets beta spending, Hwang, Shi & DeCani beta spending, user defined beta spending (\"bsOF\", \"bsP\", \"bsKD\", \"bsHSD\", \"bsUser\", default is \"none\")."
        },
        {
          "name": "userAlphaSpending",
          "required": false,
          "default": "NA_real_",
          "description": "The user defined alpha spending. Numeric vector of length kMax containing the cumulative alpha-spending (Type I error rate) up to each interim stage: 0 <= alpha_1 <= ... <= alpha_K <= alpha."
        },
        {
          "name": "userBetaSpending",
          "required": false,
          "default": "NA_real_",
          "description": "The user defined beta spending. Vector of length kMax containing the cumulative beta-spending up to each interim stage."
        },
        {
          "name": "efficacyStops",
          "required": false,
          "default": "NA",
          "description": "Logical vector of length kMax - 1 indicating efficacy stops. Default is NA."
        },
        {
          "name": "futilityStops",
          "required": false,
          "default": "NA",
          "description": "Logical vector of length kMax - 1 indicating futility stops. Default is NA."
        },
        {
          "name": "gammaB",
          "required": false,
          "default": "NA_real_",
          "description": "Parameter for beta spending function."
        },
        {
          "name": "bindingFutility",
          "required": false,
          "default": "NA",
          "description": "Logical. If bindingFutility = TRUE is specified the calculation of the critical values is affected by the futility bounds and the futility threshold is binding in the sense that the study must be stopped if the futility condition was reached (default is FALSE)."
        },
        {
          "name": "futilityBoundsScale",
          "required": false,
          "default": "c(\"zValue\", \"pValue\", \"reverseCondPower\", \"condPowerAtObserved\",      \"predictivePower\")",
          "description": "Character. The scale of the futility bounds. Must be one of \"zValue\", \"pValue\", \"reverseCondPower\", \"condPowerAtObserved\", or \"predictivePower\". Default is \"zValue\"."
        },
        {
          "name": "directionUpper",
          "required": false,
          "default": "NA",
          "description": "Logical. Specifies the direction of the alternative, only applicable for one-sided testing; default is TRUE which means that larger values of the test statistics yield smaller p-values."
        },
        {
          "name": "betaAdjustment",
          "required": false,
          "default": "NA",
          "description": "For two-sided beta spending designs, if betaAdjustement = TRUE a linear adjustment of the beta spending values is performed if an overlapping of decision regions for futility stopping at earlier stages occurs, otherwise no adjustment is performed (default is TRUE)."
        },
        {
          "name": "constantBoundsHP",
          "required": false,
          "default": "3",
          "description": "The constant bounds up to stage kMax - 1 for the Haybittle & Peto design (default is 3)."
        },
        {
          "name": "twoSidedPower",
          "required": false,
          "default": "NA",
          "description": "For two-sided testing, if twoSidedPower = TRUE is specified the sample size calculation is performed by considering both tails of the distribution. Default is FALSE, i.e., it is assumed that one tail probability is equal to 0 or the power should be directed to one part."
        },
        {
          "name": "tolerance",
          "required": false,
          "default": "1e-08",
          "description": "The numerical tolerance, default is 1e-08."
        }
      ]
    },
    {
      "name": "getDesignSet",
      "family": "design",
      "title": "Get Design Set",
      "description": "Creates a trial design set object and returns it.",
      "args": [
        {
          "name": "...",
          "required": true,
          "default": null,
          "description": "designs or design and one or more design parameters, e.g., deltaWT = c(0.1, 0.3, 0.4). design The master design (optional, you need to specify an additional parameter that shall be varied). designs The designs to compare (optional, you need to specify the variable variedParameters)."
        }
      ]
    },
    {
      "name": "getEventProbabilities",
      "family": "samplesize_power",
      "title": "Get Event Probabilities",
      "description": "Returns the event probabilities for specified parameters at given time vector.",
      "args": [
        {
          "name": "time",
          "required": true,
          "default": null,
          "description": "A numeric vector with time values."
        },
        {
          "name": "...",
          "required": true,
          "default": null,
          "description": "Ensures that all arguments (starting from the \"...\") are to be named and that a warning will be displayed if unknown arguments are passed."
        },
        {
          "name": "accrualTime",
          "required": false,
          "default": "c(0, 12)",
          "description": "The assumed accrual time intervals for the study, default is c(0, 12) (for details see getAccrualTime())."
        },
        {
          "name": "accrualIntensity",
          "required": false,
          "default": "0.1",
          "description": "A numeric vector of accrual intensities, default is the relative intensity 0.1 (for details see getAccrualTime())."
        },
        {
          "name": "accrualIntensityType",
          "required": false,
          "default": "c(\"auto\", \"absolute\", \"relative\")",
          "description": "A character value specifying the accrual intensity input type. Must be one of \"auto\", \"absolute\", or \"relative\"; default is \"auto\", i.e., if all values are < 1 the type is \"relative\", otherwise it is \"absolute\"."
        },
        {
          "name": "kappa",
          "required": false,
          "default": "1",
          "description": "A numeric value > 0. A kappa != 1 will be used for the specification of the shape of the Weibull distribution. Default is 1, i.e., the exponential survival distribution is used instead of the Weibull distribution. Note that the Weibull distribution cannot be used for the piecewise definition of the survival time distribution, i.e., only piecewiselambda (as a single value) and kappa can be specified. This function is equivalent to pweibull(t, shape = kappa, scale = 1 / lambda) of the stats package, i.e., the scale parameter is 1 / 'hazard rate'. For example, getPiecewiseExponentialDistribution(time = 130, piecewiseLambda = 0.01, kappa = 4.2) and pweibull(q = 130, shape = 4.2, scale = 1 / 0.01) provide the same result."
        },
        {
          "name": "piecewiseSurvivalTime",
          "required": false,
          "default": "NA_real_",
          "description": "A vector that specifies the time intervals for the piecewise definition of the exponential survival time cumulative distribution function (for details see getPiecewiseSurvivalTime())."
        },
        {
          "name": "lambda2",
          "required": false,
          "default": "NA_real_",
          "description": "The assumed hazard rate in the reference group, there is no default. lambda2 can also be used to define piecewise exponentially distributed survival times (see details). Must be a positive numeric of length 1."
        },
        {
          "name": "lambda1",
          "required": false,
          "default": "NA_real_",
          "description": "The assumed hazard rate in the treatment group, there is no default. lambda1 can also be used to define piecewise exponentially distributed survival times (see details). Must be a positive numeric of length 1."
        },
        {
          "name": "allocationRatioPlanned",
          "required": false,
          "default": "1",
          "description": "The planned allocation ratio n1 / n2 for a two treatment groups design, default is 1. If allocationRatioPlanned = 0 is entered, the optimal allocation ratio yielding the smallest overall sample size is determined."
        },
        {
          "name": "hazardRatio",
          "required": false,
          "default": "NA_real_",
          "description": "The vector of hazard ratios under consideration. If the event or hazard rates in both treatment groups are defined, the hazard ratio needs not to be specified as it is calculated, there is no default. Must be a positive numeric of length 1."
        },
        {
          "name": "dropoutRate1",
          "required": false,
          "default": "0",
          "description": "The assumed drop-out rate in the treatment group, default is 0."
        },
        {
          "name": "dropoutRate2",
          "required": false,
          "default": "0",
          "description": "The assumed drop-out rate in the control group, default is 0."
        },
        {
          "name": "dropoutTime",
          "required": false,
          "default": "12",
          "description": "The assumed time for drop-out rates in the control and the treatment group, default is 12."
        },
        {
          "name": "maxNumberOfSubjects",
          "required": false,
          "default": "NA_real_",
          "description": "If maxNumberOfSubjects > 0 is specified, the end of accrual at specified accrualIntensity for the specified number of subjects is determined or accrualIntensity is calculated at fixed end of accrual."
        }
      ]
    },
    {
      "name": "getFinalConfidenceInterval",
      "family": "analysis",
      "title": "Get Final Confidence Interval",
      "description": "Returns the final confidence interval for the parameter of interest. It is based on the prototype case, i.e., the test for testing a mean for normally distributed variables.",
      "args": [
        {
          "name": "design",
          "required": true,
          "default": null,
          "description": "The trial design."
        },
        {
          "name": "dataInput",
          "required": true,
          "default": null,
          "description": "The summary data used for calculating the test results. This is either an element of DatasetMeans, of DatasetRates, or of DatasetSurvival and should be created with the function getDataset(). For more information see getDataset()."
        },
        {
          "name": "...",
          "required": true,
          "default": null,
          "description": "Further (optional) arguments to be passed: normalApproximation The type of computation of the p-values. Default is FALSE for testing means (i.e., the t test is used) and TRUE for testing rates and the hazard ratio. For testing rates, if normalApproximation = FALSE is specified, the binomial test (one sample) or the exact test of Fisher (two samples) is used for calculating the p-values. In the survival setting, normalApproximation = FALSE has no effect. equalVariancesThe type of t test. For testing means in two treatment groups, either the t test assuming that the variances are equal or the t test without assuming this, i.e., the test of Welch-Satterthwaite is calculated, default is TRUE. stdErrorEstimateEstimate of standard error for calculation of final confidence intervals for comparing rates in two treatment groups, default is \"pooled\"."
        },
        {
          "name": "directionUpper",
          "required": false,
          "default": "NA",
          "description": "Logical. Specifies the direction of the alternative, only applicable for one-sided testing; default is TRUE which means that larger values of the test statistics yield smaller p-values."
        },
        {
          "name": "thetaH0",
          "required": false,
          "default": "NA_real_",
          "description": "The null hypothesis value, default is 0 for the normal and the binary case (testing means and rates, respectively), it is 1 for the survival case (testing the hazard ratio). For non-inferiority designs, thetaH0 is the non-inferiority bound. That is, in case of (one-sided) testing of means: a value != 0 (or a value != 1 for testing the mean ratio) can be specified. rates: a value != 0 (or a value != 1 for testing the risk ratio pi1 / pi2) can be specified. survival data: a bound for testing H0: hazard ratio = thetaH0 != 1 can be specified. count data: a bound for testing H0: lambda1 / lambda2 = thetaH0 != 1 can be specified. For testing a rate in one sample, a value thetaH0 in (0, 1) has to be specified for defining the null hypothesis H0: pi = thetaH0."
        },
        {
          "name": "tolerance",
          "required": false,
          "default": "1e-06",
          "description": "The numerical tolerance, default is 1e-06. Must be a positive numeric of length 1."
        },
        {
          "name": "stage",
          "required": false,
          "default": "NA_integer_",
          "description": "The stage number (optional). Default: total number of existing stages in the data input."
        }
      ]
    },
    {
      "name": "getFinalPValue",
      "family": "analysis",
      "title": "Get Final P Value",
      "description": "Returns the final p-value for given stage results.",
      "args": [
        {
          "name": "stageResults",
          "required": true,
          "default": null,
          "description": "The results at given stage, obtained from getStageResults()."
        },
        {
          "name": "...",
          "required": true,
          "default": null,
          "description": "Only available for backward compatibility."
        }
      ]
    },
    {
      "name": "getFutilityBounds",
      "family": "design",
      "title": "Get Futility Bounds",
      "description": "This function converts futility bounds between different scales such as z-value, p-value, conditional power, predictive power, reverse conditional power, and effect estimate.",
      "args": [
        {
          "name": "sourceValue",
          "required": true,
          "default": null,
          "description": "A numeric vector representing the futility bounds in the source scale."
        },
        {
          "name": "...",
          "required": true,
          "default": null,
          "description": "Ensures that all arguments (starting from the \"...\") are to be named and that a warning will be displayed if unknown arguments are passed."
        },
        {
          "name": "sourceScale",
          "required": false,
          "default": "c(\"zValue\", \"pValue\", \"conditionalPower\", \"condPowerAtObserved\",      \"predictivePower\", \"reverseCondPower\", \"effectEstimate\")",
          "description": "Character. The scale of the input futility bounds. Must be one of \"zValue\", \"pValue\", \"conditionalPower\", \"condPowerAtObserved\", \"predictivePower\", \"reverseCondPower\", or \"effectEstimate\"."
        },
        {
          "name": "targetScale",
          "required": false,
          "default": "c(\"zValue\", \"pValue\", \"conditionalPower\", \"condPowerAtObserved\",      \"predictivePower\", \"reverseCondPower\", \"effectEstimate\")",
          "description": "Character. The scale to which the futility bounds should be converted. Must be one of \"zValue\", \"pValue\", \"conditionalPower\", \"condPowerAtObserved\", \"predictivePower\", \"reverseCondPower\", or \"effectEstimate\"."
        },
        {
          "name": "design",
          "required": false,
          "default": "NULL",
          "description": "The trial design. Required if either the sourceScale or targetScale is \"reverseCondPower\" or if the conversion involves conditional or predictive power in a group sequential or Fisher design. Must be a one-sided two-stage group sequential design or Fisher's combination test design."
        },
        {
          "name": "theta",
          "required": false,
          "default": "NA_real_",
          "description": "Numeric. The assumed effect size under the alternative hypothesis."
        },
        {
          "name": "information",
          "required": false,
          "default": "NA_real_",
          "description": "Numeric vector of length 2. The information levels at the two stages."
        },
        {
          "name": "naAllowed",
          "required": false,
          "default": "FALSE",
          "description": "Logical. Indicates if NA sourceValue are permitted. Default is FALSE."
        }
      ]
    },
    {
      "name": "getGroupSequentialProbabilities",
      "family": "design",
      "title": "Get Group Sequential Probabilities",
      "description": "Calculates probabilities in the group sequential setting.",
      "args": [
        {
          "name": "decisionMatrix",
          "required": true,
          "default": null,
          "description": "A matrix with either 2 or 4 rows and kMax = length(informationRates) columns, see details."
        },
        {
          "name": "informationRates",
          "required": true,
          "default": null,
          "description": "The information rates t_1, ..., t_kMax (that must be fixed prior to the trial), default is (1:kMax) / kMax. For the weighted inverse normal design, the weights are derived through w_1 = sqrt(t_1), and w_k = sqrt(t_k - t_(k-1)). For the weighted Fisher's combination test, the weights (scales) are w_k = sqrt((t_k - t_(k-1)) / t_1) (see the documentation)."
        }
      ]
    },
    {
      "name": "getHazardRatioByLambda",
      "family": "samplesize_power",
      "title": "Survival Helper Functions for Conversion of Pi, Lambda, Median, and Hazard Ratio Values",
      "description": "Functions to convert pi, lambda, median, and hazard ratio values into each other.",
      "args": [
        {
          "name": "lambda1",
          "required": true,
          "default": null,
          "description": "Value that shall be converted."
        },
        {
          "name": "lambda2",
          "required": true,
          "default": null,
          "description": "Value that shall be converted."
        }
      ]
    },
    {
      "name": "getHazardRatioByMedian",
      "family": "samplesize_power",
      "title": "Survival Helper Functions for Conversion of Pi, Lambda, Median, and Hazard Ratio Values",
      "description": "Functions to convert pi, lambda, median, and hazard ratio values into each other.",
      "args": [
        {
          "name": "median1",
          "required": true,
          "default": null,
          "description": "Value that shall be converted."
        },
        {
          "name": "median2",
          "required": true,
          "default": null,
          "description": "Value that shall be converted."
        },
        {
          "name": "kappa",
          "required": false,
          "default": "1",
          "description": "A numeric value > 0. A kappa != 1 will be used for the specification of the shape of the Weibull distribution. Default is 1, i.e., the exponential survival distribution is used instead of the Weibull distribution. Note that the Weibull distribution cannot be used for the piecewise definition of the survival time distribution, i.e., only piecewiselambda (as a single value) and kappa can be specified. This function is equivalent to pweibull(t, shape = kappa, scale = 1 / lambda) of the stats package, i.e., the scale parameter is 1 / 'hazard rate'. For example, getPiecewiseExponentialDistribution(time = 130, piecewiseLambda = 0.01, kappa = 4.2) and pweibull(q = 130, shape = 4.2, scale = 1 / 0.01) provide the same result."
        }
      ]
    },
    {
      "name": "getHazardRatioByPi",
      "family": "samplesize_power",
      "title": "Survival Helper Functions for Conversion of Pi, Lambda, Median, and Hazard Ratio Values",
      "description": "Functions to convert pi, lambda, median, and hazard ratio values into each other.",
      "args": [
        {
          "name": "pi1",
          "required": true,
          "default": null,
          "description": "Value that shall be converted."
        },
        {
          "name": "pi2",
          "required": true,
          "default": null,
          "description": "Value that shall be converted."
        },
        {
          "name": "eventTime",
          "required": false,
          "default": "12",
          "description": "The assumed time under which the event rates are calculated, default is 12."
        },
        {
          "name": "kappa",
          "required": false,
          "default": "1",
          "description": "A numeric value > 0. A kappa != 1 will be used for the specification of the shape of the Weibull distribution. Default is 1, i.e., the exponential survival distribution is used instead of the Weibull distribution. Note that the Weibull distribution cannot be used for the piecewise definition of the survival time distribution, i.e., only piecewiselambda (as a single value) and kappa can be specified. This function is equivalent to pweibull(t, shape = kappa, scale = 1 / lambda) of the stats package, i.e., the scale parameter is 1 / 'hazard rate'. For example, getPiecewiseExponentialDistribution(time = 130, piecewiseLambda = 0.01, kappa = 4.2) and pweibull(q = 130, shape = 4.2, scale = 1 / 0.01) provide the same result."
        }
      ]
    },
    {
      "name": "getLambda1ByLambda2AndHazardRatio",
      "family": "samplesize_power",
      "title": "Survival Helper Functions for Conversion of Pi, Lambda, Median, and Hazard Ratio Values",
      "description": "Functions to convert pi, lambda, median, and hazard ratio values into each other.",
      "args": [
        {
          "name": "lambda2",
          "required": true,
          "default": null,
          "description": "Value that shall be converted."
        },
        {
          "name": "hazardRatio",
          "required": true,
          "default": null,
          "description": null
        }
      ]
    },
    {
      "name": "getLambda2ByLambda1AndHazardRatio",
      "family": "samplesize_power",
      "title": "Survival Helper Functions for Conversion of Pi, Lambda, Median, and Hazard Ratio Values",
      "description": "Functions to convert pi, lambda, median, and hazard ratio values into each other.",
      "args": [
        {
          "name": "lambda1",
          "required": true,
          "default": null,
          "description": "Value that shall be converted."
        },
        {
          "name": "hazardRatio",
          "required": true,
          "default": null,
          "description": null
        }
      ]
    },
    {
      "name": "getLambdaByMedian",
      "family": "samplesize_power",
      "title": "Survival Helper Functions for Conversion of Pi, Lambda, Median, and Hazard Ratio Values",
      "description": "Functions to convert pi, lambda, median, and hazard ratio values into each other.",
      "args": [
        {
          "name": "median",
          "required": true,
          "default": null,
          "description": "Value that shall be converted."
        },
        {
          "name": "kappa",
          "required": false,
          "default": "1",
          "description": "A numeric value > 0. A kappa != 1 will be used for the specification of the shape of the Weibull distribution. Default is 1, i.e., the exponential survival distribution is used instead of the Weibull distribution. Note that the Weibull distribution cannot be used for the piecewise definition of the survival time distribution, i.e., only piecewiselambda (as a single value) and kappa can be specified. This function is equivalent to pweibull(t, shape = kappa, scale = 1 / lambda) of the stats package, i.e., the scale parameter is 1 / 'hazard rate'. For example, getPiecewiseExponentialDistribution(time = 130, piecewiseLambda = 0.01, kappa = 4.2) and pweibull(q = 130, shape = 4.2, scale = 1 / 0.01) provide the same result."
        }
      ]
    },
    {
      "name": "getLambdaByPi",
      "family": "samplesize_power",
      "title": "Survival Helper Functions for Conversion of Pi, Lambda, Median, and Hazard Ratio Values",
      "description": "Functions to convert pi, lambda, median, and hazard ratio values into each other.",
      "args": [
        {
          "name": "piValue",
          "required": true,
          "default": null,
          "description": "Value that shall be converted."
        },
        {
          "name": "eventTime",
          "required": false,
          "default": "12",
          "description": "The assumed time under which the event rates are calculated, default is 12."
        },
        {
          "name": "kappa",
          "required": false,
          "default": "1",
          "description": "A numeric value > 0. A kappa != 1 will be used for the specification of the shape of the Weibull distribution. Default is 1, i.e., the exponential survival distribution is used instead of the Weibull distribution. Note that the Weibull distribution cannot be used for the piecewise definition of the survival time distribution, i.e., only piecewiselambda (as a single value) and kappa can be specified. This function is equivalent to pweibull(t, shape = kappa, scale = 1 / lambda) of the stats package, i.e., the scale parameter is 1 / 'hazard rate'. For example, getPiecewiseExponentialDistribution(time = 130, piecewiseLambda = 0.01, kappa = 4.2) and pweibull(q = 130, shape = 4.2, scale = 1 / 0.01) provide the same result."
        }
      ]
    },
    {
      "name": "getLambdaStepFunction",
      "family": "utility",
      "title": "Get Lambda Step Function",
      "description": "Calculates the lambda step values for a given time vector.",
      "args": [
        {
          "name": "timeValues",
          "required": true,
          "default": null,
          "description": "A numeric vector that specifies the time values for which the lambda step values shall be calculated."
        },
        {
          "name": "...",
          "required": true,
          "default": null,
          "description": "Ensures that all arguments (starting from the \"...\") are to be named and that a warning will be displayed if unknown arguments are passed."
        },
        {
          "name": "piecewiseSurvivalTime",
          "required": true,
          "default": null,
          "description": "A numeric vector that specifies the time intervals for the piecewise definition of the exponential survival time cumulative distribution function (see details)."
        },
        {
          "name": "piecewiseLambda",
          "required": true,
          "default": null,
          "description": "A numeric vector that specifies the assumed hazard rate in the treatment group."
        }
      ]
    },
    {
      "name": "getLogLevel",
      "family": "utility",
      "title": "Get Log Level",
      "description": "Returns the current rpact log level.",
      "args": []
    },
    {
      "name": "getLongFormat",
      "family": "analysis",
      "title": "Get Long Format",
      "description": "Returns the specified dataset as a data.frame in so-called long format.",
      "args": [
        {
          "name": "dataInput",
          "required": true,
          "default": null,
          "description": null
        }
      ]
    },
    {
      "name": "getMedianByLambda",
      "family": "samplesize_power",
      "title": "Survival Helper Functions for Conversion of Pi, Lambda, Median, and Hazard Ratio Values",
      "description": "Functions to convert pi, lambda, median, and hazard ratio values into each other.",
      "args": [
        {
          "name": "lambda",
          "required": true,
          "default": null,
          "description": "Value that shall be converted."
        },
        {
          "name": "kappa",
          "required": false,
          "default": "1",
          "description": "A numeric value > 0. A kappa != 1 will be used for the specification of the shape of the Weibull distribution. Default is 1, i.e., the exponential survival distribution is used instead of the Weibull distribution. Note that the Weibull distribution cannot be used for the piecewise definition of the survival time distribution, i.e., only piecewiselambda (as a single value) and kappa can be specified. This function is equivalent to pweibull(t, shape = kappa, scale = 1 / lambda) of the stats package, i.e., the scale parameter is 1 / 'hazard rate'. For example, getPiecewiseExponentialDistribution(time = 130, piecewiseLambda = 0.01, kappa = 4.2) and pweibull(q = 130, shape = 4.2, scale = 1 / 0.01) provide the same result."
        }
      ]
    },
    {
      "name": "getMedianByPi",
      "family": "samplesize_power",
      "title": "Survival Helper Functions for Conversion of Pi, Lambda, Median, and Hazard Ratio Values",
      "description": "Functions to convert pi, lambda, median, and hazard ratio values into each other.",
      "args": [
        {
          "name": "piValue",
          "required": true,
          "default": null,
          "description": "Value that shall be converted."
        },
        {
          "name": "eventTime",
          "required": false,
          "default": "12",
          "description": "The assumed time under which the event rates are calculated, default is 12."
        },
        {
          "name": "kappa",
          "required": false,
          "default": "1",
          "description": "A numeric value > 0. A kappa != 1 will be used for the specification of the shape of the Weibull distribution. Default is 1, i.e., the exponential survival distribution is used instead of the Weibull distribution. Note that the Weibull distribution cannot be used for the piecewise definition of the survival time distribution, i.e., only piecewiselambda (as a single value) and kappa can be specified. This function is equivalent to pweibull(t, shape = kappa, scale = 1 / lambda) of the stats package, i.e., the scale parameter is 1 / 'hazard rate'. For example, getPiecewiseExponentialDistribution(time = 130, piecewiseLambda = 0.01, kappa = 4.2) and pweibull(q = 130, shape = 4.2, scale = 1 / 0.01) provide the same result."
        }
      ]
    },
    {
      "name": "getNumberOfSubjects",
      "family": "samplesize_power",
      "title": "Get Number Of Subjects",
      "description": "Returns the number of recruited subjects at given time vector.",
      "args": [
        {
          "name": "time",
          "required": true,
          "default": null,
          "description": "A numeric vector with time values."
        },
        {
          "name": "...",
          "required": true,
          "default": null,
          "description": "Ensures that all arguments (starting from the \"...\") are to be named and that a warning will be displayed if unknown arguments are passed."
        },
        {
          "name": "accrualTime",
          "required": false,
          "default": "c(0, 12)",
          "description": "The assumed accrual time intervals for the study, default is c(0, 12) (for details see getAccrualTime())."
        },
        {
          "name": "accrualIntensity",
          "required": false,
          "default": "0.1",
          "description": "A numeric vector of accrual intensities, default is the relative intensity 0.1 (for details see getAccrualTime())."
        },
        {
          "name": "accrualIntensityType",
          "required": false,
          "default": "c(\"auto\", \"absolute\", \"relative\")",
          "description": "A character value specifying the accrual intensity input type. Must be one of \"auto\", \"absolute\", or \"relative\"; default is \"auto\", i.e., if all values are < 1 the type is \"relative\", otherwise it is \"absolute\"."
        },
        {
          "name": "maxNumberOfSubjects",
          "required": false,
          "default": "NA_real_",
          "description": "If maxNumberOfSubjects > 0 is specified, the end of accrual at specified accrualIntensity for the specified number of subjects is determined or accrualIntensity is calculated at fixed end of accrual."
        }
      ]
    },
    {
      "name": "getObjectRCode",
      "family": "utility",
      "title": "Get Object R Code",
      "description": "Returns the R source command of a result object.",
      "args": [
        {
          "name": "obj",
          "required": true,
          "default": null,
          "description": "The result object."
        },
        {
          "name": "...",
          "required": true,
          "default": null,
          "description": "Ensures that all arguments (starting from the \"...\") are to be named and that a warning will be displayed if unknown arguments are passed."
        },
        {
          "name": "leadingArguments",
          "required": false,
          "default": "NULL",
          "description": "A character vector with arguments that shall be inserted at the beginning of the function command, e.g., design = x. Be careful with this option because the created R command may no longer be valid if used."
        },
        {
          "name": "includeDefaultParameters",
          "required": false,
          "default": "FALSE",
          "description": "If TRUE, default parameters will be included in all rpact commands; default is FALSE."
        },
        {
          "name": "stringWrapParagraphWidth",
          "required": false,
          "default": "90",
          "description": "An integer value defining the number of characters after which a line break shall be inserted; set to NULL to insert no line breaks."
        },
        {
          "name": "prefix",
          "required": false,
          "default": "\"\"",
          "description": "A character string that shall be added to the beginning of the R command."
        },
        {
          "name": "postfix",
          "required": false,
          "default": "\"\"",
          "description": "A character string that shall be added to the end of the R command."
        },
        {
          "name": "stringWrapPrefix",
          "required": false,
          "default": "\"\"",
          "description": "A prefix character string that shall be added to each new line, typically some spaces."
        },
        {
          "name": "newArgumentValues",
          "required": false,
          "default": "list()",
          "description": "A named list with arguments that shall be renewed in the R command, e.g., newArgumentValues = list(informationRates = c(0.5, 1))."
        },
        {
          "name": "tolerance",
          "required": false,
          "default": "1e-07",
          "description": "The tolerance for defining a value as default."
        },
        {
          "name": "pipeOperator",
          "required": false,
          "default": "c(\"auto\", \"none\", \"magrittr\", \"R\")",
          "description": "The pipe operator to use in the R code, default is \"none\"."
        },
        {
          "name": "output",
          "required": false,
          "default": "c(\"vector\", \"cat\", \"test\", \"markdown\", \"internal\")",
          "description": "The output format, default is a character \"vector\"."
        },
        {
          "name": "explicitPrint",
          "required": false,
          "default": "FALSE",
          "description": "Show an explicit print command, default is FALSE."
        }
      ]
    },
    {
      "name": "getObservedInformationRates",
      "family": "analysis",
      "title": "Get Observed Information Rates",
      "description": "Recalculates the observed information rates from the specified dataset.",
      "args": [
        {
          "name": "dataInput",
          "required": true,
          "default": null,
          "description": "The dataset for which the information rates shall be recalculated."
        },
        {
          "name": "...",
          "required": true,
          "default": null,
          "description": "Ensures that all arguments (starting from the \"...\") are to be named and that a warning will be displayed if unknown arguments are passed."
        },
        {
          "name": "maxInformation",
          "required": false,
          "default": "NULL",
          "description": "Positive value specifying the maximum information."
        },
        {
          "name": "informationEpsilon",
          "required": false,
          "default": "NULL",
          "description": "Positive integer value specifying the absolute information epsilon, which defines the maximum distance from the observed information to the maximum information that causes the final analysis. Updates at the final analysis in case the observed information at the final analysis is smaller (\"under-running\") than the planned maximum information maxInformation, default is 0. Alternatively, a floating-point number > 0 and < 1 can be specified to define a relative information epsilon."
        },
        {
          "name": "stage",
          "required": false,
          "default": "NA_integer_",
          "description": "The stage number (optional). Default: total number of existing stages in the data input."
        }
      ]
    },
    {
      "name": "getOutputFormat",
      "family": "utility",
      "title": "Get Output Format",
      "description": "With this function the format of the standard outputs of all rpact objects can be shown and written to a file.",
      "args": [
        {
          "name": "parameterName",
          "required": false,
          "default": "NA_character_",
          "description": "The name of the parameter whose output format shall be returned. Leave the default NA_character_ if the output format of all parameters shall be returned."
        },
        {
          "name": "...",
          "required": true,
          "default": null,
          "description": "Ensures that all arguments (starting from the \"...\") are to be named and that a warning will be displayed if unknown arguments are passed."
        },
        {
          "name": "file",
          "required": false,
          "default": "NA_character_",
          "description": "An optional file name where to write the output formats (see Details for more information)."
        },
        {
          "name": "default",
          "required": false,
          "default": "FALSE",
          "description": "If TRUE the default output format of the specified parameter(s) will be returned, default is FALSE."
        },
        {
          "name": "fields",
          "required": false,
          "default": "TRUE",
          "description": "If TRUE the names of all affected object fields will be displayed, default is TRUE."
        }
      ]
    },
    {
      "name": "getParameterCaption",
      "family": "utility",
      "title": "Get Parameter Caption",
      "description": "Returns the parameter caption for a given object and parameter name.",
      "args": [
        {
          "name": "obj",
          "required": true,
          "default": null,
          "description": "The rpact result object."
        },
        {
          "name": "var",
          "required": true,
          "default": null,
          "description": "The variable/parameter name."
        }
      ]
    },
    {
      "name": "getParameterName",
      "family": "utility",
      "title": "Get Parameter Name",
      "description": "Returns the parameter name for a given object and parameter caption.",
      "args": [
        {
          "name": "obj",
          "required": true,
          "default": null,
          "description": "The rpact result object."
        },
        {
          "name": "parameterCaption",
          "required": true,
          "default": null,
          "description": "The parameter caption."
        }
      ]
    },
    {
      "name": "getParameterType",
      "family": "utility",
      "title": "Get Parameter Type",
      "description": "Returns the parameter type for a given object and parameter name.",
      "args": [
        {
          "name": "obj",
          "required": true,
          "default": null,
          "description": "The rpact result object."
        },
        {
          "name": "var",
          "required": true,
          "default": null,
          "description": "The variable/parameter name."
        }
      ]
    },
    {
      "name": "getPerformanceScore",
      "family": "simulation",
      "title": "Get Performance Score",
      "description": "Calculates the conditional performance score, its sub-scores and components according to (Herrmann et al. (2020), \\Sexpr[results=rd]{tools:::Rd_expr_doi(\"#1\")}10.1002/sim.8534textdoi:10.1002/sim.8534 <https://doi.org/10.1002/sim.8534>latexhttps://doi.org/10.1002/sim.8534doi:10.1002\\slash{}sim.8534https://doi.org/10.1002/sim.8534doi:10.1002/sim.8534) and (Bokelmann et al. (2024), \\Sexpr[results=rd]{tools:::Rd_expr_doi(\"#1\")}10.1186/s12874-024-02150-4textdoi:10.1186/s12874-024-02150-4 <https://doi.org/10.1186/s12874-024-02150-4>latexhttps://doi.org/10.1186/s12874-024-02150-4doi:10.1186\\slash{}s12874\\-024\\-02150\\-4https://doi.org/10.1186/s12874-024-02150-4doi:10.1186/s12874-024-02150-4) for a given simulation result from a two-stage design with continuous or binary endpoint. Larger (sub-)score and component values refer to a better performance.",
      "args": [
        {
          "name": "simulationResult",
          "required": true,
          "default": null,
          "description": "A simulation result."
        }
      ]
    },
    {
      "name": "getPi1ByPi2AndHazardRatio",
      "family": "samplesize_power",
      "title": "Survival Helper Functions for Conversion of Pi, Lambda, Median, and Hazard Ratio Values",
      "description": "Functions to convert pi, lambda, median, and hazard ratio values into each other.",
      "args": [
        {
          "name": "pi2",
          "required": true,
          "default": null,
          "description": "Value that shall be converted."
        },
        {
          "name": "hazardRatio",
          "required": true,
          "default": null,
          "description": null
        },
        {
          "name": "eventTime",
          "required": false,
          "default": "12",
          "description": "The assumed time under which the event rates are calculated, default is 12."
        },
        {
          "name": "kappa",
          "required": false,
          "default": "1",
          "description": "A numeric value > 0. A kappa != 1 will be used for the specification of the shape of the Weibull distribution. Default is 1, i.e., the exponential survival distribution is used instead of the Weibull distribution. Note that the Weibull distribution cannot be used for the piecewise definition of the survival time distribution, i.e., only piecewiselambda (as a single value) and kappa can be specified. This function is equivalent to pweibull(t, shape = kappa, scale = 1 / lambda) of the stats package, i.e., the scale parameter is 1 / 'hazard rate'. For example, getPiecewiseExponentialDistribution(time = 130, piecewiseLambda = 0.01, kappa = 4.2) and pweibull(q = 130, shape = 4.2, scale = 1 / 0.01) provide the same result."
        }
      ]
    },
    {
      "name": "getPi2ByPi1AndHazardRatio",
      "family": "samplesize_power",
      "title": "Survival Helper Functions for Conversion of Pi, Lambda, Median, and Hazard Ratio Values",
      "description": "Functions to convert pi, lambda, median, and hazard ratio values into each other.",
      "args": [
        {
          "name": "pi1",
          "required": true,
          "default": null,
          "description": "Value that shall be converted."
        },
        {
          "name": "hazardRatio",
          "required": true,
          "default": null,
          "description": null
        },
        {
          "name": "eventTime",
          "required": false,
          "default": "12",
          "description": "The assumed time under which the event rates are calculated, default is 12."
        },
        {
          "name": "kappa",
          "required": false,
          "default": "1",
          "description": "A numeric value > 0. A kappa != 1 will be used for the specification of the shape of the Weibull distribution. Default is 1, i.e., the exponential survival distribution is used instead of the Weibull distribution. Note that the Weibull distribution cannot be used for the piecewise definition of the survival time distribution, i.e., only piecewiselambda (as a single value) and kappa can be specified. This function is equivalent to pweibull(t, shape = kappa, scale = 1 / lambda) of the stats package, i.e., the scale parameter is 1 / 'hazard rate'. For example, getPiecewiseExponentialDistribution(time = 130, piecewiseLambda = 0.01, kappa = 4.2) and pweibull(q = 130, shape = 4.2, scale = 1 / 0.01) provide the same result."
        }
      ]
    },
    {
      "name": "getPiByLambda",
      "family": "samplesize_power",
      "title": "Survival Helper Functions for Conversion of Pi, Lambda, Median, and Hazard Ratio Values",
      "description": "Functions to convert pi, lambda, median, and hazard ratio values into each other.",
      "args": [
        {
          "name": "lambda",
          "required": true,
          "default": null,
          "description": "Value that shall be converted."
        },
        {
          "name": "eventTime",
          "required": false,
          "default": "12",
          "description": "The assumed time under which the event rates are calculated, default is 12."
        },
        {
          "name": "kappa",
          "required": false,
          "default": "1",
          "description": "A numeric value > 0. A kappa != 1 will be used for the specification of the shape of the Weibull distribution. Default is 1, i.e., the exponential survival distribution is used instead of the Weibull distribution. Note that the Weibull distribution cannot be used for the piecewise definition of the survival time distribution, i.e., only piecewiselambda (as a single value) and kappa can be specified. This function is equivalent to pweibull(t, shape = kappa, scale = 1 / lambda) of the stats package, i.e., the scale parameter is 1 / 'hazard rate'. For example, getPiecewiseExponentialDistribution(time = 130, piecewiseLambda = 0.01, kappa = 4.2) and pweibull(q = 130, shape = 4.2, scale = 1 / 0.01) provide the same result."
        }
      ]
    },
    {
      "name": "getPiByMedian",
      "family": "samplesize_power",
      "title": "Survival Helper Functions for Conversion of Pi, Lambda, Median, and Hazard Ratio Values",
      "description": "Functions to convert pi, lambda, median, and hazard ratio values into each other.",
      "args": [
        {
          "name": "median",
          "required": true,
          "default": null,
          "description": "Value that shall be converted."
        },
        {
          "name": "eventTime",
          "required": false,
          "default": "12",
          "description": "The assumed time under which the event rates are calculated, default is 12."
        },
        {
          "name": "kappa",
          "required": false,
          "default": "1",
          "description": "A numeric value > 0. A kappa != 1 will be used for the specification of the shape of the Weibull distribution. Default is 1, i.e., the exponential survival distribution is used instead of the Weibull distribution. Note that the Weibull distribution cannot be used for the piecewise definition of the survival time distribution, i.e., only piecewiselambda (as a single value) and kappa can be specified. This function is equivalent to pweibull(t, shape = kappa, scale = 1 / lambda) of the stats package, i.e., the scale parameter is 1 / 'hazard rate'. For example, getPiecewiseExponentialDistribution(time = 130, piecewiseLambda = 0.01, kappa = 4.2) and pweibull(q = 130, shape = 4.2, scale = 1 / 0.01) provide the same result."
        }
      ]
    },
    {
      "name": "getPiecewiseExponentialDistribution",
      "family": "samplesize_power",
      "title": "The Piecewise Exponential Distribution",
      "description": "Distribution function, quantile function and random number generation for the piecewise exponential distribution.",
      "args": [
        {
          "name": "time",
          "required": true,
          "default": null,
          "description": "Vector of time values."
        },
        {
          "name": "...",
          "required": true,
          "default": null,
          "description": "Ensures that all arguments (starting from the \"...\") are to be named and that a warning will be displayed if unknown arguments are passed."
        },
        {
          "name": "piecewiseSurvivalTime",
          "required": false,
          "default": "NA_real_",
          "description": "Vector of start times defining the \"time pieces\"."
        },
        {
          "name": "piecewiseLambda",
          "required": false,
          "default": "NA_real_",
          "description": "Vector of lambda values (hazard rates) corresponding to the start times."
        },
        {
          "name": "kappa",
          "required": false,
          "default": "1",
          "description": "A numeric value > 0. A kappa != 1 will be used for the specification of the shape of the Weibull distribution. Default is 1, i.e., the exponential survival distribution is used instead of the Weibull distribution. Note that the Weibull distribution cannot be used for the piecewise definition of the survival time distribution, i.e., only piecewiselambda (as a single value) and kappa can be specified. This function is equivalent to pweibull(t, shape = kappa, scale = 1 / lambda) of the stats package, i.e., the scale parameter is 1 / 'hazard rate'. For example, getPiecewiseExponentialDistribution(time = 130, piecewiseLambda = 0.01, kappa = 4.2) and pweibull(q = 130, shape = 4.2, scale = 1 / 0.01) provide the same result."
        }
      ]
    },
    {
      "name": "getPiecewiseExponentialQuantile",
      "family": "samplesize_power",
      "title": "The Piecewise Exponential Distribution",
      "description": "Distribution function, quantile function and random number generation for the piecewise exponential distribution.",
      "args": [
        {
          "name": "quantile",
          "required": true,
          "default": null,
          "description": "Vector of quantiles."
        },
        {
          "name": "...",
          "required": true,
          "default": null,
          "description": "Ensures that all arguments (starting from the \"...\") are to be named and that a warning will be displayed if unknown arguments are passed."
        },
        {
          "name": "piecewiseSurvivalTime",
          "required": false,
          "default": "NA_real_",
          "description": "Vector of start times defining the \"time pieces\"."
        },
        {
          "name": "piecewiseLambda",
          "required": false,
          "default": "NA_real_",
          "description": "Vector of lambda values (hazard rates) corresponding to the start times."
        },
        {
          "name": "kappa",
          "required": false,
          "default": "1",
          "description": "A numeric value > 0. A kappa != 1 will be used for the specification of the shape of the Weibull distribution. Default is 1, i.e., the exponential survival distribution is used instead of the Weibull distribution. Note that the Weibull distribution cannot be used for the piecewise definition of the survival time distribution, i.e., only piecewiselambda (as a single value) and kappa can be specified. This function is equivalent to pweibull(t, shape = kappa, scale = 1 / lambda) of the stats package, i.e., the scale parameter is 1 / 'hazard rate'. For example, getPiecewiseExponentialDistribution(time = 130, piecewiseLambda = 0.01, kappa = 4.2) and pweibull(q = 130, shape = 4.2, scale = 1 / 0.01) provide the same result."
        }
      ]
    },
    {
      "name": "getPiecewiseExponentialRandomNumbers",
      "family": "samplesize_power",
      "title": "The Piecewise Exponential Distribution",
      "description": "Distribution function, quantile function and random number generation for the piecewise exponential distribution.",
      "args": [
        {
          "name": "n",
          "required": true,
          "default": null,
          "description": "Number of observations."
        },
        {
          "name": "...",
          "required": true,
          "default": null,
          "description": "Ensures that all arguments (starting from the \"...\") are to be named and that a warning will be displayed if unknown arguments are passed."
        },
        {
          "name": "piecewiseSurvivalTime",
          "required": false,
          "default": "NA_real_",
          "description": "Vector of start times defining the \"time pieces\"."
        },
        {
          "name": "piecewiseLambda",
          "required": false,
          "default": "NA_real_",
          "description": "Vector of lambda values (hazard rates) corresponding to the start times."
        },
        {
          "name": "kappa",
          "required": false,
          "default": "1",
          "description": "A numeric value > 0. A kappa != 1 will be used for the specification of the shape of the Weibull distribution. Default is 1, i.e., the exponential survival distribution is used instead of the Weibull distribution. Note that the Weibull distribution cannot be used for the piecewise definition of the survival time distribution, i.e., only piecewiselambda (as a single value) and kappa can be specified. This function is equivalent to pweibull(t, shape = kappa, scale = 1 / lambda) of the stats package, i.e., the scale parameter is 1 / 'hazard rate'. For example, getPiecewiseExponentialDistribution(time = 130, piecewiseLambda = 0.01, kappa = 4.2) and pweibull(q = 130, shape = 4.2, scale = 1 / 0.01) provide the same result."
        }
      ]
    },
    {
      "name": "getPiecewiseSurvivalTime",
      "family": "samplesize_power",
      "title": "Get Piecewise Survival Time",
      "description": "Returns a PiecewiseSurvivalTime object that contains the all relevant parameters of an exponential survival time cumulative distribution function. Use names to obtain the field names.",
      "args": [
        {
          "name": "piecewiseSurvivalTime",
          "required": false,
          "default": "NA_real_",
          "description": "A vector that specifies the time intervals for the piecewise definition of the exponential survival time cumulative distribution function (see details)."
        },
        {
          "name": "...",
          "required": true,
          "default": null,
          "description": "Ensures that all arguments (starting from the \"...\") are to be named and that a warning will be displayed if unknown arguments are passed."
        },
        {
          "name": "lambda1",
          "required": false,
          "default": "NA_real_",
          "description": "The assumed hazard rate in the treatment group, there is no default. lambda1 can also be used to define piecewise exponentially distributed survival times (see details). Must be a positive numeric of length 1."
        },
        {
          "name": "lambda2",
          "required": false,
          "default": "NA_real_",
          "description": "The assumed hazard rate in the reference group, there is no default. lambda2 can also be used to define piecewise exponentially distributed survival times (see details). Must be a positive numeric of length 1."
        },
        {
          "name": "hazardRatio",
          "required": false,
          "default": "NA_real_",
          "description": "The vector of hazard ratios under consideration. If the event or hazard rates in both treatment groups are defined, the hazard ratio needs not to be specified as it is calculated, there is no default. Must be a positive numeric of length 1."
        },
        {
          "name": "pi1",
          "required": false,
          "default": "NA_real_",
          "description": "A numeric value or vector that represents the assumed event rate in the treatment group, default is seq(0.2, 0.5, 0.1) (power calculations and simulations) or seq(0.4, 0.6, 0.1) (sample size calculations)."
        },
        {
          "name": "pi2",
          "required": false,
          "default": "NA_real_",
          "description": "A numeric value that represents the assumed event rate in the control group, default is 0.2."
        },
        {
          "name": "median1",
          "required": false,
          "default": "NA_real_",
          "description": "The assumed median survival time in the treatment group, there is no default."
        },
        {
          "name": "median2",
          "required": false,
          "default": "NA_real_",
          "description": "The assumed median survival time in the reference group, there is no default. Must be a positive numeric of length 1."
        },
        {
          "name": "eventTime",
          "required": false,
          "default": "12",
          "description": "The assumed time under which the event rates are calculated, default is 12."
        },
        {
          "name": "kappa",
          "required": false,
          "default": "1",
          "description": "A numeric value > 0. A kappa != 1 will be used for the specification of the shape of the Weibull distribution. Default is 1, i.e., the exponential survival distribution is used instead of the Weibull distribution. Note that the Weibull distribution cannot be used for the piecewise definition of the survival time distribution, i.e., only piecewiselambda (as a single value) and kappa can be specified. This function is equivalent to pweibull(t, shape = kappa, scale = 1 / lambda) of the stats package, i.e., the scale parameter is 1 / 'hazard rate'. For example, getPiecewiseExponentialDistribution(time = 130, piecewiseLambda = 0.01, kappa = 4.2) and pweibull(q = 130, shape = 4.2, scale = 1 / 0.01) provide the same result."
        },
        {
          "name": "delayedResponseAllowed",
          "required": false,
          "default": "FALSE",
          "description": "If TRUE, delayed response is allowed; otherwise it will be validated that the response is not delayed, default is FALSE."
        }
      ]
    },
    {
      "name": "getPlotSettings",
      "family": "utility",
      "title": "Get Plot Settings",
      "description": "Returns a plot settings object.",
      "args": [
        {
          "name": "lineSize",
          "required": false,
          "default": "0.8",
          "description": "The line size, default is 0.8."
        },
        {
          "name": "pointSize",
          "required": false,
          "default": "3",
          "description": "The point size, default is 3."
        },
        {
          "name": "pointColor",
          "required": false,
          "default": "NA_character_",
          "description": "The point color (character), default is NA_character_."
        },
        {
          "name": "mainTitleFontSize",
          "required": false,
          "default": "14",
          "description": "The main title font size, default is 14."
        },
        {
          "name": "axesTextFontSize",
          "required": false,
          "default": "10",
          "description": "The axes text font size, default is 10."
        },
        {
          "name": "legendFontSize",
          "required": false,
          "default": "11",
          "description": "The legend font size, default is 11."
        },
        {
          "name": "scalingFactor",
          "required": false,
          "default": "1",
          "description": "The scaling factor, default is 1."
        }
      ]
    },
    {
      "name": "getPowerAndAverageSampleNumber",
      "family": "design",
      "title": "Get Power And Average Sample Number",
      "description": "Returns the power and average sample number of the specified design.",
      "args": [
        {
          "name": "design",
          "required": true,
          "default": null,
          "description": "The trial design."
        },
        {
          "name": "theta",
          "required": false,
          "default": "seq(-1, 1, 0.02)",
          "description": "A vector of standardized effect sizes (theta values), default is a sequence from -1 to 1."
        },
        {
          "name": "nMax",
          "required": false,
          "default": "100",
          "description": "The maximum sample size. Must be a positive integer of length 1."
        }
      ]
    },
    {
      "name": "getPowerCounts",
      "family": "samplesize_power",
      "title": "Get Power Counts",
      "description": "Returns the power, stopping probabilities, and expected sample size for testing mean rates for negative binomial distributed event numbers in two samples at given sample sizes.",
      "args": [
        {
          "name": "design",
          "required": false,
          "default": "NULL",
          "description": "The trial design. If no trial design is specified, a fixed sample size design is used. In this case, Type I error rate alpha, Type II error rate beta, twoSidedPower, and sided can be directly entered as argument where necessary."
        },
        {
          "name": "...",
          "required": true,
          "default": null,
          "description": "Ensures that all arguments (starting from the \"...\") are to be named and that a warning will be displayed if unknown arguments are passed."
        },
        {
          "name": "directionUpper",
          "required": false,
          "default": "NA",
          "description": "Logical. Specifies the direction of the alternative, only applicable for one-sided testing; default is TRUE which means that larger values of the test statistics yield smaller p-values."
        },
        {
          "name": "maxNumberOfSubjects",
          "required": false,
          "default": "NA_real_",
          "description": "maxNumberOfSubjects > 0 needs to be specified for power calculations or calculation of necessary follow-up (count data). For two treatment arms, it is the maximum number of subjects for both treatment arms."
        },
        {
          "name": "lambda1",
          "required": false,
          "default": "NA_real_",
          "description": "A numeric value or vector that represents the assumed rate of a homogeneous Poisson process in the active treatment group, there is no default."
        },
        {
          "name": "lambda2",
          "required": false,
          "default": "NA_real_",
          "description": "A numeric value that represents the assumed rate of a homogeneous Poisson process in the control group, there is no default."
        },
        {
          "name": "lambda",
          "required": false,
          "default": "NA_real_",
          "description": "A numeric value or vector that represents the assumed rate of a homogeneous Poisson process in the pooled treatment groups, there is no default."
        },
        {
          "name": "theta",
          "required": false,
          "default": "NA_real_",
          "description": "A numeric value or vector that represents the assumed mean ratios lambda1/lambda2 of a homogeneous Poisson process, there is no default."
        },
        {
          "name": "thetaH0",
          "required": false,
          "default": "1",
          "description": "The null hypothesis value, default is 0 for the normal and the binary case (testing means and rates, respectively), it is 1 for the survival case (testing the hazard ratio). For non-inferiority designs, thetaH0 is the non-inferiority bound. That is, in case of (one-sided) testing of means: a value != 0 (or a value != 1 for testing the mean ratio) can be specified. rates: a value != 0 (or a value != 1 for testing the risk ratio pi1 / pi2) can be specified. survival data: a bound for testing H0: hazard ratio = thetaH0 != 1 can be specified. count data: a bound for testing H0: lambda1 / lambda2 = thetaH0 != 1 can be specified. For testing a rate in one sample, a value thetaH0 in (0, 1) has to be specified for defining the null hypothesis H0: pi = thetaH0."
        },
        {
          "name": "overdispersion",
          "required": false,
          "default": "0",
          "description": "A numeric value that represents the assumed overdispersion of the negative binomial distribution, default is 0."
        },
        {
          "name": "fixedExposureTime",
          "required": false,
          "default": "NA_real_",
          "description": "If specified, the fixed time of exposure per subject for count data, there is no default."
        },
        {
          "name": "accrualTime",
          "required": false,
          "default": "NA_real_",
          "description": "If specified, the assumed accrual time interval(s) for the study, there is no default."
        },
        {
          "name": "accrualIntensity",
          "required": false,
          "default": "NA_real_",
          "description": "If specified, the assumed accrual intensities for the study, there is no default."
        },
        {
          "name": "followUpTime",
          "required": false,
          "default": "NA_real_",
          "description": "If specified, the assumed (additional) follow-up time for the study, there is no default. The total study duration is accrualTime + followUpTime."
        },
        {
          "name": "allocationRatioPlanned",
          "required": false,
          "default": "NA_real_",
          "description": "The planned allocation ratio n1 / n2 for a two treatment groups design, default is 1. For multi-arm designs, it is the allocation ratio relating the active arm(s) to the control. For simulating means and rates for a two treatment groups design, it can be a vector of length kMax, the number of stages. It can be a vector of length kMax, too, for multi-arm and enrichment designs. In these cases, a change of allocating subjects to treatment groups over the stages can be assessed. Note that internally allocationRatioPlanned is treated as a vector of length kMax, not a scalar."
        }
      ]
    },
    {
      "name": "getPowerMeans",
      "family": "samplesize_power",
      "title": "Get Power Means",
      "description": "Returns the power, stopping probabilities, and expected sample size for testing means in one or two samples at given maximum sample size.",
      "args": [
        {
          "name": "design",
          "required": false,
          "default": "NULL",
          "description": "The trial design. If no trial design is specified, a fixed sample size design is used. In this case, Type I error rate alpha, Type II error rate beta, twoSidedPower, and sided can be directly entered as argument where necessary."
        },
        {
          "name": "...",
          "required": true,
          "default": null,
          "description": "Ensures that all arguments (starting from the \"...\") are to be named and that a warning will be displayed if unknown arguments are passed."
        },
        {
          "name": "groups",
          "required": false,
          "default": "2L",
          "description": "The number of treatment groups (1 or 2), default is 2."
        },
        {
          "name": "normalApproximation",
          "required": false,
          "default": "FALSE",
          "description": "The type of computation of the p-values. If TRUE, the variance is assumed to be known, default is FALSE, i.e., the calculations are performed with the t distribution."
        },
        {
          "name": "meanRatio",
          "required": false,
          "default": "FALSE",
          "description": "If TRUE, the sample size for one-sided testing of H0: mu1 / mu2 = thetaH0 is calculated, default is FALSE."
        },
        {
          "name": "thetaH0",
          "required": false,
          "default": "ifelse(meanRatio, 1, 0)",
          "description": "The null hypothesis value, default is 0 for the normal and the binary case (testing means and rates, respectively), it is 1 for the survival case (testing the hazard ratio). For non-inferiority designs, thetaH0 is the non-inferiority bound. That is, in case of (one-sided) testing of means: a value != 0 (or a value != 1 for testing the mean ratio) can be specified. rates: a value != 0 (or a value != 1 for testing the risk ratio pi1 / pi2) can be specified. survival data: a bound for testing H0: hazard ratio = thetaH0 != 1 can be specified. count data: a bound for testing H0: lambda1 / lambda2 = thetaH0 != 1 can be specified. For testing a rate in one sample, a value thetaH0 in (0, 1) has to be specified for defining the null hypothesis H0: pi = thetaH0."
        },
        {
          "name": "alternative",
          "required": false,
          "default": "seq(0, 1, 0.2)",
          "description": "The alternative hypothesis value for testing means. This can be a vector of assumed alternatives, default is seq(0, 1, 0.2) (power calculations) or seq(0.2, 1, 0.2) (sample size calculations)."
        },
        {
          "name": "stDev",
          "required": false,
          "default": "1",
          "description": "The standard deviation under which the sample size or power calculation is performed, default is 1. For two-armed trials, it is allowed to specify the standard deviations separately, i.e., as vector with two elements. If meanRatio = TRUE is specified, stDev defines the coefficient of variation sigma / mu2."
        },
        {
          "name": "directionUpper",
          "required": false,
          "default": "NA",
          "description": "Logical. Specifies the direction of the alternative, only applicable for one-sided testing; default is TRUE which means that larger values of the test statistics yield smaller p-values."
        },
        {
          "name": "maxNumberOfSubjects",
          "required": false,
          "default": "NA_real_",
          "description": "maxNumberOfSubjects > 0 needs to be specified for power calculations or calculation of necessary follow-up (count data). For two treatment arms, it is the maximum number of subjects for both treatment arms."
        },
        {
          "name": "allocationRatioPlanned",
          "required": false,
          "default": "NA_real_",
          "description": "The planned allocation ratio n1 / n2 for a two treatment groups design, default is 1. For multi-arm designs, it is the allocation ratio relating the active arm(s) to the control. For simulating means and rates for a two treatment groups design, it can be a vector of length kMax, the number of stages. It can be a vector of length kMax, too, for multi-arm and enrichment designs. In these cases, a change of allocating subjects to treatment groups over the stages can be assessed. Note that internally allocationRatioPlanned is treated as a vector of length kMax, not a scalar."
        }
      ]
    },
    {
      "name": "getPowerRates",
      "family": "samplesize_power",
      "title": "Get Power Rates",
      "description": "Returns the power, stopping probabilities, and expected sample size for testing rates in one or two samples at given maximum sample size.",
      "args": [
        {
          "name": "design",
          "required": false,
          "default": "NULL",
          "description": "The trial design. If no trial design is specified, a fixed sample size design is used. In this case, Type I error rate alpha, Type II error rate beta, twoSidedPower, and sided can be directly entered as argument where necessary."
        },
        {
          "name": "...",
          "required": true,
          "default": null,
          "description": "Ensures that all arguments (starting from the \"...\") are to be named and that a warning will be displayed if unknown arguments are passed."
        },
        {
          "name": "groups",
          "required": false,
          "default": "2L",
          "description": "The number of treatment groups (1 or 2), default is 2."
        },
        {
          "name": "riskRatio",
          "required": false,
          "default": "FALSE",
          "description": "If TRUE, the power for one-sided testing of H0: pi1 / pi2 = thetaH0 is calculated, default is FALSE."
        },
        {
          "name": "thetaH0",
          "required": false,
          "default": "ifelse(riskRatio, 1, 0)",
          "description": "The null hypothesis value, default is 0 for the normal and the binary case (testing means and rates, respectively), it is 1 for the survival case (testing the hazard ratio). For non-inferiority designs, thetaH0 is the non-inferiority bound. That is, in case of (one-sided) testing of means: a value != 0 (or a value != 1 for testing the mean ratio) can be specified. rates: a value != 0 (or a value != 1 for testing the risk ratio pi1 / pi2) can be specified. survival data: a bound for testing H0: hazard ratio = thetaH0 != 1 can be specified. count data: a bound for testing H0: lambda1 / lambda2 = thetaH0 != 1 can be specified. For testing a rate in one sample, a value thetaH0 in (0, 1) has to be specified for defining the null hypothesis H0: pi = thetaH0."
        },
        {
          "name": "pi1",
          "required": false,
          "default": "seq(0.2, 0.5, 0.1)",
          "description": "A numeric value or vector that represents the assumed probability in the active treatment group if two treatment groups are considered, or the alternative probability for a one treatment group design, default is seq(0.2, 0.5, 0.1) (power calculations and simulations) or seq(0.4, 0.6, 0.1) (sample size calculations)."
        },
        {
          "name": "pi2",
          "required": false,
          "default": "0.2",
          "description": "A numeric value that represents the assumed probability in the reference group if two treatment groups are considered, default is 0.2."
        },
        {
          "name": "directionUpper",
          "required": false,
          "default": "NA",
          "description": "Logical. Specifies the direction of the alternative, only applicable for one-sided testing; default is TRUE which means that larger values of the test statistics yield smaller p-values."
        },
        {
          "name": "maxNumberOfSubjects",
          "required": false,
          "default": "NA_real_",
          "description": "maxNumberOfSubjects > 0 needs to be specified for power calculations or calculation of necessary follow-up (count data). For two treatment arms, it is the maximum number of subjects for both treatment arms."
        },
        {
          "name": "allocationRatioPlanned",
          "required": false,
          "default": "NA_real_",
          "description": "The planned allocation ratio n1 / n2 for a two treatment groups design, default is 1. For multi-arm designs, it is the allocation ratio relating the active arm(s) to the control. For simulating means and rates for a two treatment groups design, it can be a vector of length kMax, the number of stages. It can be a vector of length kMax, too, for multi-arm and enrichment designs. In these cases, a change of allocating subjects to treatment groups over the stages can be assessed. Note that internally allocationRatioPlanned is treated as a vector of length kMax, not a scalar."
        }
      ]
    },
    {
      "name": "getPowerSurvival",
      "family": "samplesize_power",
      "title": "Get Power Survival",
      "description": "Returns the power, stopping probabilities, and expected sample size for testing the hazard ratio in a two treatment groups survival design.",
      "args": [
        {
          "name": "design",
          "required": false,
          "default": "NULL",
          "description": "The trial design. If no trial design is specified, a fixed sample size design is used. In this case, Type I error rate alpha, Type II error rate beta, twoSidedPower, and sided can be directly entered as argument where necessary."
        },
        {
          "name": "...",
          "required": true,
          "default": null,
          "description": "Ensures that all arguments (starting from the \"...\") are to be named and that a warning will be displayed if unknown arguments are passed."
        },
        {
          "name": "typeOfComputation",
          "required": false,
          "default": "c(\"Schoenfeld\", \"Freedman\", \"HsiehFreedman\")",
          "description": "Three options are available: \"Schoenfeld\", \"Freedman\", \"HsiehFreedman\", the default is \"Schoenfeld\". For details, see Hsieh (Statistics in Medicine, 1992). For non-inferiority testing (i.e., thetaH0 != 1), only Schoenfeld's formula can be used."
        },
        {
          "name": "thetaH0",
          "required": false,
          "default": "1",
          "description": "The null hypothesis value, default is 0 for the normal and the binary case (testing means and rates, respectively), it is 1 for the survival case (testing the hazard ratio). For non-inferiority designs, thetaH0 is the non-inferiority bound. That is, in case of (one-sided) testing of means: a value != 0 (or a value != 1 for testing the mean ratio) can be specified. rates: a value != 0 (or a value != 1 for testing the risk ratio pi1 / pi2) can be specified. survival data: a bound for testing H0: hazard ratio = thetaH0 != 1 can be specified. count data: a bound for testing H0: lambda1 / lambda2 = thetaH0 != 1 can be specified. For testing a rate in one sample, a value thetaH0 in (0, 1) has to be specified for defining the null hypothesis H0: pi = thetaH0."
        },
        {
          "name": "directionUpper",
          "required": false,
          "default": "NA",
          "description": "Logical. Specifies the direction of the alternative, only applicable for one-sided testing; default is TRUE which means that larger values of the test statistics yield smaller p-values."
        },
        {
          "name": "pi1",
          "required": false,
          "default": "NA_real_",
          "description": "A numeric value or vector that represents the assumed event rate in the treatment group, default is seq(0.2, 0.5, 0.1) (power calculations and simulations) or seq(0.4, 0.6, 0.1) (sample size calculations)."
        },
        {
          "name": "pi2",
          "required": false,
          "default": "NA_real_",
          "description": "A numeric value that represents the assumed event rate in the control group, default is 0.2."
        },
        {
          "name": "lambda1",
          "required": false,
          "default": "NA_real_",
          "description": "The assumed hazard rate in the treatment group, there is no default. lambda1 can also be used to define piecewise exponentially distributed survival times (see details). Must be a positive numeric of length 1."
        },
        {
          "name": "lambda2",
          "required": false,
          "default": "NA_real_",
          "description": "The assumed hazard rate in the reference group, there is no default. lambda2 can also be used to define piecewise exponentially distributed survival times (see details). Must be a positive numeric of length 1."
        },
        {
          "name": "median1",
          "required": false,
          "default": "NA_real_",
          "description": "The assumed median survival time in the treatment group, there is no default."
        },
        {
          "name": "median2",
          "required": false,
          "default": "NA_real_",
          "description": "The assumed median survival time in the reference group, there is no default. Must be a positive numeric of length 1."
        },
        {
          "name": "kappa",
          "required": false,
          "default": "1",
          "description": "A numeric value > 0. A kappa != 1 will be used for the specification of the shape of the Weibull distribution. Default is 1, i.e., the exponential survival distribution is used instead of the Weibull distribution. Note that the Weibull distribution cannot be used for the piecewise definition of the survival time distribution, i.e., only piecewiselambda (as a single value) and kappa can be specified. This function is equivalent to pweibull(t, shape = kappa, scale = 1 / lambda) of the stats package, i.e., the scale parameter is 1 / 'hazard rate'. For example, getPiecewiseExponentialDistribution(time = 130, piecewiseLambda = 0.01, kappa = 4.2) and pweibull(q = 130, shape = 4.2, scale = 1 / 0.01) provide the same result."
        },
        {
          "name": "hazardRatio",
          "required": false,
          "default": "NA_real_",
          "description": "The vector of hazard ratios under consideration. If the event or hazard rates in both treatment groups are defined, the hazard ratio needs not to be specified as it is calculated, there is no default. Must be a positive numeric of length 1."
        },
        {
          "name": "piecewiseSurvivalTime",
          "required": false,
          "default": "NA_real_",
          "description": "A vector that specifies the time intervals for the piecewise definition of the exponential survival time cumulative distribution function (for details see getPiecewiseSurvivalTime())."
        },
        {
          "name": "allocationRatioPlanned",
          "required": false,
          "default": "1",
          "description": "The planned allocation ratio n1 / n2 for a two treatment groups design, default is 1. For multi-arm designs, it is the allocation ratio relating the active arm(s) to the control. For simulating means and rates for a two treatment groups design, it can be a vector of length kMax, the number of stages. It can be a vector of length kMax, too, for multi-arm and enrichment designs. In these cases, a change of allocating subjects to treatment groups over the stages can be assessed. Note that internally allocationRatioPlanned is treated as a vector of length kMax, not a scalar."
        },
        {
          "name": "eventTime",
          "required": false,
          "default": "12",
          "description": "The assumed time under which the event rates are calculated, default is 12."
        },
        {
          "name": "accrualTime",
          "required": false,
          "default": "c(0, 12)",
          "description": "The assumed accrual time intervals for the study, default is c(0, 12) (for details see getAccrualTime())."
        },
        {
          "name": "accrualIntensity",
          "required": false,
          "default": "0.1",
          "description": "A numeric vector of accrual intensities, default is the relative intensity 0.1 (for details see getAccrualTime())."
        },
        {
          "name": "accrualIntensityType",
          "required": false,
          "default": "c(\"auto\", \"absolute\", \"relative\")",
          "description": "A character value specifying the accrual intensity input type. Must be one of \"auto\", \"absolute\", or \"relative\"; default is \"auto\", i.e., if all values are < 1 the type is \"relative\", otherwise it is \"absolute\"."
        },
        {
          "name": "maxNumberOfSubjects",
          "required": false,
          "default": "NA_real_",
          "description": "maxNumberOfSubjects > 0 needs to be specified. If accrual time and accrual intensity are specified, this will be calculated. Must be a positive integer of length 1."
        },
        {
          "name": "maxNumberOfEvents",
          "required": false,
          "default": "NA_real_",
          "description": "maxNumberOfEvents > 0 is the maximum number of events, it determines the power of the test and needs to be specified."
        },
        {
          "name": "dropoutRate1",
          "required": false,
          "default": "0",
          "description": "The assumed drop-out rate in the treatment group, default is 0."
        },
        {
          "name": "dropoutRate2",
          "required": false,
          "default": "0",
          "description": "The assumed drop-out rate in the control group, default is 0."
        },
        {
          "name": "dropoutTime",
          "required": false,
          "default": "12",
          "description": "The assumed time for drop-out rates in the control and the treatment group, default is 12."
        }
      ]
    },
    {
      "name": "getRawData",
      "family": "simulation",
      "title": "Get Simulation Raw Data for Survival",
      "description": "Returns the raw survival data which was generated for simulation.",
      "args": [
        {
          "name": "x",
          "required": true,
          "default": null,
          "description": "A SimulationResults object created by getSimulationSurvival()."
        },
        {
          "name": "aggregate",
          "required": false,
          "default": "FALSE",
          "description": "Logical. If TRUE the raw data will be aggregated similar to the result of getData(), default is FALSE."
        }
      ]
    },
    {
      "name": "getRepeatedConfidenceIntervals",
      "family": "analysis",
      "title": "Get Repeated Confidence Intervals",
      "description": "Calculates and returns the lower and upper limit of the repeated confidence intervals of the trial.",
      "args": [
        {
          "name": "design",
          "required": true,
          "default": null,
          "description": "The trial design."
        },
        {
          "name": "dataInput",
          "required": true,
          "default": null,
          "description": "The summary data used for calculating the test results. This is either an element of DatasetMeans, of DatasetRates, or of DatasetSurvival and should be created with the function getDataset(). For more information see getDataset()."
        },
        {
          "name": "...",
          "required": true,
          "default": null,
          "description": "Further arguments to be passed to methods (cf., separate functions in \"See Also\" below), e.g., normalApproximationThe type of computation of the p-values. Default is FALSE for testing means (i.e., the t test is used) and TRUE for testing rates and the hazard ratio. For testing rates, if normalApproximation = FALSE is specified, the binomial test (one sample) or the exact test of Fisher (two samples) is used for calculating the p-values. In the survival setting, normalApproximation = FALSE has no effect. equalVariancesThe type of t test. For testing means in two treatment groups, either the t test assuming that the variances are equal or the t test without assuming this, i.e., the test of Welch-Satterthwaite is calculated, default is TRUE. intersectionTestDefines the multiple test for the intersection hypotheses in the closed system of hypotheses when testing multiple hypotheses. Five options are available in multi-arm designs: \"Dunnett\", \"Bonferroni\", \"Simes\", \"Sidak\", and \"Hierarchical\", default is \"Dunnett\". Four options are available in population enrichment designs: \"SpiessensDebois\" (one subset only), \"Bonferroni\", \"Simes\", and \"Sidak\", default is \"Simes\". varianceOptionDefines the way to calculate the variance in multiple treatment arms (> 2) or population enrichment designs for testing means. For multiple arms, three options are available: \"overallPooled\", \"pairwisePooled\", and \"notPooled\", default is \"overallPooled\". For enrichment designs, the options are: \"pooled\", \"pooledFromFull\" (one subset only), and \"notPooled\", default is \"pooled\". stratifiedAnalysisFor enrichment designs, typically a stratified analysis should be chosen. For testing means and rates, also a non-stratified analysis based on overall data can be performed. For survival data, only a stratified analysis is possible (see Brannath et al., 2009), default is TRUE."
        },
        {
          "name": "directionUpper",
          "required": false,
          "default": "NA",
          "description": "Logical. Specifies the direction of the alternative, only applicable for one-sided testing; default is TRUE which means that larger values of the test statistics yield smaller p-values."
        },
        {
          "name": "tolerance",
          "required": false,
          "default": "1e-06",
          "description": "The numerical tolerance, default is 1e-06. Must be a positive numeric of length 1."
        },
        {
          "name": "stage",
          "required": false,
          "default": "NA_integer_",
          "description": "The stage number (optional). Default: total number of existing stages in the data input."
        }
      ]
    },
    {
      "name": "getRepeatedPValues",
      "family": "analysis",
      "title": "Get Repeated P Values",
      "description": "Calculates the repeated p-values for a given test results.",
      "args": [
        {
          "name": "stageResults",
          "required": true,
          "default": null,
          "description": "The results at given stage, obtained from getStageResults()."
        },
        {
          "name": "...",
          "required": true,
          "default": null,
          "description": "Ensures that all arguments (starting from the \"...\") are to be named and that a warning will be displayed if unknown arguments are passed."
        },
        {
          "name": "tolerance",
          "required": false,
          "default": "1e-06",
          "description": "The numerical tolerance, default is 1e-06. Must be a positive numeric of length 1."
        }
      ]
    },
    {
      "name": "getSampleSizeCounts",
      "family": "samplesize_power",
      "title": "Get Sample Size Counts",
      "description": "Returns the sample size for testing the ratio of mean rates of negative binomial distributed event numbers in two samples at given effect.",
      "args": [
        {
          "name": "design",
          "required": false,
          "default": "NULL",
          "description": "The trial design. If no trial design is specified, a fixed sample size design is used. In this case, Type I error rate alpha, Type II error rate beta, twoSidedPower, and sided can be directly entered as argument where necessary."
        },
        {
          "name": "...",
          "required": true,
          "default": null,
          "description": "Ensures that all arguments (starting from the \"...\") are to be named and that a warning will be displayed if unknown arguments are passed."
        },
        {
          "name": "lambda1",
          "required": false,
          "default": "NA_real_",
          "description": "A numeric value or vector that represents the assumed rate of a homogeneous Poisson process in the active treatment group, there is no default."
        },
        {
          "name": "lambda2",
          "required": false,
          "default": "NA_real_",
          "description": "A numeric value that represents the assumed rate of a homogeneous Poisson process in the control group, there is no default."
        },
        {
          "name": "lambda",
          "required": false,
          "default": "NA_real_",
          "description": "A numeric value or vector that represents the assumed rate of a homogeneous Poisson process in the pooled treatment groups, there is no default."
        },
        {
          "name": "theta",
          "required": false,
          "default": "NA_real_",
          "description": "A numeric value or vector that represents the assumed mean ratios lambda1/lambda2 of a homogeneous Poisson process, there is no default."
        },
        {
          "name": "thetaH0",
          "required": false,
          "default": "1",
          "description": "The null hypothesis value, default is 0 for the normal and the binary case (testing means and rates, respectively), it is 1 for the survival case (testing the hazard ratio). For non-inferiority designs, thetaH0 is the non-inferiority bound. That is, in case of (one-sided) testing of means: a value != 0 (or a value != 1 for testing the mean ratio) can be specified. rates: a value != 0 (or a value != 1 for testing the risk ratio pi1 / pi2) can be specified. survival data: a bound for testing H0: hazard ratio = thetaH0 != 1 can be specified. count data: a bound for testing H0: lambda1 / lambda2 = thetaH0 != 1 can be specified. For testing a rate in one sample, a value thetaH0 in (0, 1) has to be specified for defining the null hypothesis H0: pi = thetaH0."
        },
        {
          "name": "overdispersion",
          "required": false,
          "default": "0",
          "description": "A numeric value that represents the assumed overdispersion of the negative binomial distribution, default is 0."
        },
        {
          "name": "fixedExposureTime",
          "required": false,
          "default": "NA_real_",
          "description": "If specified, the fixed time of exposure per subject for count data, there is no default."
        },
        {
          "name": "accrualTime",
          "required": false,
          "default": "NA_real_",
          "description": "If specified, the assumed accrual time interval(s) for the study, there is no default."
        },
        {
          "name": "accrualIntensity",
          "required": false,
          "default": "NA_real_",
          "description": "If specified, the assumed accrual intensities for the study, there is no default."
        },
        {
          "name": "followUpTime",
          "required": false,
          "default": "NA_real_",
          "description": "If specified, the assumed (additional) follow-up time for the study, there is no default. The total study duration is accrualTime + followUpTime."
        },
        {
          "name": "maxNumberOfSubjects",
          "required": false,
          "default": "NA_integer_",
          "description": "maxNumberOfSubjects > 0 needs to be specified for power calculations or calculation of necessary follow-up (count data). For two treatment arms, it is the maximum number of subjects for both treatment arms."
        },
        {
          "name": "allocationRatioPlanned",
          "required": false,
          "default": "NA_real_",
          "description": "The planned allocation ratio n1 / n2 for a two treatment groups design, default is 1. If allocationRatioPlanned = 0 is entered, the optimal allocation ratio yielding the smallest overall sample size is determined."
        }
      ]
    },
    {
      "name": "getSampleSizeMeans",
      "family": "samplesize_power",
      "title": "Get Sample Size Means",
      "description": "Returns the sample size for testing means in one or two samples.",
      "args": [
        {
          "name": "design",
          "required": false,
          "default": "NULL",
          "description": "The trial design. If no trial design is specified, a fixed sample size design is used. In this case, Type I error rate alpha, Type II error rate beta, twoSidedPower, and sided can be directly entered as argument where necessary."
        },
        {
          "name": "...",
          "required": true,
          "default": null,
          "description": "Ensures that all arguments (starting from the \"...\") are to be named and that a warning will be displayed if unknown arguments are passed."
        },
        {
          "name": "groups",
          "required": false,
          "default": "2L",
          "description": "The number of treatment groups (1 or 2), default is 2."
        },
        {
          "name": "normalApproximation",
          "required": false,
          "default": "FALSE",
          "description": "The type of computation of the p-values. If TRUE, the variance is assumed to be known, default is FALSE, i.e., the calculations are performed with the t distribution."
        },
        {
          "name": "meanRatio",
          "required": false,
          "default": "FALSE",
          "description": "If TRUE, the sample size for one-sided testing of H0: mu1 / mu2 = thetaH0 is calculated, default is FALSE."
        },
        {
          "name": "thetaH0",
          "required": false,
          "default": "ifelse(meanRatio, 1, 0)",
          "description": "The null hypothesis value, default is 0 for the normal and the binary case (testing means and rates, respectively), it is 1 for the survival case (testing the hazard ratio). For non-inferiority designs, thetaH0 is the non-inferiority bound. That is, in case of (one-sided) testing of means: a value != 0 (or a value != 1 for testing the mean ratio) can be specified. rates: a value != 0 (or a value != 1 for testing the risk ratio pi1 / pi2) can be specified. survival data: a bound for testing H0: hazard ratio = thetaH0 != 1 can be specified. count data: a bound for testing H0: lambda1 / lambda2 = thetaH0 != 1 can be specified. For testing a rate in one sample, a value thetaH0 in (0, 1) has to be specified for defining the null hypothesis H0: pi = thetaH0."
        },
        {
          "name": "alternative",
          "required": false,
          "default": "seq(0.2, 1, 0.2)",
          "description": "The alternative hypothesis value for testing means. This can be a vector of assumed alternatives, default is seq(0, 1, 0.2) (power calculations) or seq(0.2, 1, 0.2) (sample size calculations)."
        },
        {
          "name": "stDev",
          "required": false,
          "default": "1",
          "description": "The standard deviation under which the sample size or power calculation is performed, default is 1. For two-armed trials, it is allowed to specify the standard deviations separately, i.e., as vector with two elements. If meanRatio = TRUE is specified, stDev defines the coefficient of variation sigma / mu2."
        },
        {
          "name": "allocationRatioPlanned",
          "required": false,
          "default": "NA_real_",
          "description": "The planned allocation ratio n1 / n2 for a two treatment groups design, default is 1. If allocationRatioPlanned = 0 is entered, the optimal allocation ratio yielding the smallest overall sample size is determined."
        }
      ]
    },
    {
      "name": "getSampleSizeRates",
      "family": "samplesize_power",
      "title": "Get Sample Size Rates",
      "description": "Returns the sample size for testing rates in one or two samples.",
      "args": [
        {
          "name": "design",
          "required": false,
          "default": "NULL",
          "description": "The trial design. If no trial design is specified, a fixed sample size design is used. In this case, Type I error rate alpha, Type II error rate beta, twoSidedPower, and sided can be directly entered as argument where necessary."
        },
        {
          "name": "...",
          "required": true,
          "default": null,
          "description": "Ensures that all arguments (starting from the \"...\") are to be named and that a warning will be displayed if unknown arguments are passed."
        },
        {
          "name": "groups",
          "required": false,
          "default": "2L",
          "description": "The number of treatment groups (1 or 2), default is 2."
        },
        {
          "name": "normalApproximation",
          "required": false,
          "default": "TRUE",
          "description": "If FALSE, the sample size for the case of one treatment group is calculated exactly using the binomial distribution, default is TRUE."
        },
        {
          "name": "conservative",
          "required": false,
          "default": "TRUE",
          "description": "For the case of one treatment group and normalApproximation = FALSE, if TRUE, the sample size is calculated such that for larger sample size than the calculated, the power is larger than 1 - beta, for conservative = FALSE, the minimum sample size, for which power exceeds 1 - beta is calculated, default is TRUE."
        },
        {
          "name": "riskRatio",
          "required": false,
          "default": "FALSE",
          "description": "If TRUE, the sample size for one-sided testing of H0: pi1 / pi2 = thetaH0 is calculated, default is FALSE."
        },
        {
          "name": "thetaH0",
          "required": false,
          "default": "ifelse(riskRatio, 1, 0)",
          "description": "The null hypothesis value, default is 0 for the normal and the binary case (testing means and rates, respectively), it is 1 for the survival case (testing the hazard ratio). For non-inferiority designs, thetaH0 is the non-inferiority bound. That is, in case of (one-sided) testing of means: a value != 0 (or a value != 1 for testing the mean ratio) can be specified. rates: a value != 0 (or a value != 1 for testing the risk ratio pi1 / pi2) can be specified. survival data: a bound for testing H0: hazard ratio = thetaH0 != 1 can be specified. count data: a bound for testing H0: lambda1 / lambda2 = thetaH0 != 1 can be specified. For testing a rate in one sample, a value thetaH0 in (0, 1) has to be specified for defining the null hypothesis H0: pi = thetaH0."
        },
        {
          "name": "pi1",
          "required": false,
          "default": "c(0.4, 0.5, 0.6)",
          "description": "A numeric value or vector that represents the assumed probability in the active treatment group if two treatment groups are considered, or the alternative probability for a one treatment group design, default is seq(0.2, 0.5, 0.1) (power calculations and simulations) or seq(0.4, 0.6, 0.1) (sample size calculations)."
        },
        {
          "name": "pi2",
          "required": false,
          "default": "0.2",
          "description": "A numeric value that represents the assumed probability in the reference group if two treatment groups are considered, default is 0.2."
        },
        {
          "name": "allocationRatioPlanned",
          "required": false,
          "default": "NA_real_",
          "description": "The planned allocation ratio n1 / n2 for a two treatment groups design, default is 1. If allocationRatioPlanned = 0 is entered, the optimal allocation ratio yielding the smallest overall sample size is determined."
        }
      ]
    },
    {
      "name": "getSampleSizeSurvival",
      "family": "samplesize_power",
      "title": "Get Sample Size Survival",
      "description": "Returns the sample size for testing the hazard ratio in a two treatment groups survival design.",
      "args": [
        {
          "name": "design",
          "required": false,
          "default": "NULL",
          "description": "The trial design. If no trial design is specified, a fixed sample size design is used. In this case, Type I error rate alpha, Type II error rate beta, twoSidedPower, and sided can be directly entered as argument where necessary."
        },
        {
          "name": "...",
          "required": true,
          "default": null,
          "description": "Ensures that all arguments (starting from the \"...\") are to be named and that a warning will be displayed if unknown arguments are passed."
        },
        {
          "name": "typeOfComputation",
          "required": false,
          "default": "c(\"Schoenfeld\", \"Freedman\", \"HsiehFreedman\")",
          "description": "Three options are available: \"Schoenfeld\", \"Freedman\", \"HsiehFreedman\", the default is \"Schoenfeld\". For details, see Hsieh (Statistics in Medicine, 1992). For non-inferiority testing (i.e., thetaH0 != 1), only Schoenfeld's formula can be used."
        },
        {
          "name": "thetaH0",
          "required": false,
          "default": "1",
          "description": "The null hypothesis value, default is 0 for the normal and the binary case (testing means and rates, respectively), it is 1 for the survival case (testing the hazard ratio). For non-inferiority designs, thetaH0 is the non-inferiority bound. That is, in case of (one-sided) testing of means: a value != 0 (or a value != 1 for testing the mean ratio) can be specified. rates: a value != 0 (or a value != 1 for testing the risk ratio pi1 / pi2) can be specified. survival data: a bound for testing H0: hazard ratio = thetaH0 != 1 can be specified. count data: a bound for testing H0: lambda1 / lambda2 = thetaH0 != 1 can be specified. For testing a rate in one sample, a value thetaH0 in (0, 1) has to be specified for defining the null hypothesis H0: pi = thetaH0."
        },
        {
          "name": "pi1",
          "required": false,
          "default": "NA_real_",
          "description": "A numeric value or vector that represents the assumed event rate in the treatment group, default is seq(0.2, 0.5, 0.1) (power calculations and simulations) or seq(0.4, 0.6, 0.1) (sample size calculations)."
        },
        {
          "name": "pi2",
          "required": false,
          "default": "NA_real_",
          "description": "A numeric value that represents the assumed event rate in the control group, default is 0.2."
        },
        {
          "name": "lambda1",
          "required": false,
          "default": "NA_real_",
          "description": "The assumed hazard rate in the treatment group, there is no default. lambda1 can also be used to define piecewise exponentially distributed survival times (see details). Must be a positive numeric of length 1."
        },
        {
          "name": "lambda2",
          "required": false,
          "default": "NA_real_",
          "description": "The assumed hazard rate in the reference group, there is no default. lambda2 can also be used to define piecewise exponentially distributed survival times (see details). Must be a positive numeric of length 1."
        },
        {
          "name": "median1",
          "required": false,
          "default": "NA_real_",
          "description": "The assumed median survival time in the treatment group, there is no default."
        },
        {
          "name": "median2",
          "required": false,
          "default": "NA_real_",
          "description": "The assumed median survival time in the reference group, there is no default. Must be a positive numeric of length 1."
        },
        {
          "name": "kappa",
          "required": false,
          "default": "1",
          "description": "A numeric value > 0. A kappa != 1 will be used for the specification of the shape of the Weibull distribution. Default is 1, i.e., the exponential survival distribution is used instead of the Weibull distribution. Note that the Weibull distribution cannot be used for the piecewise definition of the survival time distribution, i.e., only piecewiselambda (as a single value) and kappa can be specified. This function is equivalent to pweibull(t, shape = kappa, scale = 1 / lambda) of the stats package, i.e., the scale parameter is 1 / 'hazard rate'. For example, getPiecewiseExponentialDistribution(time = 130, piecewiseLambda = 0.01, kappa = 4.2) and pweibull(q = 130, shape = 4.2, scale = 1 / 0.01) provide the same result."
        },
        {
          "name": "hazardRatio",
          "required": false,
          "default": "NA_real_",
          "description": "The vector of hazard ratios under consideration. If the event or hazard rates in both treatment groups are defined, the hazard ratio needs not to be specified as it is calculated, there is no default. Must be a positive numeric of length 1."
        },
        {
          "name": "piecewiseSurvivalTime",
          "required": false,
          "default": "NA_real_",
          "description": "A vector that specifies the time intervals for the piecewise definition of the exponential survival time cumulative distribution function (for details see getPiecewiseSurvivalTime())."
        },
        {
          "name": "allocationRatioPlanned",
          "required": false,
          "default": "NA_real_",
          "description": "The planned allocation ratio n1 / n2 for a two treatment groups design, default is 1. If allocationRatioPlanned = 0 is entered, the optimal allocation ratio yielding the smallest overall sample size is determined."
        },
        {
          "name": "eventTime",
          "required": false,
          "default": "12",
          "description": "The assumed time under which the event rates are calculated, default is 12."
        },
        {
          "name": "accrualTime",
          "required": false,
          "default": "c(0, 12)",
          "description": "The assumed accrual time intervals for the study, default is c(0, 12) (for details see getAccrualTime())."
        },
        {
          "name": "accrualIntensity",
          "required": false,
          "default": "0.1",
          "description": "A numeric vector of accrual intensities, default is the relative intensity 0.1 (for details see getAccrualTime())."
        },
        {
          "name": "accrualIntensityType",
          "required": false,
          "default": "c(\"auto\", \"absolute\", \"relative\")",
          "description": "A character value specifying the accrual intensity input type. Must be one of \"auto\", \"absolute\", or \"relative\"; default is \"auto\", i.e., if all values are < 1 the type is \"relative\", otherwise it is \"absolute\"."
        },
        {
          "name": "followUpTime",
          "required": false,
          "default": "NA_real_",
          "description": "The assumed (additional) follow-up time for the study, default is 6. The total study duration is accrualTime + followUpTime."
        },
        {
          "name": "maxNumberOfSubjects",
          "required": false,
          "default": "NA_real_",
          "description": "If maxNumberOfSubjects > 0 is specified, the follow-up time for the required number of events is determined."
        },
        {
          "name": "dropoutRate1",
          "required": false,
          "default": "0",
          "description": "The assumed drop-out rate in the treatment group, default is 0."
        },
        {
          "name": "dropoutRate2",
          "required": false,
          "default": "0",
          "description": "The assumed drop-out rate in the control group, default is 0."
        },
        {
          "name": "dropoutTime",
          "required": false,
          "default": "12",
          "description": "The assumed time for drop-out rates in the control and the treatment group, default is 12."
        }
      ]
    },
    {
      "name": "getSimulationCounts",
      "family": "simulation",
      "title": "Get Simulation Counts",
      "description": "Returns the simulated power, stopping probabilities, conditional power, and expected sample size for testing mean rates for negative binomial distributed event numbers in the two treatment groups testing situation.",
      "args": [
        {
          "name": "design",
          "required": false,
          "default": "NULL",
          "description": "The trial design. If no trial design is specified, a fixed sample size design is used. In this case, Type I error rate alpha, Type II error rate beta, twoSidedPower, and sided can be directly entered as argument where necessary."
        },
        {
          "name": "...",
          "required": true,
          "default": null,
          "description": "Ensures that all arguments (starting from the \"...\") are to be named and that a warning will be displayed if unknown arguments are passed."
        },
        {
          "name": "plannedCalendarTime",
          "required": false,
          "default": "NA_real_",
          "description": "For simulating count data, the time points where an analysis is planned to be performed. Should be a vector of length kMax"
        },
        {
          "name": "maxNumberOfSubjects",
          "required": false,
          "default": "NA_real_",
          "description": "maxNumberOfSubjects > 0 needs to be specified for power calculations or calculation of necessary follow-up (count data). For two treatment arms, it is the maximum number of subjects for both treatment arms."
        },
        {
          "name": "lambda1",
          "required": false,
          "default": "NA_real_",
          "description": "A numeric value or vector that represents the assumed rate of a homogeneous Poisson process in the active treatment group, there is no default."
        },
        {
          "name": "lambda2",
          "required": false,
          "default": "NA_real_",
          "description": "A numeric value that represents the assumed rate of a homogeneous Poisson process in the control group, there is no default."
        },
        {
          "name": "lambda",
          "required": false,
          "default": "NA_real_",
          "description": "A numeric value or vector that represents the assumed rate of a homogeneous Poisson process in the pooled treatment groups, there is no default."
        },
        {
          "name": "theta",
          "required": false,
          "default": "NA_real_",
          "description": "A numeric value or vector that represents the assumed mean ratios lambda1/lambda2 of a homogeneous Poisson process, there is no default."
        },
        {
          "name": "directionUpper",
          "required": false,
          "default": "NA",
          "description": "Logical. Specifies the direction of the alternative, only applicable for one-sided testing; default is TRUE which means that larger values of the test statistics yield smaller p-values."
        },
        {
          "name": "thetaH0",
          "required": false,
          "default": "1",
          "description": "The null hypothesis value, default is 0 for the normal and the binary case (testing means and rates, respectively), it is 1 for the survival case (testing the hazard ratio). For non-inferiority designs, thetaH0 is the non-inferiority bound. That is, in case of (one-sided) testing of means: a value != 0 (or a value != 1 for testing the mean ratio) can be specified. rates: a value != 0 (or a value != 1 for testing the risk ratio pi1 / pi2) can be specified. survival data: a bound for testing H0: hazard ratio = thetaH0 != 1 can be specified. count data: a bound for testing H0: lambda1 / lambda2 = thetaH0 != 1 can be specified. For testing a rate in one sample, a value thetaH0 in (0, 1) has to be specified for defining the null hypothesis H0: pi = thetaH0."
        },
        {
          "name": "overdispersion",
          "required": false,
          "default": "0",
          "description": "A numeric value that represents the assumed overdispersion of the negative binomial distribution, default is 0."
        },
        {
          "name": "fixedExposureTime",
          "required": false,
          "default": "NA_real_",
          "description": "If specified, the fixed time of exposure per subject for count data, there is no default."
        },
        {
          "name": "accrualTime",
          "required": false,
          "default": "NA_real_",
          "description": "If specified, the assumed accrual time interval(s) for the study, there is no default."
        },
        {
          "name": "accrualIntensity",
          "required": false,
          "default": "NA_real_",
          "description": "If specified, the assumed accrual intensities for the study, there is no default."
        },
        {
          "name": "followUpTime",
          "required": false,
          "default": "NA_real_",
          "description": "If specified, the assumed (additional) follow-up time for the study, there is no default. The total study duration is accrualTime + followUpTime."
        },
        {
          "name": "allocationRatioPlanned",
          "required": false,
          "default": "NA_real_",
          "description": "The planned allocation ratio n1 / n2 for a two treatment groups design, default is 1. For multi-arm designs, it is the allocation ratio relating the active arm(s) to the control. For simulating means and rates for a two treatment groups design, it can be a vector of length kMax, the number of stages. It can be a vector of length kMax, too, for multi-arm and enrichment designs. In these cases, a change of allocating subjects to treatment groups over the stages can be assessed. Note that internally allocationRatioPlanned is treated as a vector of length kMax, not a scalar."
        },
        {
          "name": "maxNumberOfIterations",
          "required": false,
          "default": "1000L",
          "description": "The number of simulation iterations, default is 1000. Must be a positive integer of length 1."
        },
        {
          "name": "seed",
          "required": false,
          "default": "NA_real_",
          "description": "The seed to reproduce the simulation, default is a random seed."
        },
        {
          "name": "showStatistics",
          "required": false,
          "default": "FALSE",
          "description": "Logical. If TRUE, summary statistics of the simulated data are displayed for the print command, otherwise the output is suppressed, default is FALSE."
        }
      ]
    },
    {
      "name": "getSimulationEnrichmentMeans",
      "family": "simulation",
      "title": "Get Simulation Enrichment Means",
      "description": "Returns the simulated power, stopping and selection probabilities, conditional power, and expected sample size or testing means in an enrichment design testing situation.",
      "args": [
        {
          "name": "design",
          "required": false,
          "default": "NULL",
          "description": "The trial design. If no trial design is specified, a fixed sample size design is used. In this case, Type I error rate alpha, Type II error rate beta, twoSidedPower, and sided can be directly entered as argument where necessary."
        },
        {
          "name": "...",
          "required": true,
          "default": null,
          "description": "Ensures that all arguments (starting from the \"...\") are to be named and that a warning will be displayed if unknown arguments are passed."
        },
        {
          "name": "effectList",
          "required": false,
          "default": "NULL",
          "description": "List of subsets, prevalences, and effect sizes with columns and number of rows reflecting the different situations to consider (see examples)."
        },
        {
          "name": "intersectionTest",
          "required": false,
          "default": "c(\"Simes\", \"SpiessensDebois\", \"Bonferroni\", \"Sidak\")",
          "description": "Defines the multiple test for the intersection hypotheses in the closed system of hypotheses. Four options are available in enrichment designs: \"SpiessensDebois\", \"Bonferroni\", \"Simes\", and \"Sidak\", default is \"Simes\"."
        },
        {
          "name": "stratifiedAnalysis",
          "required": false,
          "default": "TRUE",
          "description": "Logical. For enrichment designs, typically a stratified analysis should be chosen. For testing rates, also a non-stratified analysis based on overall data can be performed. For survival data, only a stratified analysis is possible (see Brannath et al., 2009), default is TRUE."
        },
        {
          "name": "adaptations",
          "required": false,
          "default": "NA",
          "description": "A logical vector of length kMax - 1 indicating whether or not an adaptation takes place at interim k, default is rep(TRUE, kMax - 1)."
        },
        {
          "name": "typeOfSelection",
          "required": false,
          "default": "c(\"best\", \"rBest\", \"epsilon\", \"all\", \"userDefined\")",
          "description": "The way the treatment arms or populations are selected at interim. Five options are available: \"best\", \"rbest\", \"epsilon\", \"all\", and \"userDefined\", default is \"best\". For \"rbest\" (select the rValue best treatment arms/populations), the parameter rValue has to be specified, for \"epsilon\" (select treatment arm/population not worse than epsilon compared to the best), the parameter epsilonValue has to be specified. If \"userDefined\" is selected, \"selectArmsFunction\" or \"selectPopulationsFunction\" has to be specified."
        },
        {
          "name": "effectMeasure",
          "required": false,
          "default": "c(\"effectEstimate\", \"testStatistic\")",
          "description": "Criterion for treatment arm/population selection, either based on test statistic (\"testStatistic\") or effect estimate (difference for means and rates or ratio for survival) (\"effectEstimate\"), default is \"effectEstimate\"."
        },
        {
          "name": "successCriterion",
          "required": false,
          "default": "c(\"all\", \"atLeastOne\")",
          "description": "Defines when the study is stopped for efficacy at interim. Two options are available: \"all\" stops the trial if the efficacy criterion is fulfilled for all selected treatment arms/populations, \"atLeastOne\" stops if at least one of the selected treatment arms/populations is shown to be superior to control at interim, default is \"all\"."
        },
        {
          "name": "epsilonValue",
          "required": false,
          "default": "NA_real_",
          "description": "For typeOfSelection = \"epsilon\" (select treatment arm / population not worse than epsilon compared to the best), the parameter epsilonValue has to be specified. Must be a numeric of length 1."
        },
        {
          "name": "rValue",
          "required": false,
          "default": "NA_real_",
          "description": "For typeOfSelection = \"rbest\" (select the rValue best treatment arms / populations), the parameter rValue has to be specified."
        },
        {
          "name": "threshold",
          "required": false,
          "default": "-Inf",
          "description": "Selection criterion: treatment arm / population is selected only if effectMeasure exceeds threshold, default is -Inf. threshold can also be a vector of length activeArms referring to a separate threshold condition over the treatment arms."
        },
        {
          "name": "plannedSubjects",
          "required": false,
          "default": "NA_integer_",
          "description": "plannedSubjects is a numeric vector of length kMax (the number of stages of the design) that determines the number of cumulated (overall) subjects when the interim stages are planned. For two treatment arms, it is the number of subjects for both treatment arms. For multi-arm designs, plannedSubjects refers to the number of subjects per selected active arm."
        },
        {
          "name": "allocationRatioPlanned",
          "required": false,
          "default": "NA_real_",
          "description": "The planned allocation ratio n1 / n2 for a two treatment groups design, default is 1. For multi-arm designs, it is the allocation ratio relating the active arm(s) to the control. For simulating means and rates for a two treatment groups design, it can be a vector of length kMax, the number of stages. It can be a vector of length kMax, too, for multi-arm and enrichment designs. In these cases, a change of allocating subjects to treatment groups over the stages can be assessed. Note that internally allocationRatioPlanned is treated as a vector of length kMax, not a scalar."
        },
        {
          "name": "minNumberOfSubjectsPerStage",
          "required": false,
          "default": "NA_real_",
          "description": "When performing a data driven sample size recalculation, the numeric vector minNumberOfSubjectsPerStage with length kMax determines the minimum number of subjects per stage (i.e., not cumulated), the first element is not taken into account. For two treatment arms, it is the number of subjects for both treatment arms. For multi-arm designs minNumberOfSubjectsPerStage refers to the minimum number of subjects per selected active arm."
        },
        {
          "name": "maxNumberOfSubjectsPerStage",
          "required": false,
          "default": "NA_real_",
          "description": "When performing a data driven sample size recalculation, the numeric vector maxNumberOfSubjectsPerStage with length kMax determines the maximum number of subjects per stage (i.e., not cumulated), the first element is not taken into account. For two treatment arms, it is the number of subjects for both treatment arms. For multi-arm designs maxNumberOfSubjectsPerStage refers to the maximum number of subjects per selected active arm."
        },
        {
          "name": "conditionalPower",
          "required": false,
          "default": "NA_real_",
          "description": "If conditionalPower together with minNumberOfSubjectsPerStage and maxNumberOfSubjectsPerStage (or minNumberOfEventsPerStage and maxNumberOfEventsPerStage for survival designs) is specified, a sample size recalculation based on the specified conditional power is performed. It is defined as the power for the subsequent stage given the current data. By default, the conditional power will be calculated under the observed effect size. Optionally, you can also specify thetaH1 and stDevH1 (for simulating means), pi1H1 and pi2H1 (for simulating rates), or thetaH1 (for simulating hazard ratios) as parameters under which it is calculated and the sample size recalculation is performed."
        },
        {
          "name": "thetaH1",
          "required": false,
          "default": "NA_real_",
          "description": "If specified, the value of the alternative under which the conditional power or sample size recalculation calculation is performed. Must be a numeric of length 1."
        },
        {
          "name": "stDevH1",
          "required": false,
          "default": "NA_real_",
          "description": "If specified, the value of the standard deviation under which the conditional power or sample size recalculation calculation is performed, default is the value of stDev."
        },
        {
          "name": "maxNumberOfIterations",
          "required": false,
          "default": "1000L",
          "description": "The number of simulation iterations, default is 1000. Must be a positive integer of length 1."
        },
        {
          "name": "seed",
          "required": false,
          "default": "NA_real_",
          "description": "The seed to reproduce the simulation, default is a random seed."
        },
        {
          "name": "calcSubjectsFunction",
          "required": false,
          "default": "NULL",
          "description": "Optionally, a function can be entered that defines the way of performing the sample size recalculation. By default, sample size recalculation is performed with conditional power and specified minNumberOfSubjectsPerStage and maxNumberOfSubjectsPerStage (see details and examples)."
        },
        {
          "name": "selectPopulationsFunction",
          "required": false,
          "default": "NULL",
          "description": "Optionally, a function can be entered that defines the way of how populations are selected. This function is allowed to depend on effectVector with length populations stage, conditionalPower, conditionalCriticalValue, plannedSubjects/plannedEvents, allocationRatioPlanned, selectedPopulations, thetaH1 (for means and survival), stDevH1 (for means), overallEffects, and for rates additionally: piTreatmentsH1, piControlH1, overallRates, and overallRatesControl (see examples)."
        },
        {
          "name": "showStatistics",
          "required": false,
          "default": "FALSE",
          "description": "Logical. If TRUE, summary statistics of the simulated data are displayed for the print command, otherwise the output is suppressed, default is FALSE."
        }
      ]
    },
    {
      "name": "getSimulationEnrichmentRates",
      "family": "simulation",
      "title": "Get Simulation Enrichment Rates",
      "description": "Returns the simulated power, stopping and selection probabilities, conditional power, and expected sample size for testing rates in an enrichment design testing situation.",
      "args": [
        {
          "name": "design",
          "required": false,
          "default": "NULL",
          "description": "The trial design. If no trial design is specified, a fixed sample size design is used. In this case, Type I error rate alpha, Type II error rate beta, twoSidedPower, and sided can be directly entered as argument where necessary."
        },
        {
          "name": "...",
          "required": true,
          "default": null,
          "description": "Ensures that all arguments (starting from the \"...\") are to be named and that a warning will be displayed if unknown arguments are passed."
        },
        {
          "name": "effectList",
          "required": false,
          "default": "NULL",
          "description": "List of subsets, prevalences, and effect sizes with columns and number of rows reflecting the different situations to consider (see examples)."
        },
        {
          "name": "intersectionTest",
          "required": false,
          "default": "c(\"Simes\", \"SpiessensDebois\", \"Bonferroni\", \"Sidak\")",
          "description": "Defines the multiple test for the intersection hypotheses in the closed system of hypotheses. Four options are available in enrichment designs: \"SpiessensDebois\", \"Bonferroni\", \"Simes\", and \"Sidak\", default is \"Simes\"."
        },
        {
          "name": "stratifiedAnalysis",
          "required": false,
          "default": "TRUE",
          "description": "Logical. For enrichment designs, typically a stratified analysis should be chosen. For testing rates, also a non-stratified analysis based on overall data can be performed. For survival data, only a stratified analysis is possible (see Brannath et al., 2009), default is TRUE."
        },
        {
          "name": "directionUpper",
          "required": false,
          "default": "NA",
          "description": "Logical. Specifies the direction of the alternative, only applicable for one-sided testing; default is TRUE which means that larger values of the test statistics yield smaller p-values."
        },
        {
          "name": "adaptations",
          "required": false,
          "default": "NA",
          "description": "A logical vector of length kMax - 1 indicating whether or not an adaptation takes place at interim k, default is rep(TRUE, kMax - 1)."
        },
        {
          "name": "typeOfSelection",
          "required": false,
          "default": "c(\"best\", \"rBest\", \"epsilon\", \"all\", \"userDefined\")",
          "description": "The way the treatment arms or populations are selected at interim. Five options are available: \"best\", \"rbest\", \"epsilon\", \"all\", and \"userDefined\", default is \"best\". For \"rbest\" (select the rValue best treatment arms/populations), the parameter rValue has to be specified, for \"epsilon\" (select treatment arm/population not worse than epsilon compared to the best), the parameter epsilonValue has to be specified. If \"userDefined\" is selected, \"selectArmsFunction\" or \"selectPopulationsFunction\" has to be specified."
        },
        {
          "name": "effectMeasure",
          "required": false,
          "default": "c(\"effectEstimate\", \"testStatistic\")",
          "description": "Criterion for treatment arm/population selection, either based on test statistic (\"testStatistic\") or effect estimate (difference for means and rates or ratio for survival) (\"effectEstimate\"), default is \"effectEstimate\"."
        },
        {
          "name": "successCriterion",
          "required": false,
          "default": "c(\"all\", \"atLeastOne\")",
          "description": "Defines when the study is stopped for efficacy at interim. Two options are available: \"all\" stops the trial if the efficacy criterion is fulfilled for all selected treatment arms/populations, \"atLeastOne\" stops if at least one of the selected treatment arms/populations is shown to be superior to control at interim, default is \"all\"."
        },
        {
          "name": "epsilonValue",
          "required": false,
          "default": "NA_real_",
          "description": "For typeOfSelection = \"epsilon\" (select treatment arm / population not worse than epsilon compared to the best), the parameter epsilonValue has to be specified. Must be a numeric of length 1."
        },
        {
          "name": "rValue",
          "required": false,
          "default": "NA_real_",
          "description": "For typeOfSelection = \"rbest\" (select the rValue best treatment arms / populations), the parameter rValue has to be specified."
        },
        {
          "name": "threshold",
          "required": false,
          "default": "-Inf",
          "description": "Selection criterion: treatment arm / population is selected only if effectMeasure exceeds threshold, default is -Inf. threshold can also be a vector of length activeArms referring to a separate threshold condition over the treatment arms."
        },
        {
          "name": "plannedSubjects",
          "required": false,
          "default": "NA_real_",
          "description": "plannedSubjects is a numeric vector of length kMax (the number of stages of the design) that determines the number of cumulated (overall) subjects when the interim stages are planned. For two treatment arms, it is the number of subjects for both treatment arms. For multi-arm designs, plannedSubjects refers to the number of subjects per selected active arm."
        },
        {
          "name": "allocationRatioPlanned",
          "required": false,
          "default": "NA_real_",
          "description": "The planned allocation ratio n1 / n2 for a two treatment groups design, default is 1. For multi-arm designs, it is the allocation ratio relating the active arm(s) to the control. For simulating means and rates for a two treatment groups design, it can be a vector of length kMax, the number of stages. It can be a vector of length kMax, too, for multi-arm and enrichment designs. In these cases, a change of allocating subjects to treatment groups over the stages can be assessed. Note that internally allocationRatioPlanned is treated as a vector of length kMax, not a scalar."
        },
        {
          "name": "minNumberOfSubjectsPerStage",
          "required": false,
          "default": "NA_real_",
          "description": "When performing a data driven sample size recalculation, the numeric vector minNumberOfSubjectsPerStage with length kMax determines the minimum number of subjects per stage (i.e., not cumulated), the first element is not taken into account. For two treatment arms, it is the number of subjects for both treatment arms. For multi-arm designs minNumberOfSubjectsPerStage refers to the minimum number of subjects per selected active arm."
        },
        {
          "name": "maxNumberOfSubjectsPerStage",
          "required": false,
          "default": "NA_real_",
          "description": "When performing a data driven sample size recalculation, the numeric vector maxNumberOfSubjectsPerStage with length kMax determines the maximum number of subjects per stage (i.e., not cumulated), the first element is not taken into account. For two treatment arms, it is the number of subjects for both treatment arms. For multi-arm designs maxNumberOfSubjectsPerStage refers to the maximum number of subjects per selected active arm."
        },
        {
          "name": "conditionalPower",
          "required": false,
          "default": "NA_real_",
          "description": "If conditionalPower together with minNumberOfSubjectsPerStage and maxNumberOfSubjectsPerStage (or minNumberOfEventsPerStage and maxNumberOfEventsPerStage for survival designs) is specified, a sample size recalculation based on the specified conditional power is performed. It is defined as the power for the subsequent stage given the current data. By default, the conditional power will be calculated under the observed effect size. Optionally, you can also specify thetaH1 and stDevH1 (for simulating means), pi1H1 and pi2H1 (for simulating rates), or thetaH1 (for simulating hazard ratios) as parameters under which it is calculated and the sample size recalculation is performed."
        },
        {
          "name": "piTreatmentH1",
          "required": false,
          "default": "NA_real_",
          "description": "If specified, the assumed probabilities in the active arm under which the sample size recalculation was performed and the conditional power was calculated."
        },
        {
          "name": "piControlH1",
          "required": false,
          "default": "NA_real_",
          "description": "If specified, the assumed probabilities in the control arm under which the sample size recalculation was performed and the conditional power was calculated."
        },
        {
          "name": "maxNumberOfIterations",
          "required": false,
          "default": "1000L",
          "description": "The number of simulation iterations, default is 1000. Must be a positive integer of length 1."
        },
        {
          "name": "seed",
          "required": false,
          "default": "NA_real_",
          "description": "The seed to reproduce the simulation, default is a random seed."
        },
        {
          "name": "calcSubjectsFunction",
          "required": false,
          "default": "NULL",
          "description": "Optionally, a function can be entered that defines the way of performing the sample size recalculation. By default, sample size recalculation is performed with conditional power and specified minNumberOfSubjectsPerStage and maxNumberOfSubjectsPerStage (see details and examples)."
        },
        {
          "name": "selectPopulationsFunction",
          "required": false,
          "default": "NULL",
          "description": "Optionally, a function can be entered that defines the way of how populations are selected. This function is allowed to depend on effectVector with length populations stage, conditionalPower, conditionalCriticalValue, plannedSubjects/plannedEvents, allocationRatioPlanned, selectedPopulations, thetaH1 (for means and survival), stDevH1 (for means), overallEffects, and for rates additionally: piTreatmentsH1, piControlH1, overallRates, and overallRatesControl (see examples)."
        },
        {
          "name": "showStatistics",
          "required": false,
          "default": "FALSE",
          "description": "Logical. If TRUE, summary statistics of the simulated data are displayed for the print command, otherwise the output is suppressed, default is FALSE."
        }
      ]
    },
    {
      "name": "getSimulationEnrichmentSurvival",
      "family": "simulation",
      "title": "Get Simulation Enrichment Survival",
      "description": "Returns the simulated power, stopping and selection probabilities, conditional power, and expected sample size for testing hazard ratios in an enrichment design testing situation. In contrast to getSimulationSurvival() (where survival times are simulated), normally distributed logrank test statistics are simulated.",
      "args": [
        {
          "name": "design",
          "required": false,
          "default": "NULL",
          "description": "The trial design. If no trial design is specified, a fixed sample size design is used. In this case, Type I error rate alpha, Type II error rate beta, twoSidedPower, and sided can be directly entered as argument where necessary."
        },
        {
          "name": "...",
          "required": true,
          "default": null,
          "description": "Ensures that all arguments (starting from the \"...\") are to be named and that a warning will be displayed if unknown arguments are passed."
        },
        {
          "name": "effectList",
          "required": false,
          "default": "NULL",
          "description": "List of subsets, prevalences, and effect sizes with columns and number of rows reflecting the different situations to consider (see examples)."
        },
        {
          "name": "intersectionTest",
          "required": false,
          "default": "c(\"Simes\", \"SpiessensDebois\", \"Bonferroni\", \"Sidak\")",
          "description": "Defines the multiple test for the intersection hypotheses in the closed system of hypotheses. Four options are available in enrichment designs: \"SpiessensDebois\", \"Bonferroni\", \"Simes\", and \"Sidak\", default is \"Simes\"."
        },
        {
          "name": "stratifiedAnalysis",
          "required": false,
          "default": "TRUE",
          "description": "Logical. For enrichment designs, typically a stratified analysis should be chosen. For testing rates, also a non-stratified analysis based on overall data can be performed. For survival data, only a stratified analysis is possible (see Brannath et al., 2009), default is TRUE."
        },
        {
          "name": "directionUpper",
          "required": false,
          "default": "NA",
          "description": "Logical. Specifies the direction of the alternative, only applicable for one-sided testing; default is TRUE which means that larger values of the test statistics yield smaller p-values."
        },
        {
          "name": "adaptations",
          "required": false,
          "default": "NA",
          "description": "A logical vector of length kMax - 1 indicating whether or not an adaptation takes place at interim k, default is rep(TRUE, kMax - 1)."
        },
        {
          "name": "typeOfSelection",
          "required": false,
          "default": "c(\"best\", \"rBest\", \"epsilon\", \"all\", \"userDefined\")",
          "description": "The way the treatment arms or populations are selected at interim. Five options are available: \"best\", \"rbest\", \"epsilon\", \"all\", and \"userDefined\", default is \"best\". For \"rbest\" (select the rValue best treatment arms/populations), the parameter rValue has to be specified, for \"epsilon\" (select treatment arm/population not worse than epsilon compared to the best), the parameter epsilonValue has to be specified. If \"userDefined\" is selected, \"selectArmsFunction\" or \"selectPopulationsFunction\" has to be specified."
        },
        {
          "name": "effectMeasure",
          "required": false,
          "default": "c(\"effectEstimate\", \"testStatistic\")",
          "description": "Criterion for treatment arm/population selection, either based on test statistic (\"testStatistic\") or effect estimate (difference for means and rates or ratio for survival) (\"effectEstimate\"), default is \"effectEstimate\"."
        },
        {
          "name": "successCriterion",
          "required": false,
          "default": "c(\"all\", \"atLeastOne\")",
          "description": "Defines when the study is stopped for efficacy at interim. Two options are available: \"all\" stops the trial if the efficacy criterion is fulfilled for all selected treatment arms/populations, \"atLeastOne\" stops if at least one of the selected treatment arms/populations is shown to be superior to control at interim, default is \"all\"."
        },
        {
          "name": "epsilonValue",
          "required": false,
          "default": "NA_real_",
          "description": "For typeOfSelection = \"epsilon\" (select treatment arm / population not worse than epsilon compared to the best), the parameter epsilonValue has to be specified. Must be a numeric of length 1."
        },
        {
          "name": "rValue",
          "required": false,
          "default": "NA_real_",
          "description": "For typeOfSelection = \"rbest\" (select the rValue best treatment arms / populations), the parameter rValue has to be specified."
        },
        {
          "name": "threshold",
          "required": false,
          "default": "-Inf",
          "description": "Selection criterion: treatment arm / population is selected only if effectMeasure exceeds threshold, default is -Inf. threshold can also be a vector of length activeArms referring to a separate threshold condition over the treatment arms."
        },
        {
          "name": "plannedEvents",
          "required": false,
          "default": "NA_real_",
          "description": "plannedEvents is a numeric vector of length kMax (the number of stages of the design) that determines the number of cumulated (overall) events in survival designs when the interim stages are planned. For two treatment arms, it is the number of events for both treatment arms. For multi-arm designs, plannedEvents refers to the overall number of events for the selected arms plus control."
        },
        {
          "name": "allocationRatioPlanned",
          "required": false,
          "default": "NA_real_",
          "description": "The planned allocation ratio n1 / n2 for a two treatment groups design, default is 1. For multi-arm designs, it is the allocation ratio relating the active arm(s) to the control. For simulating means and rates for a two treatment groups design, it can be a vector of length kMax, the number of stages. It can be a vector of length kMax, too, for multi-arm and enrichment designs. In these cases, a change of allocating subjects to treatment groups over the stages can be assessed. Note that internally allocationRatioPlanned is treated as a vector of length kMax, not a scalar."
        },
        {
          "name": "minNumberOfEventsPerStage",
          "required": false,
          "default": "NA_real_",
          "description": "When performing a data driven sample size recalculation, the numeric vector minNumberOfEventsPerStage with length kMax determines the minimum number of events per stage (i.e., not cumulated), the first element is not taken into account."
        },
        {
          "name": "maxNumberOfEventsPerStage",
          "required": false,
          "default": "NA_real_",
          "description": "When performing a data driven sample size recalculation, the numeric vector maxNumberOfEventsPerStage with length kMax determines the maximum number of events per stage (i.e., not cumulated), the first element is not taken into account."
        },
        {
          "name": "conditionalPower",
          "required": false,
          "default": "NA_real_",
          "description": "If conditionalPower together with minNumberOfSubjectsPerStage and maxNumberOfSubjectsPerStage (or minNumberOfEventsPerStage and maxNumberOfEventsPerStage for survival designs) is specified, a sample size recalculation based on the specified conditional power is performed. It is defined as the power for the subsequent stage given the current data. By default, the conditional power will be calculated under the observed effect size. Optionally, you can also specify thetaH1 and stDevH1 (for simulating means), pi1H1 and pi2H1 (for simulating rates), or thetaH1 (for simulating hazard ratios) as parameters under which it is calculated and the sample size recalculation is performed."
        },
        {
          "name": "thetaH1",
          "required": false,
          "default": "NA_real_",
          "description": "If specified, the value of the alternative under which the conditional power or sample size recalculation calculation is performed. Must be a numeric of length 1."
        },
        {
          "name": "maxNumberOfIterations",
          "required": false,
          "default": "1000L",
          "description": "The number of simulation iterations, default is 1000. Must be a positive integer of length 1."
        },
        {
          "name": "seed",
          "required": false,
          "default": "NA_real_",
          "description": "The seed to reproduce the simulation, default is a random seed."
        },
        {
          "name": "calcEventsFunction",
          "required": false,
          "default": "NULL",
          "description": "Optionally, a function can be entered that defines the way of performing the sample size recalculation. By default, event number recalculation is performed with conditional power and specified minNumberOfEventsPerStage and maxNumberOfEventsPerStage (see details and examples)."
        },
        {
          "name": "selectPopulationsFunction",
          "required": false,
          "default": "NULL",
          "description": "Optionally, a function can be entered that defines the way of how populations are selected. This function is allowed to depend on effectVector with length populations stage, conditionalPower, conditionalCriticalValue, plannedSubjects/plannedEvents, allocationRatioPlanned, selectedPopulations, thetaH1 (for means and survival), stDevH1 (for means), overallEffects, and for rates additionally: piTreatmentsH1, piControlH1, overallRates, and overallRatesControl (see examples)."
        },
        {
          "name": "showStatistics",
          "required": false,
          "default": "FALSE",
          "description": "Logical. If TRUE, summary statistics of the simulated data are displayed for the print command, otherwise the output is suppressed, default is FALSE."
        }
      ]
    },
    {
      "name": "getSimulationMeans",
      "family": "simulation",
      "title": "Get Simulation Means",
      "description": "Returns the simulated power, stopping probabilities, conditional power, and expected sample size for testing means in a one or two treatment groups testing situation.",
      "args": [
        {
          "name": "design",
          "required": false,
          "default": "NULL",
          "description": "The trial design. If no trial design is specified, a fixed sample size design is used. In this case, Type I error rate alpha, Type II error rate beta, twoSidedPower, and sided can be directly entered as argument where necessary."
        },
        {
          "name": "...",
          "required": true,
          "default": null,
          "description": "Ensures that all arguments (starting from the \"...\") are to be named and that a warning will be displayed if unknown arguments are passed."
        },
        {
          "name": "groups",
          "required": false,
          "default": "2L",
          "description": "The number of treatment groups (1 or 2), default is 2."
        },
        {
          "name": "normalApproximation",
          "required": false,
          "default": "TRUE",
          "description": "The type of computation of the p-values. Default is TRUE, i.e., normally distributed test statistics are generated. If FALSE, the t test is used for calculating the p-values, i.e., t distributed test statistics are generated."
        },
        {
          "name": "meanRatio",
          "required": false,
          "default": "FALSE",
          "description": "If TRUE, the design characteristics for one-sided testing of H0: mu1 / mu2 = thetaH0 are simulated, default is FALSE."
        },
        {
          "name": "thetaH0",
          "required": false,
          "default": "ifelse(meanRatio, 1, 0)",
          "description": "The null hypothesis value, default is 0 for the normal and the binary case (testing means and rates, respectively), it is 1 for the survival case (testing the hazard ratio). For non-inferiority designs, thetaH0 is the non-inferiority bound. That is, in case of (one-sided) testing of means: a value != 0 (or a value != 1 for testing the mean ratio) can be specified. rates: a value != 0 (or a value != 1 for testing the risk ratio pi1 / pi2) can be specified. survival data: a bound for testing H0: hazard ratio = thetaH0 != 1 can be specified. count data: a bound for testing H0: lambda1 / lambda2 = thetaH0 != 1 can be specified. For testing a rate in one sample, a value thetaH0 in (0, 1) has to be specified for defining the null hypothesis H0: pi = thetaH0."
        },
        {
          "name": "alternative",
          "required": false,
          "default": "seq(0, 1, 0.2)",
          "description": "The alternative hypothesis value for testing means under which the data is simulated. This can be a vector of assumed alternatives, default is seq(0, 1, 0.2)."
        },
        {
          "name": "stDev",
          "required": false,
          "default": "1",
          "description": "The standard deviation under which the data is simulated, default is 1. For two-armed trials, it is allowed to specify the standard deviations separately, i.e., as vector with two elements. If meanRatio = TRUE is specified, stDev defines the coefficient of variation sigma / mu2."
        },
        {
          "name": "plannedSubjects",
          "required": false,
          "default": "NA_real_",
          "description": "plannedSubjects is a numeric vector of length kMax (the number of stages of the design) that determines the number of cumulated (overall) subjects when the interim stages are planned. For two treatment arms, it is the number of subjects for both treatment arms. For multi-arm designs, plannedSubjects refers to the number of subjects per selected active arm."
        },
        {
          "name": "directionUpper",
          "required": false,
          "default": "NA",
          "description": "Logical. Specifies the direction of the alternative, only applicable for one-sided testing; default is TRUE which means that larger values of the test statistics yield smaller p-values."
        },
        {
          "name": "allocationRatioPlanned",
          "required": false,
          "default": "NA_real_",
          "description": "The planned allocation ratio n1 / n2 for a two treatment groups design, default is 1. For multi-arm designs, it is the allocation ratio relating the active arm(s) to the control. For simulating means and rates for a two treatment groups design, it can be a vector of length kMax, the number of stages. It can be a vector of length kMax, too, for multi-arm and enrichment designs. In these cases, a change of allocating subjects to treatment groups over the stages can be assessed. Note that internally allocationRatioPlanned is treated as a vector of length kMax, not a scalar."
        },
        {
          "name": "minNumberOfSubjectsPerStage",
          "required": false,
          "default": "NA_real_",
          "description": "When performing a data driven sample size recalculation, the numeric vector minNumberOfSubjectsPerStage with length kMax determines the minimum number of subjects per stage (i.e., not cumulated), the first element is not taken into account. For two treatment arms, it is the number of subjects for both treatment arms. For multi-arm designs minNumberOfSubjectsPerStage refers to the minimum number of subjects per selected active arm."
        },
        {
          "name": "maxNumberOfSubjectsPerStage",
          "required": false,
          "default": "NA_real_",
          "description": "When performing a data driven sample size recalculation, the numeric vector maxNumberOfSubjectsPerStage with length kMax determines the maximum number of subjects per stage (i.e., not cumulated), the first element is not taken into account. For two treatment arms, it is the number of subjects for both treatment arms. For multi-arm designs maxNumberOfSubjectsPerStage refers to the maximum number of subjects per selected active arm."
        },
        {
          "name": "conditionalPower",
          "required": false,
          "default": "NA_real_",
          "description": "If conditionalPower together with minNumberOfSubjectsPerStage and maxNumberOfSubjectsPerStage (or minNumberOfEventsPerStage and maxNumberOfEventsPerStage for survival designs) is specified, a sample size recalculation based on the specified conditional power is performed. It is defined as the power for the subsequent stage given the current data. By default, the conditional power will be calculated under the observed effect size. Optionally, you can also specify thetaH1 and stDevH1 (for simulating means), pi1H1 and pi2H1 (for simulating rates), or thetaH1 (for simulating hazard ratios) as parameters under which it is calculated and the sample size recalculation is performed."
        },
        {
          "name": "thetaH1",
          "required": false,
          "default": "NA_real_",
          "description": "If specified, the value of the alternative under which the conditional power or sample size recalculation calculation is performed. Must be a numeric of length 1."
        },
        {
          "name": "stDevH1",
          "required": false,
          "default": "NA_real_",
          "description": "If specified, the value of the standard deviation under which the conditional power or sample size recalculation calculation is performed, default is the value of stDev."
        },
        {
          "name": "maxNumberOfIterations",
          "required": false,
          "default": "1000L",
          "description": "The number of simulation iterations, default is 1000. Must be a positive integer of length 1."
        },
        {
          "name": "seed",
          "required": false,
          "default": "NA_real_",
          "description": "The seed to reproduce the simulation, default is a random seed."
        },
        {
          "name": "calcSubjectsFunction",
          "required": false,
          "default": "NULL",
          "description": "Optionally, a function can be entered that defines the way of performing the sample size recalculation. By default, sample size recalculation is performed with conditional power and specified minNumberOfSubjectsPerStage and maxNumberOfSubjectsPerStage (see details and examples)."
        },
        {
          "name": "showStatistics",
          "required": false,
          "default": "FALSE",
          "description": "Logical. If TRUE, summary statistics of the simulated data are displayed for the print command, otherwise the output is suppressed, default is FALSE."
        }
      ]
    },
    {
      "name": "getSimulationMultiArmMeans",
      "family": "simulation",
      "title": "Get Simulation Multi-Arm Means",
      "description": "Returns the simulated power, stopping and selection probabilities, conditional power, and expected sample size for testing means in a multi-arm treatment groups testing situation.",
      "args": [
        {
          "name": "design",
          "required": false,
          "default": "NULL",
          "description": "The trial design. If no trial design is specified, a fixed sample size design is used. In this case, Type I error rate alpha, Type II error rate beta, twoSidedPower, and sided can be directly entered as argument where necessary."
        },
        {
          "name": "...",
          "required": true,
          "default": null,
          "description": "Ensures that all arguments (starting from the \"...\") are to be named and that a warning will be displayed if unknown arguments are passed."
        },
        {
          "name": "activeArms",
          "required": false,
          "default": "NA_integer_",
          "description": "The number of active treatment arms to be compared with control, default is 3."
        },
        {
          "name": "effectMatrix",
          "required": false,
          "default": "NULL",
          "description": "Matrix of effect sizes with activeArms columns and number of rows reflecting the different situations to consider."
        },
        {
          "name": "typeOfShape",
          "required": false,
          "default": "c(\"linear\", \"sigmoidEmax\", \"userDefined\")",
          "description": "The shape of the dose-response relationship over the treatment groups. This can be either \"linear\", \"sigmoidEmax\", or \"userDefined\", default is \"linear\". For \"linear\", muMaxVector specifies the range of effect sizes for the treatment group with highest response. If \"sigmoidEmax\" is selected, gED50 and slope has to be entered to specify the ED50 and the slope of the sigmoid Emax model. For \"sigmoidEmax\", muMaxVector specifies the range of effect sizes for the treatment group with response according to infinite dose. If \"userDefined\" is selected, effectMatrix has to be entered."
        },
        {
          "name": "muMaxVector",
          "required": false,
          "default": "seq(0, 1, 0.2)",
          "description": "Range of effect sizes for the treatment group with highest response for \"linear\" and \"sigmoidEmax\" model, default is seq(0, 1, 0.2)."
        },
        {
          "name": "gED50",
          "required": false,
          "default": "NA_real_",
          "description": "If typeOfShape = \"sigmoidEmax\" is selected, gED50 has to be entered to specify the ED50 of the sigmoid Emax model."
        },
        {
          "name": "slope",
          "required": false,
          "default": "1",
          "description": "If typeOfShape = \"sigmoidEmax\" is selected, slope can be entered to specify the slope of the sigmoid Emax model, default is 1."
        },
        {
          "name": "doseLevels",
          "required": false,
          "default": "NA_real_",
          "description": "The dose levels for the dose response relationship. If not specified, these dose levels are 1,...,activeArms."
        },
        {
          "name": "intersectionTest",
          "required": false,
          "default": "c(\"Dunnett\", \"Bonferroni\", \"Simes\", \"Sidak\", \"Hierarchical\")",
          "description": "Defines the multiple test for the intersection hypotheses in the closed system of hypotheses. Five options are available in multi-arm designs: \"Dunnett\", \"Bonferroni\", \"Simes\", \"Sidak\", and \"Hierarchical\", default is \"Dunnett\"."
        },
        {
          "name": "stDev",
          "required": false,
          "default": "1",
          "description": "The standard deviation under which the data is simulated, default is 1. For two-armed trials, it is allowed to specify the standard deviations separately, i.e., as vector with two elements. If meanRatio = TRUE is specified, stDev defines the coefficient of variation sigma / mu2."
        },
        {
          "name": "adaptations",
          "required": false,
          "default": "NA",
          "description": "A logical vector of length kMax - 1 indicating whether or not an adaptation takes place at interim k, default is rep(TRUE, kMax - 1)."
        },
        {
          "name": "typeOfSelection",
          "required": false,
          "default": "c(\"best\", \"rBest\", \"epsilon\", \"all\", \"userDefined\")",
          "description": "The way the treatment arms or populations are selected at interim. Five options are available: \"best\", \"rbest\", \"epsilon\", \"all\", and \"userDefined\", default is \"best\". For \"rbest\" (select the rValue best treatment arms/populations), the parameter rValue has to be specified, for \"epsilon\" (select treatment arm/population not worse than epsilon compared to the best), the parameter epsilonValue has to be specified. If \"userDefined\" is selected, \"selectArmsFunction\" or \"selectPopulationsFunction\" has to be specified."
        },
        {
          "name": "effectMeasure",
          "required": false,
          "default": "c(\"effectEstimate\", \"testStatistic\")",
          "description": "Criterion for treatment arm/population selection, either based on test statistic (\"testStatistic\") or effect estimate (difference for means and rates or ratio for survival) (\"effectEstimate\"), default is \"effectEstimate\"."
        },
        {
          "name": "successCriterion",
          "required": false,
          "default": "c(\"all\", \"atLeastOne\")",
          "description": "Defines when the study is stopped for efficacy at interim. Two options are available: \"all\" stops the trial if the efficacy criterion is fulfilled for all selected treatment arms/populations, \"atLeastOne\" stops if at least one of the selected treatment arms/populations is shown to be superior to control at interim, default is \"all\"."
        },
        {
          "name": "epsilonValue",
          "required": false,
          "default": "NA_real_",
          "description": "For typeOfSelection = \"epsilon\" (select treatment arm / population not worse than epsilon compared to the best), the parameter epsilonValue has to be specified. Must be a numeric of length 1."
        },
        {
          "name": "rValue",
          "required": false,
          "default": "NA_real_",
          "description": "For typeOfSelection = \"rbest\" (select the rValue best treatment arms / populations), the parameter rValue has to be specified."
        },
        {
          "name": "threshold",
          "required": false,
          "default": "-Inf",
          "description": "Selection criterion: treatment arm / population is selected only if effectMeasure exceeds threshold, default is -Inf. threshold can also be a vector of length activeArms referring to a separate threshold condition over the treatment arms."
        },
        {
          "name": "plannedSubjects",
          "required": false,
          "default": "NA_integer_",
          "description": "plannedSubjects is a numeric vector of length kMax (the number of stages of the design) that determines the number of cumulated (overall) subjects when the interim stages are planned. For two treatment arms, it is the number of subjects for both treatment arms. For multi-arm designs, plannedSubjects refers to the number of subjects per selected active arm."
        },
        {
          "name": "allocationRatioPlanned",
          "required": false,
          "default": "NA_real_",
          "description": "The planned allocation ratio n1 / n2 for a two treatment groups design, default is 1. For multi-arm designs, it is the allocation ratio relating the active arm(s) to the control. For simulating means and rates for a two treatment groups design, it can be a vector of length kMax, the number of stages. It can be a vector of length kMax, too, for multi-arm and enrichment designs. In these cases, a change of allocating subjects to treatment groups over the stages can be assessed. Note that internally allocationRatioPlanned is treated as a vector of length kMax, not a scalar."
        },
        {
          "name": "minNumberOfSubjectsPerStage",
          "required": false,
          "default": "NA_real_",
          "description": "When performing a data driven sample size recalculation, the numeric vector minNumberOfSubjectsPerStage with length kMax determines the minimum number of subjects per stage (i.e., not cumulated), the first element is not taken into account. For two treatment arms, it is the number of subjects for both treatment arms. For multi-arm designs minNumberOfSubjectsPerStage refers to the minimum number of subjects per selected active arm."
        },
        {
          "name": "maxNumberOfSubjectsPerStage",
          "required": false,
          "default": "NA_real_",
          "description": "When performing a data driven sample size recalculation, the numeric vector maxNumberOfSubjectsPerStage with length kMax determines the maximum number of subjects per stage (i.e., not cumulated), the first element is not taken into account. For two treatment arms, it is the number of subjects for both treatment arms. For multi-arm designs maxNumberOfSubjectsPerStage refers to the maximum number of subjects per selected active arm."
        },
        {
          "name": "conditionalPower",
          "required": false,
          "default": "NA_real_",
          "description": "If conditionalPower together with minNumberOfSubjectsPerStage and maxNumberOfSubjectsPerStage (or minNumberOfEventsPerStage and maxNumberOfEventsPerStage for survival designs) is specified, a sample size recalculation based on the specified conditional power is performed. It is defined as the power for the subsequent stage given the current data. By default, the conditional power will be calculated under the observed effect size. Optionally, you can also specify thetaH1 and stDevH1 (for simulating means), pi1H1 and pi2H1 (for simulating rates), or thetaH1 (for simulating hazard ratios) as parameters under which it is calculated and the sample size recalculation is performed."
        },
        {
          "name": "thetaH1",
          "required": false,
          "default": "NA_real_",
          "description": "If specified, the value of the alternative under which the conditional power or sample size recalculation calculation is performed. Must be a numeric of length 1."
        },
        {
          "name": "stDevH1",
          "required": false,
          "default": "NA_real_",
          "description": "If specified, the value of the standard deviation under which the conditional power or sample size recalculation calculation is performed, default is the value of stDev."
        },
        {
          "name": "maxNumberOfIterations",
          "required": false,
          "default": "1000L",
          "description": "The number of simulation iterations, default is 1000. Must be a positive integer of length 1."
        },
        {
          "name": "seed",
          "required": false,
          "default": "NA_real_",
          "description": "The seed to reproduce the simulation, default is a random seed."
        },
        {
          "name": "calcSubjectsFunction",
          "required": false,
          "default": "NULL",
          "description": "Optionally, a function can be entered that defines the way of performing the sample size recalculation. By default, sample size recalculation is performed with conditional power and specified minNumberOfSubjectsPerStage and maxNumberOfSubjectsPerStage (see details and examples)."
        },
        {
          "name": "selectArmsFunction",
          "required": false,
          "default": "NULL",
          "description": "Optionally, a function can be entered that defines the way of how treatment arms are selected. This function is allowed to depend on effectVector with length activeArms, stage, conditionalPower, conditionalCriticalValue, plannedSubjects/plannedEvents, allocationRatioPlanned, selectedArms, thetaH1 (for means and survival), stDevH1 (for means), overallEffects, and for rates additionally: piTreatmentsH1, piControlH1, overallRates, and overallRatesControl (see examples)."
        },
        {
          "name": "showStatistics",
          "required": false,
          "default": "FALSE",
          "description": "Logical. If TRUE, summary statistics of the simulated data are displayed for the print command, otherwise the output is suppressed, default is FALSE."
        }
      ]
    },
    {
      "name": "getSimulationMultiArmRates",
      "family": "simulation",
      "title": "Get Simulation Multi-Arm Rates",
      "description": "Returns the simulated power, stopping and selection probabilities, conditional power, and expected sample size for testing rates in a multi-arm treatment groups testing situation.",
      "args": [
        {
          "name": "design",
          "required": false,
          "default": "NULL",
          "description": "The trial design. If no trial design is specified, a fixed sample size design is used. In this case, Type I error rate alpha, Type II error rate beta, twoSidedPower, and sided can be directly entered as argument where necessary."
        },
        {
          "name": "...",
          "required": true,
          "default": null,
          "description": "Ensures that all arguments (starting from the \"...\") are to be named and that a warning will be displayed if unknown arguments are passed."
        },
        {
          "name": "activeArms",
          "required": false,
          "default": "NA_integer_",
          "description": "The number of active treatment arms to be compared with control, default is 3."
        },
        {
          "name": "effectMatrix",
          "required": false,
          "default": "NULL",
          "description": "Matrix of effect sizes with activeArms columns and number of rows reflecting the different situations to consider."
        },
        {
          "name": "typeOfShape",
          "required": false,
          "default": "c(\"linear\", \"sigmoidEmax\", \"userDefined\")",
          "description": "The shape of the dose-response relationship over the treatment groups. This can be either \"linear\", \"sigmoidEmax\", or \"userDefined\", default is \"linear\". For \"linear\", piMaxVector specifies the range of effect sizes for the treatment group with highest response. If \"sigmoidEmax\" is selected, gED50 and slope has to be entered to specify the ED50 and the slope of the sigmoid Emax model. For \"sigmoidEmax\", piMaxVector specifies the range of effect sizes for the treatment group with response according to infinite dose. If \"userDefined\" is selected, effectMatrix has to be entered."
        },
        {
          "name": "piMaxVector",
          "required": false,
          "default": "seq(0.2, 0.5, 0.1)",
          "description": "Range of assumed probabilities for the treatment group with highest response for \"linear\" and \"sigmoidEmax\" model, default is seq(0, 1, 0.2)."
        },
        {
          "name": "piControl",
          "required": false,
          "default": "0.2",
          "description": "If specified, the assumed probability in the control arm for simulation and under which the sample size recalculation is performed."
        },
        {
          "name": "gED50",
          "required": false,
          "default": "NA_real_",
          "description": "If typeOfShape = \"sigmoidEmax\" is selected, gED50 has to be entered to specify the ED50 of the sigmoid Emax model."
        },
        {
          "name": "slope",
          "required": false,
          "default": "1",
          "description": "If typeOfShape = \"sigmoidEmax\" is selected, slope can be entered to specify the slope of the sigmoid Emax model, default is 1."
        },
        {
          "name": "doseLevels",
          "required": false,
          "default": "NA_real_",
          "description": "The dose levels for the dose response relationship. If not specified, these dose levels are 1,...,activeArms."
        },
        {
          "name": "intersectionTest",
          "required": false,
          "default": "c(\"Dunnett\", \"Bonferroni\", \"Simes\", \"Sidak\", \"Hierarchical\")",
          "description": "Defines the multiple test for the intersection hypotheses in the closed system of hypotheses. Five options are available in multi-arm designs: \"Dunnett\", \"Bonferroni\", \"Simes\", \"Sidak\", and \"Hierarchical\", default is \"Dunnett\"."
        },
        {
          "name": "directionUpper",
          "required": false,
          "default": "NA",
          "description": "Logical. Specifies the direction of the alternative, only applicable for one-sided testing; default is TRUE which means that larger values of the test statistics yield smaller p-values."
        },
        {
          "name": "adaptations",
          "required": false,
          "default": "NA",
          "description": "A logical vector of length kMax - 1 indicating whether or not an adaptation takes place at interim k, default is rep(TRUE, kMax - 1)."
        },
        {
          "name": "typeOfSelection",
          "required": false,
          "default": "c(\"best\", \"rBest\", \"epsilon\", \"all\", \"userDefined\")",
          "description": "The way the treatment arms or populations are selected at interim. Five options are available: \"best\", \"rbest\", \"epsilon\", \"all\", and \"userDefined\", default is \"best\". For \"rbest\" (select the rValue best treatment arms/populations), the parameter rValue has to be specified, for \"epsilon\" (select treatment arm/population not worse than epsilon compared to the best), the parameter epsilonValue has to be specified. If \"userDefined\" is selected, \"selectArmsFunction\" or \"selectPopulationsFunction\" has to be specified."
        },
        {
          "name": "effectMeasure",
          "required": false,
          "default": "c(\"effectEstimate\", \"testStatistic\")",
          "description": "Criterion for treatment arm/population selection, either based on test statistic (\"testStatistic\") or effect estimate (difference for means and rates or ratio for survival) (\"effectEstimate\"), default is \"effectEstimate\"."
        },
        {
          "name": "successCriterion",
          "required": false,
          "default": "c(\"all\", \"atLeastOne\")",
          "description": "Defines when the study is stopped for efficacy at interim. Two options are available: \"all\" stops the trial if the efficacy criterion is fulfilled for all selected treatment arms/populations, \"atLeastOne\" stops if at least one of the selected treatment arms/populations is shown to be superior to control at interim, default is \"all\"."
        },
        {
          "name": "epsilonValue",
          "required": false,
          "default": "NA_real_",
          "description": "For typeOfSelection = \"epsilon\" (select treatment arm / population not worse than epsilon compared to the best), the parameter epsilonValue has to be specified. Must be a numeric of length 1."
        },
        {
          "name": "rValue",
          "required": false,
          "default": "NA_real_",
          "description": "For typeOfSelection = \"rbest\" (select the rValue best treatment arms / populations), the parameter rValue has to be specified."
        },
        {
          "name": "threshold",
          "required": false,
          "default": "-Inf",
          "description": "Selection criterion: treatment arm / population is selected only if effectMeasure exceeds threshold, default is -Inf. threshold can also be a vector of length activeArms referring to a separate threshold condition over the treatment arms."
        },
        {
          "name": "plannedSubjects",
          "required": false,
          "default": "NA_real_",
          "description": "plannedSubjects is a numeric vector of length kMax (the number of stages of the design) that determines the number of cumulated (overall) subjects when the interim stages are planned. For two treatment arms, it is the number of subjects for both treatment arms. For multi-arm designs, plannedSubjects refers to the number of subjects per selected active arm."
        },
        {
          "name": "allocationRatioPlanned",
          "required": false,
          "default": "NA_real_",
          "description": "The planned allocation ratio n1 / n2 for a two treatment groups design, default is 1. For multi-arm designs, it is the allocation ratio relating the active arm(s) to the control. For simulating means and rates for a two treatment groups design, it can be a vector of length kMax, the number of stages. It can be a vector of length kMax, too, for multi-arm and enrichment designs. In these cases, a change of allocating subjects to treatment groups over the stages can be assessed. Note that internally allocationRatioPlanned is treated as a vector of length kMax, not a scalar."
        },
        {
          "name": "minNumberOfSubjectsPerStage",
          "required": false,
          "default": "NA_real_",
          "description": "When performing a data driven sample size recalculation, the numeric vector minNumberOfSubjectsPerStage with length kMax determines the minimum number of subjects per stage (i.e., not cumulated), the first element is not taken into account. For two treatment arms, it is the number of subjects for both treatment arms. For multi-arm designs minNumberOfSubjectsPerStage refers to the minimum number of subjects per selected active arm."
        },
        {
          "name": "maxNumberOfSubjectsPerStage",
          "required": false,
          "default": "NA_real_",
          "description": "When performing a data driven sample size recalculation, the numeric vector maxNumberOfSubjectsPerStage with length kMax determines the maximum number of subjects per stage (i.e., not cumulated), the first element is not taken into account. For two treatment arms, it is the number of subjects for both treatment arms. For multi-arm designs maxNumberOfSubjectsPerStage refers to the maximum number of subjects per selected active arm."
        },
        {
          "name": "conditionalPower",
          "required": false,
          "default": "NA_real_",
          "description": "If conditionalPower together with minNumberOfSubjectsPerStage and maxNumberOfSubjectsPerStage (or minNumberOfEventsPerStage and maxNumberOfEventsPerStage for survival designs) is specified, a sample size recalculation based on the specified conditional power is performed. It is defined as the power for the subsequent stage given the current data. By default, the conditional power will be calculated under the observed effect size. Optionally, you can also specify thetaH1 and stDevH1 (for simulating means), pi1H1 and pi2H1 (for simulating rates), or thetaH1 (for simulating hazard ratios) as parameters under which it is calculated and the sample size recalculation is performed."
        },
        {
          "name": "piTreatmentsH1",
          "required": false,
          "default": "NA_real_",
          "description": "If specified, the assumed probability in the active treatment arm(s) under which the sample size recalculation is performed."
        },
        {
          "name": "piControlH1",
          "required": false,
          "default": "NA_real_",
          "description": "If specified, the assumed probability in the reference group (if different from piControl) for which the conditional power was calculated."
        },
        {
          "name": "maxNumberOfIterations",
          "required": false,
          "default": "1000L",
          "description": "The number of simulation iterations, default is 1000. Must be a positive integer of length 1."
        },
        {
          "name": "seed",
          "required": false,
          "default": "NA_real_",
          "description": "The seed to reproduce the simulation, default is a random seed."
        },
        {
          "name": "calcSubjectsFunction",
          "required": false,
          "default": "NULL",
          "description": "Optionally, a function can be entered that defines the way of performing the sample size recalculation. By default, sample size recalculation is performed with conditional power and specified minNumberOfSubjectsPerStage and maxNumberOfSubjectsPerStage (see details and examples)."
        },
        {
          "name": "selectArmsFunction",
          "required": false,
          "default": "NULL",
          "description": "Optionally, a function can be entered that defines the way of how treatment arms are selected. This function is allowed to depend on effectVector with length activeArms, stage, conditionalPower, conditionalCriticalValue, plannedSubjects/plannedEvents, allocationRatioPlanned, selectedArms, thetaH1 (for means and survival), stDevH1 (for means), overallEffects, and for rates additionally: piTreatmentsH1, piControlH1, overallRates, and overallRatesControl (see examples)."
        },
        {
          "name": "showStatistics",
          "required": false,
          "default": "FALSE",
          "description": "Logical. If TRUE, summary statistics of the simulated data are displayed for the print command, otherwise the output is suppressed, default is FALSE."
        }
      ]
    },
    {
      "name": "getSimulationMultiArmSurvival",
      "family": "simulation",
      "title": "Get Simulation Multi-Arm Survival",
      "description": "Returns the simulated power, stopping and selection probabilities, conditional power, and expected sample size for testing hazard ratios in a multi-arm treatment groups testing situation. In contrast to getSimulationSurvival() (where survival times are simulated), normally distributed logrank test statistics are simulated.",
      "args": [
        {
          "name": "design",
          "required": false,
          "default": "NULL",
          "description": "The trial design. If no trial design is specified, a fixed sample size design is used. In this case, Type I error rate alpha, Type II error rate beta, twoSidedPower, and sided can be directly entered as argument where necessary."
        },
        {
          "name": "...",
          "required": true,
          "default": null,
          "description": "Ensures that all arguments (starting from the \"...\") are to be named and that a warning will be displayed if unknown arguments are passed."
        },
        {
          "name": "activeArms",
          "required": false,
          "default": "NA_integer_",
          "description": "The number of active treatment arms to be compared with control, default is 3."
        },
        {
          "name": "effectMatrix",
          "required": false,
          "default": "NULL",
          "description": "Matrix of effect sizes with activeArms columns and number of rows reflecting the different situations to consider."
        },
        {
          "name": "typeOfShape",
          "required": false,
          "default": "c(\"linear\", \"sigmoidEmax\", \"userDefined\")",
          "description": "The shape of the dose-response relationship over the treatment groups. This can be either \"linear\", \"sigmoidEmax\", or \"userDefined\", default is \"linear\". For \"linear\", omegaMaxVector specifies the range of effect sizes for the treatment group with highest response. If \"sigmoidEmax\" is selected, gED50 and slope has to be entered to specify the ED50 and the slope of the sigmoid Emax model. For \"sigmoidEmax\", omegaMaxVector specifies the range of effect sizes for the treatment group with response according to infinite dose. If \"userDefined\" is selected, effectMatrix has to be entered."
        },
        {
          "name": "omegaMaxVector",
          "required": false,
          "default": "seq(1, 2.6, 0.4)",
          "description": "Range of hazard ratios with highest response for \"linear\" and \"sigmoidEmax\" model, default is seq(1, 2.6, 0.4)."
        },
        {
          "name": "gED50",
          "required": false,
          "default": "NA_real_",
          "description": "If typeOfShape = \"sigmoidEmax\" is selected, gED50 has to be entered to specify the ED50 of the sigmoid Emax model."
        },
        {
          "name": "slope",
          "required": false,
          "default": "1",
          "description": "If typeOfShape = \"sigmoidEmax\" is selected, slope can be entered to specify the slope of the sigmoid Emax model, default is 1."
        },
        {
          "name": "doseLevels",
          "required": false,
          "default": "NA_real_",
          "description": "The dose levels for the dose response relationship. If not specified, these dose levels are 1,...,activeArms."
        },
        {
          "name": "intersectionTest",
          "required": false,
          "default": "c(\"Dunnett\", \"Bonferroni\", \"Simes\", \"Sidak\", \"Hierarchical\")",
          "description": "Defines the multiple test for the intersection hypotheses in the closed system of hypotheses. Five options are available in multi-arm designs: \"Dunnett\", \"Bonferroni\", \"Simes\", \"Sidak\", and \"Hierarchical\", default is \"Dunnett\"."
        },
        {
          "name": "directionUpper",
          "required": false,
          "default": "NA",
          "description": "Logical. Specifies the direction of the alternative, only applicable for one-sided testing; default is TRUE which means that larger values of the test statistics yield smaller p-values."
        },
        {
          "name": "adaptations",
          "required": false,
          "default": "NA",
          "description": "A logical vector of length kMax - 1 indicating whether or not an adaptation takes place at interim k, default is rep(TRUE, kMax - 1)."
        },
        {
          "name": "typeOfSelection",
          "required": false,
          "default": "c(\"best\", \"rBest\", \"epsilon\", \"all\", \"userDefined\")",
          "description": "The way the treatment arms or populations are selected at interim. Five options are available: \"best\", \"rbest\", \"epsilon\", \"all\", and \"userDefined\", default is \"best\". For \"rbest\" (select the rValue best treatment arms/populations), the parameter rValue has to be specified, for \"epsilon\" (select treatment arm/population not worse than epsilon compared to the best), the parameter epsilonValue has to be specified. If \"userDefined\" is selected, \"selectArmsFunction\" or \"selectPopulationsFunction\" has to be specified."
        },
        {
          "name": "effectMeasure",
          "required": false,
          "default": "c(\"effectEstimate\", \"testStatistic\")",
          "description": "Criterion for treatment arm/population selection, either based on test statistic (\"testStatistic\") or effect estimate (difference for means and rates or ratio for survival) (\"effectEstimate\"), default is \"effectEstimate\"."
        },
        {
          "name": "successCriterion",
          "required": false,
          "default": "c(\"all\", \"atLeastOne\")",
          "description": "Defines when the study is stopped for efficacy at interim. Two options are available: \"all\" stops the trial if the efficacy criterion is fulfilled for all selected treatment arms/populations, \"atLeastOne\" stops if at least one of the selected treatment arms/populations is shown to be superior to control at interim, default is \"all\"."
        },
        {
          "name": "correlationComputation",
          "required": false,
          "default": "c(\"alternative\", \"null\")",
          "description": "If correlationComputation = \"alternative\", for simulating log-rank statistics in the many-to-one design, a correlation matrix according to Deng et al. (Biometrics, 2019) accounting for the respective alternative is used; if correlationComputation = \"null\", a constant correlation matrix valid under the null, i.e., not accounting for the alternative is used, default is \"alternative\"."
        },
        {
          "name": "epsilonValue",
          "required": false,
          "default": "NA_real_",
          "description": "For typeOfSelection = \"epsilon\" (select treatment arm / population not worse than epsilon compared to the best), the parameter epsilonValue has to be specified. Must be a numeric of length 1."
        },
        {
          "name": "rValue",
          "required": false,
          "default": "NA_real_",
          "description": "For typeOfSelection = \"rbest\" (select the rValue best treatment arms / populations), the parameter rValue has to be specified."
        },
        {
          "name": "threshold",
          "required": false,
          "default": "-Inf",
          "description": "Selection criterion: treatment arm / population is selected only if effectMeasure exceeds threshold, default is -Inf. threshold can also be a vector of length activeArms referring to a separate threshold condition over the treatment arms."
        },
        {
          "name": "plannedEvents",
          "required": false,
          "default": "NA_real_",
          "description": "plannedEvents is a numeric vector of length kMax (the number of stages of the design) that determines the number of cumulated (overall) events in survival designs when the interim stages are planned. For two treatment arms, it is the number of events for both treatment arms. For multi-arm designs, plannedEvents refers to the overall number of events for the selected arms plus control."
        },
        {
          "name": "allocationRatioPlanned",
          "required": false,
          "default": "NA_real_",
          "description": "The planned allocation ratio n1 / n2 for a two treatment groups design, default is 1. For multi-arm designs, it is the allocation ratio relating the active arm(s) to the control. For simulating means and rates for a two treatment groups design, it can be a vector of length kMax, the number of stages. It can be a vector of length kMax, too, for multi-arm and enrichment designs. In these cases, a change of allocating subjects to treatment groups over the stages can be assessed. Note that internally allocationRatioPlanned is treated as a vector of length kMax, not a scalar."
        },
        {
          "name": "minNumberOfEventsPerStage",
          "required": false,
          "default": "NA_real_",
          "description": "When performing a data driven sample size recalculation, the numeric vector minNumberOfEventsPerStage with length kMax determines the minimum number of events per stage (i.e., not cumulated), the first element is not taken into account."
        },
        {
          "name": "maxNumberOfEventsPerStage",
          "required": false,
          "default": "NA_real_",
          "description": "When performing a data driven sample size recalculation, the numeric vector maxNumberOfEventsPerStage with length kMax determines the maximum number of events per stage (i.e., not cumulated), the first element is not taken into account."
        },
        {
          "name": "conditionalPower",
          "required": false,
          "default": "NA_real_",
          "description": "If conditionalPower together with minNumberOfSubjectsPerStage and maxNumberOfSubjectsPerStage (or minNumberOfEventsPerStage and maxNumberOfEventsPerStage for survival designs) is specified, a sample size recalculation based on the specified conditional power is performed. It is defined as the power for the subsequent stage given the current data. By default, the conditional power will be calculated under the observed effect size. Optionally, you can also specify thetaH1 and stDevH1 (for simulating means), pi1H1 and pi2H1 (for simulating rates), or thetaH1 (for simulating hazard ratios) as parameters under which it is calculated and the sample size recalculation is performed."
        },
        {
          "name": "thetaH1",
          "required": false,
          "default": "NA_real_",
          "description": "If specified, the value of the alternative under which the conditional power or sample size recalculation calculation is performed. Must be a numeric of length 1."
        },
        {
          "name": "maxNumberOfIterations",
          "required": false,
          "default": "1000L",
          "description": "The number of simulation iterations, default is 1000. Must be a positive integer of length 1."
        },
        {
          "name": "seed",
          "required": false,
          "default": "NA_real_",
          "description": "The seed to reproduce the simulation, default is a random seed."
        },
        {
          "name": "calcEventsFunction",
          "required": false,
          "default": "NULL",
          "description": "Optionally, a function can be entered that defines the way of performing the sample size recalculation. By default, event number recalculation is performed with conditional power and specified minNumberOfEventsPerStage and maxNumberOfEventsPerStage (see details and examples)."
        },
        {
          "name": "selectArmsFunction",
          "required": false,
          "default": "NULL",
          "description": "Optionally, a function can be entered that defines the way of how treatment arms are selected. This function is allowed to depend on effectVector with length activeArms, stage, conditionalPower, conditionalCriticalValue, plannedSubjects/plannedEvents, allocationRatioPlanned, selectedArms, thetaH1 (for means and survival), stDevH1 (for means), overallEffects, and for rates additionally: piTreatmentsH1, piControlH1, overallRates, and overallRatesControl (see examples)."
        },
        {
          "name": "showStatistics",
          "required": false,
          "default": "FALSE",
          "description": "Logical. If TRUE, summary statistics of the simulated data are displayed for the print command, otherwise the output is suppressed, default is FALSE."
        }
      ]
    },
    {
      "name": "getSimulationRates",
      "family": "simulation",
      "title": "Get Simulation Rates",
      "description": "Returns the simulated power, stopping probabilities, conditional power, and expected sample size for testing rates in a one or two treatment groups testing situation.",
      "args": [
        {
          "name": "design",
          "required": false,
          "default": "NULL",
          "description": "The trial design. If no trial design is specified, a fixed sample size design is used. In this case, Type I error rate alpha, Type II error rate beta, twoSidedPower, and sided can be directly entered as argument where necessary."
        },
        {
          "name": "...",
          "required": true,
          "default": null,
          "description": "Ensures that all arguments (starting from the \"...\") are to be named and that a warning will be displayed if unknown arguments are passed."
        },
        {
          "name": "groups",
          "required": false,
          "default": "2L",
          "description": "The number of treatment groups (1 or 2), default is 2."
        },
        {
          "name": "normalApproximation",
          "required": false,
          "default": "TRUE",
          "description": "The type of computation of the p-values. Default is FALSE for testing means (i.e., the t test is used) and TRUE for testing rates and the hazard ratio. For testing rates, if normalApproximation = FALSE is specified, the binomial test (one sample) or the exact test of Fisher (two samples) is used for calculating the p-values. In the survival setting normalApproximation = FALSE has no effect."
        },
        {
          "name": "riskRatio",
          "required": false,
          "default": "FALSE",
          "description": "If TRUE, the design characteristics for one-sided testing of H0: pi1 / pi2 = thetaH0 are simulated, default is FALSE."
        },
        {
          "name": "thetaH0",
          "required": false,
          "default": "ifelse(riskRatio, 1, 0)",
          "description": "The null hypothesis value, default is 0 for the normal and the binary case (testing means and rates, respectively), it is 1 for the survival case (testing the hazard ratio). For non-inferiority designs, thetaH0 is the non-inferiority bound. That is, in case of (one-sided) testing of means: a value != 0 (or a value != 1 for testing the mean ratio) can be specified. rates: a value != 0 (or a value != 1 for testing the risk ratio pi1 / pi2) can be specified. survival data: a bound for testing H0: hazard ratio = thetaH0 != 1 can be specified. count data: a bound for testing H0: lambda1 / lambda2 = thetaH0 != 1 can be specified. For testing a rate in one sample, a value thetaH0 in (0, 1) has to be specified for defining the null hypothesis H0: pi = thetaH0."
        },
        {
          "name": "pi1",
          "required": false,
          "default": "seq(0.2, 0.5, 0.1)",
          "description": "A numeric value or vector that represents the assumed probability in the active treatment group if two treatment groups are considered, or the alternative probability for a one treatment group design, default is seq(0.2, 0.5, 0.1) (power calculations and simulations) or seq(0.4, 0.6, 0.1) (sample size calculations)."
        },
        {
          "name": "pi2",
          "required": false,
          "default": "NA_real_",
          "description": "A numeric value that represents the assumed probability in the reference group if two treatment groups are considered, default is 0.2."
        },
        {
          "name": "plannedSubjects",
          "required": false,
          "default": "NA_real_",
          "description": "plannedSubjects is a numeric vector of length kMax (the number of stages of the design) that determines the number of cumulated (overall) subjects when the interim stages are planned. For two treatment arms, it is the number of subjects for both treatment arms. For multi-arm designs, plannedSubjects refers to the number of subjects per selected active arm."
        },
        {
          "name": "directionUpper",
          "required": false,
          "default": "NA",
          "description": "Logical. Specifies the direction of the alternative, only applicable for one-sided testing; default is TRUE which means that larger values of the test statistics yield smaller p-values."
        },
        {
          "name": "allocationRatioPlanned",
          "required": false,
          "default": "NA_real_",
          "description": "The planned allocation ratio n1 / n2 for a two treatment groups design, default is 1. For multi-arm designs, it is the allocation ratio relating the active arm(s) to the control. For simulating means and rates for a two treatment groups design, it can be a vector of length kMax, the number of stages. It can be a vector of length kMax, too, for multi-arm and enrichment designs. In these cases, a change of allocating subjects to treatment groups over the stages can be assessed. Note that internally allocationRatioPlanned is treated as a vector of length kMax, not a scalar."
        },
        {
          "name": "minNumberOfSubjectsPerStage",
          "required": false,
          "default": "NA_real_",
          "description": "When performing a data driven sample size recalculation, the numeric vector minNumberOfSubjectsPerStage with length kMax determines the minimum number of subjects per stage (i.e., not cumulated), the first element is not taken into account. For two treatment arms, it is the number of subjects for both treatment arms. For multi-arm designs minNumberOfSubjectsPerStage refers to the minimum number of subjects per selected active arm."
        },
        {
          "name": "maxNumberOfSubjectsPerStage",
          "required": false,
          "default": "NA_real_",
          "description": "When performing a data driven sample size recalculation, the numeric vector maxNumberOfSubjectsPerStage with length kMax determines the maximum number of subjects per stage (i.e., not cumulated), the first element is not taken into account. For two treatment arms, it is the number of subjects for both treatment arms. For multi-arm designs maxNumberOfSubjectsPerStage refers to the maximum number of subjects per selected active arm."
        },
        {
          "name": "conditionalPower",
          "required": false,
          "default": "NA_real_",
          "description": "If conditionalPower together with minNumberOfSubjectsPerStage and maxNumberOfSubjectsPerStage (or minNumberOfEventsPerStage and maxNumberOfEventsPerStage for survival designs) is specified, a sample size recalculation based on the specified conditional power is performed. It is defined as the power for the subsequent stage given the current data. By default, the conditional power will be calculated under the observed effect size. Optionally, you can also specify thetaH1 and stDevH1 (for simulating means), pi1H1 and pi2H1 (for simulating rates), or thetaH1 (for simulating hazard ratios) as parameters under which it is calculated and the sample size recalculation is performed."
        },
        {
          "name": "pi1H1",
          "required": false,
          "default": "NA_real_",
          "description": "If specified, the assumed probability in the active treatment group if two treatment groups are considered, or the assumed probability for a one treatment group design, for which the conditional power was calculated."
        },
        {
          "name": "pi2H1",
          "required": false,
          "default": "NA_real_",
          "description": "If specified, the assumed probability in the reference group if two treatment groups are considered, for which the conditional power was calculated."
        },
        {
          "name": "maxNumberOfIterations",
          "required": false,
          "default": "1000L",
          "description": "The number of simulation iterations, default is 1000. Must be a positive integer of length 1."
        },
        {
          "name": "seed",
          "required": false,
          "default": "NA_real_",
          "description": "The seed to reproduce the simulation, default is a random seed."
        },
        {
          "name": "calcSubjectsFunction",
          "required": false,
          "default": "NULL",
          "description": "Optionally, a function can be entered that defines the way of performing the sample size recalculation. By default, sample size recalculation is performed with conditional power and specified minNumberOfSubjectsPerStage and maxNumberOfSubjectsPerStage (see details and examples)."
        },
        {
          "name": "showStatistics",
          "required": false,
          "default": "FALSE",
          "description": "Logical. If TRUE, summary statistics of the simulated data are displayed for the print command, otherwise the output is suppressed, default is FALSE."
        }
      ]
    },
    {
      "name": "getSimulationSurvival",
      "family": "simulation",
      "title": "Get Simulation Survival",
      "description": "Returns the analysis times, power, stopping probabilities, conditional power, and expected sample size for testing the hazard ratio in a two treatment groups survival design.",
      "args": [
        {
          "name": "design",
          "required": false,
          "default": "NULL",
          "description": "The trial design. If no trial design is specified, a fixed sample size design is used. In this case, Type I error rate alpha, Type II error rate beta, twoSidedPower, and sided can be directly entered as argument where necessary."
        },
        {
          "name": "...",
          "required": true,
          "default": null,
          "description": "Ensures that all arguments (starting from the \"...\") are to be named and that a warning will be displayed if unknown arguments are passed."
        },
        {
          "name": "thetaH0",
          "required": false,
          "default": "1",
          "description": "The null hypothesis value, default is 0 for the normal and the binary case (testing means and rates, respectively), it is 1 for the survival case (testing the hazard ratio). For non-inferiority designs, thetaH0 is the non-inferiority bound. That is, in case of (one-sided) testing of means: a value != 0 (or a value != 1 for testing the mean ratio) can be specified. rates: a value != 0 (or a value != 1 for testing the risk ratio pi1 / pi2) can be specified. survival data: a bound for testing H0: hazard ratio = thetaH0 != 1 can be specified. count data: a bound for testing H0: lambda1 / lambda2 = thetaH0 != 1 can be specified. For testing a rate in one sample, a value thetaH0 in (0, 1) has to be specified for defining the null hypothesis H0: pi = thetaH0."
        },
        {
          "name": "directionUpper",
          "required": false,
          "default": "NA",
          "description": "Logical. Specifies the direction of the alternative, only applicable for one-sided testing; default is TRUE which means that larger values of the test statistics yield smaller p-values."
        },
        {
          "name": "pi1",
          "required": false,
          "default": "NA_real_",
          "description": "A numeric value or vector that represents the assumed event rate in the treatment group, default is seq(0.2, 0.5, 0.1) (power calculations and simulations) or seq(0.4, 0.6, 0.1) (sample size calculations)."
        },
        {
          "name": "pi2",
          "required": false,
          "default": "NA_real_",
          "description": "A numeric value that represents the assumed event rate in the control group, default is 0.2."
        },
        {
          "name": "lambda1",
          "required": false,
          "default": "NA_real_",
          "description": "The assumed hazard rate in the treatment group, there is no default. lambda1 can also be used to define piecewise exponentially distributed survival times (see details). Must be a positive numeric of length 1."
        },
        {
          "name": "lambda2",
          "required": false,
          "default": "NA_real_",
          "description": "The assumed hazard rate in the reference group, there is no default. lambda2 can also be used to define piecewise exponentially distributed survival times (see details). Must be a positive numeric of length 1."
        },
        {
          "name": "median1",
          "required": false,
          "default": "NA_real_",
          "description": "The assumed median survival time in the treatment group, there is no default."
        },
        {
          "name": "median2",
          "required": false,
          "default": "NA_real_",
          "description": "The assumed median survival time in the reference group, there is no default. Must be a positive numeric of length 1."
        },
        {
          "name": "hazardRatio",
          "required": false,
          "default": "NA_real_",
          "description": "The vector of hazard ratios under consideration. If the event or hazard rates in both treatment groups are defined, the hazard ratio needs not to be specified as it is calculated, there is no default. Must be a positive numeric of length 1."
        },
        {
          "name": "kappa",
          "required": false,
          "default": "1",
          "description": "A numeric value > 0. A kappa != 1 will be used for the specification of the shape of the Weibull distribution. Default is 1, i.e., the exponential survival distribution is used instead of the Weibull distribution. Note that the Weibull distribution cannot be used for the piecewise definition of the survival time distribution, i.e., only piecewiselambda (as a single value) and kappa can be specified. This function is equivalent to pweibull(t, shape = kappa, scale = 1 / lambda) of the stats package, i.e., the scale parameter is 1 / 'hazard rate'. For example, getPiecewiseExponentialDistribution(time = 130, piecewiseLambda = 0.01, kappa = 4.2) and pweibull(q = 130, shape = 4.2, scale = 1 / 0.01) provide the same result."
        },
        {
          "name": "piecewiseSurvivalTime",
          "required": false,
          "default": "NA_real_",
          "description": "A vector that specifies the time intervals for the piecewise definition of the exponential survival time cumulative distribution function (for details see getPiecewiseSurvivalTime())."
        },
        {
          "name": "allocation1",
          "required": false,
          "default": "1",
          "description": "The number how many subjects are assigned to treatment 1 in a subsequent order, default is 1"
        },
        {
          "name": "allocation2",
          "required": false,
          "default": "1",
          "description": "The number how many subjects are assigned to treatment 2 in a subsequent order, default is 1"
        },
        {
          "name": "eventTime",
          "required": false,
          "default": "12",
          "description": "The assumed time under which the event rates are calculated, default is 12."
        },
        {
          "name": "accrualTime",
          "required": false,
          "default": "c(0, 12)",
          "description": "The assumed accrual time intervals for the study, default is c(0, 12) (for details see getAccrualTime())."
        },
        {
          "name": "accrualIntensity",
          "required": false,
          "default": "0.1",
          "description": "A numeric vector of accrual intensities, default is the relative intensity 0.1 (for details see getAccrualTime())."
        },
        {
          "name": "accrualIntensityType",
          "required": false,
          "default": "c(\"auto\", \"absolute\", \"relative\")",
          "description": "A character value specifying the accrual intensity input type. Must be one of \"auto\", \"absolute\", or \"relative\"; default is \"auto\", i.e., if all values are < 1 the type is \"relative\", otherwise it is \"absolute\"."
        },
        {
          "name": "dropoutRate1",
          "required": false,
          "default": "0",
          "description": "The assumed drop-out rate in the treatment group, default is 0."
        },
        {
          "name": "dropoutRate2",
          "required": false,
          "default": "0",
          "description": "The assumed drop-out rate in the control group, default is 0."
        },
        {
          "name": "dropoutTime",
          "required": false,
          "default": "12",
          "description": "The assumed time for drop-out rates in the control and the treatment group, default is 12."
        },
        {
          "name": "maxNumberOfSubjects",
          "required": false,
          "default": "NA_real_",
          "description": "maxNumberOfSubjects > 0 needs to be specified. If accrual time and accrual intensity are specified, this will be calculated. Must be a positive integer of length 1."
        },
        {
          "name": "plannedEvents",
          "required": false,
          "default": "NA_real_",
          "description": "plannedEvents is a numeric vector of length kMax (the number of stages of the design) that determines the number of cumulated (overall) events in survival designs when the interim stages are planned. For two treatment arms, it is the number of events for both treatment arms. For multi-arm designs, plannedEvents refers to the overall number of events for the selected arms plus control."
        },
        {
          "name": "minNumberOfEventsPerStage",
          "required": false,
          "default": "NA_real_",
          "description": "When performing a data driven sample size recalculation, the numeric vector minNumberOfEventsPerStage with length kMax determines the minimum number of events per stage (i.e., not cumulated), the first element is not taken into account."
        },
        {
          "name": "maxNumberOfEventsPerStage",
          "required": false,
          "default": "NA_real_",
          "description": "When performing a data driven sample size recalculation, the numeric vector maxNumberOfEventsPerStage with length kMax determines the maximum number of events per stage (i.e., not cumulated), the first element is not taken into account."
        },
        {
          "name": "conditionalPower",
          "required": false,
          "default": "NA_real_",
          "description": "If conditionalPower together with minNumberOfSubjectsPerStage and maxNumberOfSubjectsPerStage (or minNumberOfEventsPerStage and maxNumberOfEventsPerStage for survival designs) is specified, a sample size recalculation based on the specified conditional power is performed. It is defined as the power for the subsequent stage given the current data. By default, the conditional power will be calculated under the observed effect size. Optionally, you can also specify thetaH1 and stDevH1 (for simulating means), pi1H1 and pi2H1 (for simulating rates), or thetaH1 (for simulating hazard ratios) as parameters under which it is calculated and the sample size recalculation is performed."
        },
        {
          "name": "thetaH1",
          "required": false,
          "default": "NA_real_",
          "description": "If specified, the value of the alternative under which the conditional power or sample size recalculation calculation is performed. Must be a numeric of length 1."
        },
        {
          "name": "maxNumberOfIterations",
          "required": false,
          "default": "1000L",
          "description": "The number of simulation iterations, default is 1000. Must be a positive integer of length 1."
        },
        {
          "name": "maxNumberOfRawDatasetsPerStage",
          "required": false,
          "default": "0",
          "description": "The number of raw datasets per stage that shall be extracted and saved as data.frame, default is 0. getRawData() can be used to get the extracted raw data from the object."
        },
        {
          "name": "longTimeSimulationAllowed",
          "required": false,
          "default": "FALSE",
          "description": "Logical that indicates whether long time simulations that consumes more than 30 seconds are allowed or not, default is FALSE."
        },
        {
          "name": "seed",
          "required": false,
          "default": "NA_real_",
          "description": "The seed to reproduce the simulation, default is a random seed."
        },
        {
          "name": "calcEventsFunction",
          "required": false,
          "default": "NULL",
          "description": "Optionally, a function can be entered that defines the way of performing the sample size recalculation. By default, event number recalculation is performed with conditional power and specified minNumberOfEventsPerStage and maxNumberOfEventsPerStage (see details and examples)."
        },
        {
          "name": "showStatistics",
          "required": false,
          "default": "FALSE",
          "description": "Logical. If TRUE, summary statistics of the simulated data are displayed for the print command, otherwise the output is suppressed, default is FALSE."
        }
      ]
    },
    {
      "name": "getStageResults",
      "family": "analysis",
      "title": "Get Stage Results",
      "description": "Returns summary statistics and p-values for a given data set and a given design.",
      "args": [
        {
          "name": "design",
          "required": true,
          "default": null,
          "description": "The trial design."
        },
        {
          "name": "dataInput",
          "required": true,
          "default": null,
          "description": "The summary data used for calculating the test results. This is either an element of DatasetMeans, of DatasetRates, or of DatasetSurvival and should be created with the function getDataset(). For more information see getDataset()."
        },
        {
          "name": "...",
          "required": true,
          "default": null,
          "description": "Further (optional) arguments to be passed: thetaH0The null hypothesis value, default is 0 for the normal and the binary case (testing means and rates, respectively), it is 1 for the survival case (testing the hazard ratio). For non-inferiority designs, thetaH0 is the non-inferiority bound. That is, in case of (one-sided) testing of means: a value != 0 (or a value != 1 for testing the mean ratio) can be specified. rates: a value != 0 (or a value != 1 for testing the risk ratio pi1 / pi2) can be specified. survival data: a bound for testing H0: hazard ratio = thetaH0 != 1 can be specified. For testing a rate in one sample, a value thetaH0 in (0, 1) has to be specified for defining the null hypothesis H0: pi = thetaH0. normalApproximationThe type of computation of the p-values. Default is FALSE for testing means (i.e., the t test is used) and TRUE for testing rates and the hazard ratio. For testing rates, if normalApproximation = FALSE is specified, the binomial test (one sample) or the exact test of Fisher (two samples) is used for calculating the p-values. In the survival setting, normalApproximation = FALSE has no effect. equalVariancesThe type of t test. For testing means in two treatment groups, either the t test assuming that the variances are equal or the t test without assuming this, i.e., the test of Welch-Satterthwaite is calculated, default is TRUE. intersectionTestDefines the multiple test for the intersection hypotheses in the closed system of hypotheses when testing multiple hypotheses. Five options are available in multi-arm designs: \"Dunnett\", \"Bonferroni\", \"Simes\", \"Sidak\", and \"Hierarchical\", default is \"Dunnett\". Four options are available in population enrichment designs: \"SpiessensDebois\" (one subset only), \"Bonferroni\", \"Simes\", and \"Sidak\", default is \"Simes\". varianceOptionDefines the way to calculate the variance in multiple treatment arms (> 2) or population enrichment designs for testing means. For multiple arms, three options are available: \"overallPooled\", \"pairwisePooled\", and \"notPooled\", default is \"overallPooled\". For enrichment designs, the options are: \"pooled\", \"pooledFromFull\" (one subset only), and \"notPooled\", default is \"pooled\". stratifiedAnalysisFor enrichment designs, typically a stratified analysis should be chosen. For testing means and rates, also a non-stratified analysis based on overall data can be performed. For survival data, only a stratified analysis is possible (see Brannath et al., 2009), default is TRUE."
        },
        {
          "name": "stage",
          "required": false,
          "default": "NA_integer_",
          "description": "The stage number (optional). Default: total number of existing stages in the data input."
        },
        {
          "name": "directionUpper",
          "required": false,
          "default": "NA",
          "description": "Logical. Specifies the direction of the alternative, only applicable for one-sided testing; default is TRUE which means that larger values of the test statistics yield smaller p-values."
        }
      ]
    },
    {
      "name": "getSystemIdentifier",
      "family": "utility",
      "title": "Get System Identifier",
      "description": "This function generates a unique system identifier based on the platform, R version, and rpact package version.",
      "args": [
        {
          "name": "date",
          "required": false,
          "default": "NULL",
          "description": "A character string or Date representing the date. Default is Sys.Date()."
        }
      ]
    },
    {
      "name": "getTestActions",
      "family": "analysis",
      "title": "Get Test Actions",
      "description": "Returns test actions.",
      "args": [
        {
          "name": "stageResults",
          "required": true,
          "default": null,
          "description": "The results at given stage, obtained from getStageResults()."
        },
        {
          "name": "...",
          "required": true,
          "default": null,
          "description": "Only available for backward compatibility."
        }
      ]
    },
    {
      "name": "getTestLabel",
      "family": "utility",
      "title": "Get Test Label",
      "description": "Returns a string representation of the input value for use as a test label. Handles various types, including NULL, NA, vectors, and custom objects.",
      "args": [
        {
          "name": "x",
          "required": true,
          "default": null,
          "description": "The value to be converted into a test label."
        }
      ]
    },
    {
      "name": "getWideFormat",
      "family": "analysis",
      "title": "Get Wide Format",
      "description": "Returns the specified dataset as a data.frame in so-called wide format.",
      "args": [
        {
          "name": "dataInput",
          "required": true,
          "default": null,
          "description": null
        }
      ]
    }
  ]
}
